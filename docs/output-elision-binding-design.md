1. [generic] Preserve output elision as a relationship between source-bound input recipes and an output recipe, never as Unknown equality or a name comparison.
2. [profile data] Enable function-item output elision in Forms. Higher-ranked function types, closures, locals, and receiver adjustment are separate binders/obligations.
3. [generic] Add Output, OutputRegion, and OutputApplication recipe variants; no new public Type or TypeInfo field is needed.
4. [generic] Output recipes carry their own source-bound input recipes, so materialization does not depend on signature map iteration order or spelling lookup.
5. [generic] Retain parameter boundaries: exactly one input parameter may contribute lifetimes, and its regions must agree by ID. Repeated uses of one ID inside one tuple parameter agree; two lifetime-bearing parameters are ambiguous even with the same ID. No candidate, unknown evidence, unsupported nested binders, and bounded traversal failures remain Unknown. rustc::resolve_fn_params confirms this grouping.
6. [generic] Count before alias expansion: a written/omitted alias lifetime argument counts even when its RHS erases it; a hidden RHS lifetime does not count. rustc rejected the initial distinct-expanded-region interpretation; retain all three counterexamples as positive/negative guards.
7. [generic] Materialize only explicit OutputRegion markers with that identity. Explicit named/static regions and unsupported Unknown evidence must not be rewritten.
8. [generic] Fill omitted output lifetime slots only from the bound nominal owner's kinds; every omitted output slot shares the selected input region, without shifting explicit type arguments.
9. [generic] Re-materialize embedded input/output recipes against current bound owner kinds after provider changes, then persist these relationships for deletion and fresh-arena cold reload parity; no alias-expansion ordering pass is needed for elision.
10. [generic] Calls reuse the existing callee-ID inference/substitution path; output capture creates no new anonymous generic parameter.
11. [profile data] Fence receiver-bearing methods in this first slice: ignored/explicit self receivers cannot incorrectly allow an ordinary argument to determine an output region. Retain a legal receiver-output probe for the next slice.
12. [generic] Test named/static/elided inputs, repeated versus distinct regions, omitted output slots, aliases, call/member/field yields, ambiguity/zero-input barriers, source ownership, late batches, provider edits/deletion and cold reload.
13. [evidence] Promote the retained legal output probe only after exact-ID fresh/cold success, validate new positive/negative fixture legality with rustc, and retain the independent Rust target-label/99% gap.
14. Semantics: https://doc.rust-lang.org/reference/lifetime-elision.html and https://doc.rust-lang.org/nomicon/lifetime-elision.html; implementation evidence: https://raw.githubusercontent.com/rust-lang/rust/master/compiler/rustc_resolve/src/late.rs, resolve_fn_params (distinct candidates per parameter, then cross-parameter ambiguity). Syntax policy is profile data; identities are numeric and rustc 1.94.0 fixtures arbitrate edge cases.

## Verification and retained boundary — 2026-09-07

- ✅ [generic] The original `make(p: &Doc) -> Alias<'_>` call/member cascade now selects the exact expected declaration IDs both fresh and after cold reload. The new output cohort covers 19 positive/negative engine cases, plus a retained legal receiver case; all 20 agree with rustc 1.94.0 on acceptance or E0106 rejection.
- ✅ [generic] Selected core/profile regression suite: 2,069 passed, 23 explicitly ignored. All six explicit Rust compiler-legality groups passed (175 fixtures total). Existing TypeScript 5.9.3 oracle checks passed: scope cohort 67 cases / 136 labels; module cohort 25 cases / 79 labels.
- ✅ [generic] Provider edits cover late binding, lifetime arity changes, alias-erased versus hidden regions, explicit type-argument positions, deletion and fresh-arena reload. Filtered external declarations and serialized portable caches preserve the filtered function owner's anonymous region identity. Output capture creates no additional anonymous parameters.
- ✅ [generic] Binding epoch 16 / extraction schema 36 invalidate older structural recipes. New variants are internal recipes; this slice adds no public Type/TypeInfo fields. Native audit: 222 changed production Rust files, no file-budget violations or resolver string-identity pattern growth; `git diff --check` passes. These are targeted checks, not a full semantic proof.
- ❌ [generic] `impl C { fn make(&self, other: &Doc) -> Alias<'_> }` still loses the downstream `touch` target. Explicitly running the retained legal probe produces `[C.make]` instead of `[C.make, Alias.touch]`. Source capture deliberately fences receiver-bearing elision with Unknown until receiver borrow provenance survives projection/adjustment and callee-ID substitution; ordinary inputs must not impersonate the receiver.
- ⚠️ [generic] Higher-ranked binders, local/body inference variables, borrow/outlives/variance solving and independently compiler-labelled Rust occurrence targets remain open. No representative 99% recall/precision, consumer compatibility, latency, token-efficiency or competitor claim follows from this slice.
- ✅ [generic] Integration closeout: all 12 tests passed across incremental replacement/deletion/no-op, per-file manifests, per-package context and the existing TS/JS/Rust resolution corpora. Cargo sessions are terminal; no compiler process remains running from this slice.
- Follow-on slice: [receiver-output-binding-design.md](receiver-output-binding-design.md) supersedes the borrowed-receiver gap above. The original `p: &C` probe is now green; owned-value auto-borrow provenance remains a separate retained legal failure.
