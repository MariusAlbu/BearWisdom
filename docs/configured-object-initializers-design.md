1. Capture object literals and immediately invoked function returns as source-addressed initializer recipes.
2. Keep object identity separate from structural equality, using configured context, source instance and literal span.
3. Retain each member's source key, full signature and optional navigation row without synthetic declarations.
4. Bind closure reads through existing lexical BindingIds and source queries; never evaluate function bodies at runtime.
5. Infer supported direct objects and linear IIFE returns, preserving annotations and rejecting incomplete control-flow evidence.
6. Materialize member types and callable signatures in the selected source view, retaining generic owner identities.
7. Route object member reads and calls through source keys and exact origins before nominal member traversal.
8. Preserve object identity through substitutions, portable arenas, context refresh and cold reconstruction.
9. Verify independent compiler types and declaration targets with namesake, invalid, unsupported and stale-source controls.
10. Close the environmentManager imported-member cascade and compare every retained fresh/cold benchmark occurrence.
