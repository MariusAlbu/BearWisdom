1. Extend the generic callable and source-call applicability engines; no language hooks or spelling-based semantic recovery.
2. Distinguish fixed parameters, repeated array elements and correlated tuple/array rest alternatives in private proof data.
3. Resolve mutable/readonly array roles through selected canonical declaration IDs and validate all element/source contexts.
4. Follow pinned compiler positional comparison for array rests and full tail comparison for non-array alternatives.
5. Preserve required/optional arity, prefix positions, ordinary/method/callback variance and complete result obligations.
6. Expand homogeneous rest elements only to the supplied source-call argument count; reuse generic inference and exact overload selection.
7. Keep unknown, foreign, malformed and uncaptured variadic tuple/generic-rest shapes authoritative rather than guessing a signature.
8. Capture independent TypeScript diagnostics, callable/constructor relations, selected constructor origins and downstream declarations before implementation.
9. Verify portable/poisoned/navigation-row-absent/cold inputs plus configuration/provider/edit/deletion controls.
10. Run targeted shared-engine/language/integration tests, audit budgets/ID discipline and compare every frozen benchmark occurrence before roadmap updates.
