1. [generic] Treat anonymous input lifetimes as real parameter identities, not Unknown equality, a shared wildcard, or a spelling-based alias.
2. [profile data] Describe placeholder tokens and the function-input elision policy in source Forms; no language-name branch enters resolution.
3. [generic] Capture owner declaration slot plus source byte for each anonymous input region; omitted lifetime argument slots add their numeric parameter position.
4. [generic] Keep anonymous parameter IDs separate from explicit generic parameter order so explicit type arguments do not shift.
5. [generic] Retain source-addressed InputRegion/InputApplication recipes until nominal owner kinds are bound; never infer omitted slots from type text.
6. [generic] Provision anonymous GenericParamIds in canonical owner TypeInfo before materializing signatures, using numeric source-site keys; repeated capture of one AST site reuses its ID.
7. [generic] Only an attested leading lifetime-parameter group may be omitted. Explicit lifetime arguments keep their positions; wrong arity/kinds and unsupported consts fail closed.
8. [generic] Persist input recipes and anonymous parameter tables; provider kind/arity edits recompute applications and deletion cannot reuse the old head.
9. [generic] Reuse existing Region(GenericParamId), substitution and portable arena/cache remapping; no new runtime string identity map or new public Type variant is needed.
10. [generic] Call inference may bind the callee's anonymous input IDs, but never the caller's or receiver owner's IDs; missing/conflicting inference remains Unknown.
11. [generic] Limit the first slice to function-item input annotations, including nested nominal/reference arguments; body inference variables, output elision, self-receiver adjustments and higher-ranked function types need separate evidence.
12. [generic] Test fresh/cold exact targets, distinct sibling/nested source sites, same-site agreement, static/rigid mismatch barriers, call/yield cascades, omitted mixed arguments and provider edits.
13. [generic] Promote the retained legal Alias<'_> input probe only after passing exact-ID tests; retain a legal output-elision failure and compiler legality checks without claiming independent Rust target labels or 99%.
14. Semantics reference: https://doc.rust-lang.org/reference/lifetime-elision.html#lifetime-elision-in-functions — distinct input parameters; placeholder follows elision; output and receiver rules are separate obligations.

## Verification — 2026-09-07

- Implemented input-only source recipes in `namespace_types`, retained module recipes in `module_type_inputs`, numeric binder provisioning in `compilation_type_bindings`/`elided_inputs`, and callee-only anonymous inference in `bound_call`. Rust opts in through Forms data; resolution has no new string-identity relation.
- Anonymous slots are `(owner declaration ID, source byte, omitted argument index)`. Explicit region arguments do not allocate unused omitted slots; changed/deleted providers prune inactive slots. An unchanged active source slot retains its ID. TypeInfo's serde-defaulted anonymous table stays separate from explicit generic parameter order.
- Bound input recipes are re-materialized after provider binding, not frozen from a missing or stale declaration. Binding epoch 15 and extraction schema 35 invalidate earlier serialized recipe shapes.
- 2,063 selected core/profile tests passed, zero failed, 22 explicitly ignored; 5,287 outside the selected filter. Added source-anchor isolation, same-site signature/annotation agreement, call-environment ownership, kind/arity barriers, serde compatibility, late provider binding, lifetime arity 1→2→0→1, deletion, and fresh-arena cold reload checks.
- Fourteen elided-input fixture cases cover omitted/placeholder regions, bare and mixed-argument aliases, nominal fields, generic call/member/Self yields, sibling/nested owners, primitive-spelled alias shadowing, and wrong-referent abstention. Exact target expectations pass fresh and cold. These are regression labels, not compiler-emitted source-target labels.
- Five rustc acceptance/rejection groups passed (155 fixtures including 77 alias fixtures); both TypeScript 5.9.3 independent-label cohorts passed (67 cases / 136 call labels and 25 cases / 79 module labels). Compiler acceptance is not a Rust reference-binding oracle.
- Retained positive output probe explicitly fails: `make(p: &b::Doc) -> Alias<'_>` resolves `make` (row 17) but not downstream `Alias.touch` (row 16). Expected `[17, 16]`, actual `[17]`; rustc accepts the fixture. `namespace_types::lifetime_expr` still emits Unknown for output placeholders, so the missing input/output relationship is the next upstream cause.
- No measured 99% result, speed/token-efficiency result, borrow/outlives proof, output/local elision, higher-ranked binder support, or complete receiver adjustment is claimed. Full generic/default/const and invalid-code semantics remain broader obligations.
- All 12 selected integration tests passed: incremental (5), per-file manifest (2), per-package context (2), and TS/JS/Rust resolution corpora (1 each).
- Native Windows audit covered 221 changed production files: zero file-budget growth violations and zero string-identity pattern growth violations. `git -c core.safecrlf=false diff --check` passed. Existing warnings remain; no format/lint or workspace-wide test run was used.
- Public TypeInfo gained a serde-defaulted field. Read-only locked/offline consumer checks were attempted with targets inside BearWisdom: AlphaT fails dependency selection at yanked `der 0.8.0` via `ureq 3.3.0` / `ort 2.0.0-rc.12`; Lynx requires updating its lockfile. Neither reached compilation. Both consumer lockfile hashes were unchanged; compatibility is not claimed verified.
