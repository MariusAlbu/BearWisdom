1. [generic] Capture detached-body owner paths against the namespace binder's source scopes and NameIds; resolve after declarations/imports have been reserved.
2. [profile data] Supply extension nodes, target/member fields and unsupported applicability syntax as data, without a language branch in the engine.
3. [generic] Install a source Self binding in each extension scope, so signatures and body annotations consume the same owner binding as member attachment.
4. [generic] Preserve declaration slots for every captured member, including unresolved owner barriers; never attach through a printed qualified name.
5. [generic] Persist owner BindingId, origin SourceModuleId and member declaration IDs in fingerprinted ModuleInput recipes; advance the binding epoch.
6. [generic] Require a uniquely bound nominal owner and numeric same-crate declaration origin before attaching inherent members; foreign, conditional, missing and ambiguous evidence must not grant membership.
7. [generic] Rebuild semantic member/enclosing-owner indices after module bindings, clearing prior captured attachments before retargeting; keep member privacy anchored to its declaration module.
8. [generic] Rebind Self return/field recipes after owner selection and preserve fresh/cold and late-batch behavior without cached-name resurrection.
9. [generic] Verify exact IDs for qualified/renamed/forward owners, namesake isolation, private descendants, negative targets and changed/deleted providers; validate fixture legality independently with rustc.
10. [generic] Generic/constrained applicability, trait dispatch and type-alias owners require explicit semantics; unknown is not a successful binding. Full 99% and IDE query parity remain separate verification gates.
11. [generic] Implemented unconditional blanket mappings: source generic argument BindingIds must bijectively match the impl parameter BindingIds. Map them to the nominal owner's GenericParamId positions, including permutations; persist SourceParameter recipes and Self applications so changed owners cannot retain stale generic IDs.
12. [generic] Verified explicit rejection for foreign-crate/missing/ambiguous/conditional owners and unsupported syntax; legal type-alias ownership remains an explicitly failing ignored probe, not a successful negative case. Generic bounds, specialization, lifetime/const parameters and complete applicability barriers remain open.
13. [generic] Semantic member/enclosing-owner indices are reconstructed from fingerprinted recipes; this does not migrate every database-only containment/navigation query. Actual provider source edits and deletion rebind unchanged impl members and Self returns across cold reload.
14. [generic] Alias-owner slice: expand only source-bound alias TypeId templates, with bounded recursion; require the terminal nominal declaration to belong to the impl's crate.
15. [generic] Retain a member-ID-keyed receiver pattern for alias impls, rather than granting membership to every application of the terminal nominal.
16. [generic] Match bound nominal IDs, precise primitive IDs, generic parameter IDs and structural applications; unresolved, cyclic or lossy types cannot prove applicability.
17. [generic] Typed member selection filters provably inapplicable candidates and propagates unknown applicability as a hard barrier; ID-only selection cannot approve a restricted receiver.
18. [generic] Carry the matched alias GenericParamId environment into member returns and call inference, preserving permutations independently of nominal parameter spelling.
19. [profile data] Capture exact intrinsic scalar identities from primitive syntax nodes. Coarse numeric inference categories remain distinct from signed/unsigned/width-specific source types.
20. [generic] Alias and nested generic recipes must not erase reference/pointer/lifetime structure into false equality. Unsupported exact structures remain unknown until represented losslessly.
21. [generic] Rebind alias/signature recipes before rebuilding member applicability after fresh ingestion, provider edits and cold reload; persist inputs rather than derived match results.
22. [generic] Test plain, chained, permuted and fixed-argument aliases, namesake and primitive mismatches, cycles, foreign terminal owners and stale-state cleanup; independently check Rust fixture legality.
23. [generic] This slice does not establish compiler-target-labelled Rust recall, full trait/constraint semantics, lossless reference/lifetime/const types, or the overall 99% gate.
24. [generic] Type-only source declarations fence unavailable runtime references, just like type-only imports. Static alias receivers require a captured type-space fact; a TypeAlias kind alone is not permission to synthesize a value.
25. [generic] Direct class/function value initializers retain their source BindingId provenance. This preserves real value shadowing of type-only aliases without reviving global name fallback; arbitrary value-copy/assignment flow remains separate work.
26. [generic] Verified alias slice (2026-09-07): binding epoch 12; 2,019 selected core tests passed, 22 explicitly ignored. Twenty alias fixture legality results agree with rustc, including the retained legal reference-argument probe that still fails engine resolution. These are not compiler-provided member target labels.
27. [generic] Exact scalar source capture first consults the source scope's type binding, so a user declaration named i32 wins over the intrinsic fallback. Primitive syntax only is decoded through profile data; String is not treated as an intrinsic identity.
28. [generic] Represent exact indirection arguments structurally: child TypeId, mutability, reference-versus-pointer kind, and an explicit lifetime category; no type spelling participates in matching.
29. [profile data] Decode reference/pointer CST nodes, mutable specifiers and static lifetime tokens at ingestion through namespace type forms.
30. [generic] Static lifetimes are attested; elided or named lifetimes without a bound region recipe remain Unknown. Unknown-versus-Unknown must never prove applicability.
31. [generic] This stage preserves exact alias RHS and generic argument structure; legacy root reference projection is not a lifetime solver and is not represented as exact equality.
32. [generic] Recursively retain indirection through source binding, import-dependent signature recipes, alias expansion and GenericParamId substitution.
33. [generic] Carry the same structure through arena snapshots, merged snapshots and portable parse-cache trees, remapping child IDs in each arena.
34. [generic] Match kind and mutability before referent IDs; preserve uncertainty for unresolved lifetimes and unsupported coercion/variance evidence rather than inventing equality.
35. [generic] Expand aliases within referents with the existing declaration-ID cycle and work bounds; reference structure cannot hide a recursive alias.
36. [generic] Verify static shared/mutable references, raw pointers, nested tuples, generic referent substitution, same-spelled distinct declarations and negative shape controls fresh and cold.
37. [generic] Verify persisted import recipes retarget referent IDs and deletion leaves Unknown, not a stale nominal. Advance binding/extraction epochs for changed source recipes.
38. [generic] Promote the retained legal reference-alias probe only after it passes. Check fixture legality with rustc; do not claim compiler target labels or a measured 99% result.
39. [generic] Keep the broad roadmap item open: lifetime parameter binding/outlives/variance, const expressions, direct constrained impls and independent Rust source-occurrence target labels still require work.
40. [generic] Root-projection follow-up: preserve Indirect TypeIds in annotations, fields, returns and parameter recipes, not only alias/application arguments.
41. [profile data] Make reference member projection an explicit opt-in language-profile axis; raw pointers never borrow their pointee's member index implicitly.
42. [generic] Normalize receiver aliases and permitted reference projections at the member-walk boundary, using a bounded TypeId cycle set and clearing stale owner IDs when shape changes.
43. [generic] Preserve the original type fact and terminal yield: a projected lookup receiver is not an assignment, argument conversion or replacement of the source annotation.
44. [generic] Apply the same normalization at roots, tuple/array projections, intermediate yields and existing overload alternative yields; keep legacy smart-wrapper handling isolated.
45. [generic] Unsupported reference projection and raw pointer roots keep no nominal receiver ID, preventing alias-carried IDs from escaping the structural barrier.
46. [generic] Match indirection structurally during generic referent inference without turning inferred arguments into proof of lifetime/borrow-check validity.
47. [generic] Test direct/aliased/nested reference roots, fields and returned references, preservation of generic reference arguments, and rejection of implicit raw-pointer member borrowing.
48. [generic] Retain separate unresolved-lifetime tests; reference projection is not borrow checking, autoderef trait selection, autoref/mutability validation or outlives solving.
49. [generic] Re-run the selected engine/core and integration gates, compiler legality cohorts, source identity audit and consumer compile checks before recording completion of this slice.
50. [generic] Implemented both stages: all supported source indirections now remain structural in root annotations as well as exact arguments; the earlier stage-only root erasure described in line 31 has been removed. Member projection is enabled by Rust profile data and defaults off in other full profile initializers.
51. [generic] Verification (2026-09-07): 2,044 selected core/profile tests passed, 22 ignored; 48 alias fixture legality checks agree with rustc (126 across the five Rust groups), including the still-failing named-lifetime engine probe. TS compiler labels remain 136 scope calls and 79 module calls.
52. [generic] Receiver projection uses a fixed 32-entry TypeId cycle buffer, avoiding a new per-hop heap allocation. Original reference facts/terminal yields are not rewritten to the projected nominal type; nested generic inference distinguishes Item<&'static Doc> from Item<Doc>.
53. [generic] Final verification: 12 integration tests passed; dirty-production audit covered 219 files with zero line-budget/identity-pattern issues and diff --check passed. Consumer checks remain unverified before compilation: AlphaT's locked offline dependency resolution hits yanked der 0.8.0 through ureq/ort; Lynx requires a lockfile update. Neither consumer lockfile was changed.
