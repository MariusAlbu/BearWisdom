# Build
/target*
web/dist/

# Build-generated (build.rs regenerates these from the grammars on every build — do not track)
/crates/bearwisdom/src/indexer/query_builtins.rs
/crates/bearwisdom/src/indexer/query_builtins/

# Data
/models
*.db
.bearwisdom/
.mcp.json

# Dependencies
node_modules/

# Benchmark output
bench-archive/
bench-results/

# Local / session
# Resolution fix-specs / plans / analyses — working notes, not source.
resolution-documents/
# The tracked baseline is baseline.json — subset baselines must NOT be created (update it directly).
baseline-*.json
!baseline.json
.claude/
*.ndjson
.env
research/
scripts/
benchmark.md
CLAUDE.md
!crates/**/CLAUDE.md
.r-lib-cache/
.tmp/*
_catalogue/*