# Resolution roadmap — from 69.98% to the +20-point re-census gate

Baseline `281f8515`: corpus 69.98% edge-weighted, 9.28M resolved / 3.99M residue.
Census: `resolution-documents/2026-09-04-corpus-residue-census.md`.

**Gate (last milestone):** one full corpus recapture, run only when targeted recaptures
on the smoke set project **≥ 90.0% edge-weighted** (+20 points; ≈ 2.65M of the 3.99M
residue must resolve). Assumption: "20+% more resolved" means +20 percentage points; if it
means 20% of the residue (+0.8M refs, ≈ 76.5%) the gate moves, nothing else changes.

Discipline that applies to every milestone: generic engine first, profile data second,
hooks only when data cannot express it; no builtin lists; targeted recaptures only;
every fix must flip a cascade, not one ref; commit per landed step.

Smoke set for targeted recaptures: the 15-project TS/JS/Vue corpus plus zig-compiler-fresh,
puppet-core (ruby), php-laravel, velocity-apache-struts (java), kotlin-ktor,
pascal-castle-fresh, dart-serverpod, fsharp-fable, make-curl (c), scala-gatling.

## M0 — Hygiene

- [x] `covers()` O(packages) → ancestor walk in `SymbolLocationIndex` (ts-ever-demand 43 → ~4 min)
- [x] commit the `covers()` change
- [x] worktrees: research `target/` (6.3 GB) removed, metadata pruned, 39 merged branches deleted
- [x] decide the 48 unmerged `worktree-*` / agent branches (list in session report; delete or keep)
- [x] dogfood index rebuilt without the 60,716 stale `.claude/worktrees/*` rows; DB vacuumed (5.6 GB + 1.5 GB WAL)
- [x] walker: never index `.claude/worktrees/**` even when the secondary import-pull scan would pull it (root cause was the file watcher bypassing the exclusions; fixed in `indexer/watch_filter.rs`)
- [x] `bw_search` returns worktree copies before the main tree — verify gone after rebuild

## M1 — Honest census (generic cause recording; expected rate change 0)

Landed: commits `b48f4089` (covers), watcher exclusions, cause recording.

The recorded `cause_kind` misfiles ~663k chain deaths as bare-root deaths. Fix recording
first, re-census, then rank resolution work on the honest numbers.

- [x] `engine/chain_root.rs:69` — value-shaped untypable root records the same-file binding's `untyped_binding`/`uncaptured_field`, else the new `untyped_root`; never `classify_unbound_root(root name)`
  - [x] failing test first: two-segment chain, root is an untyped local, assert `UntypedBinding` with the local blamed
- [x] `engine/semantic_model.rs` fallthrough keeps the walk's cause when the bare ladder also misses (member cause survives)
- [x] `engine/semantic_model.rs:92` `chain_root_is_namespace` — only a namespace the FILE binds (import, ambient, same-package) qualifies; a same-named namespace elsewhere in the index must not re-run the chain as a bare ladder
  - [x] failing test first: field `mapper` shadowed by package `x.mapper`; the root stays a value
- [x] `engine/compilation.rs:2396` `is_external_name` — wire to the externals surface so `unbound_external_known` fires; stdlib names leave `name_unknown`
- [x] `engine/chain.rs:2063` — the untyped-root floor now records `untyped_root` / the same-file binding; `:316` is unreachable (namespace anchor always leaves one segment)
- [x] package-alias roots (`utilsstrings.ToLower`, `clientpkg.New`) record `import_unlinked`, not `name_unknown`
- [x] re-census on 14 projects (addendum in the census doc): `external_known` 242k is the largest honest bucket → M3 reachability outranks M4 supply
- [x] import discipline: external-head attestation removed (denied roots the scoped lookup could not see; superset −7.8k); re-enable only with materialized-module evidence (M6)
- [x] `resolution_corpus_rust` fails at baseline `281f8515` (SelfProbe nested-module import binds the crate-root twin; AliasDoc root dies `uncaptured_field`) — pre-existing, trace and fix

## M2 — Root typing (generic engine; the largest resolution lever)

Receiver-rooted chains whose root local/param/field has no captured type: zig 91%,
lua 79%, elixir 70%, ts 63%, js 54%, rust 51%, java 47%, kotlin/fsharp 48% of their
ROOT-family rows.

- [x] audit: only java/python/ruby/swift emit parameter symbols; typed params surface as `Property` in ts/csharp/go; flow runner now synthesizes binding symbols and 11 languages' flow queries seed parameter + typed-local types (`132f2482`)
- [ ] lambda / closure params typed from the callee's parameter types (call-site arg→param binding, generic)
  - [x] exact scoped callback parameter seeding for Scala, Java, C#, Kotlin, Swift, Dart, Go, Python, and adjacent-PHPDoc PHP contracts
  - [ ] Ruby prerequisite: ingest selected RBS/RBI callable block contracts and capture trailing-block, Proc, and lambda parameter declaration spans as positional call arguments; seed only exact scoped bindings after arity/selected-callee proof, preserving nested/shadowed blocks and fresh/cold reload
- [x] `this` / `self` root fails enclosing-type lookup (scala `this.modify`) — trace, fix the id-keyed enclosing lookup
- [x] pointer-sigil types (`*fiber.Ctx`) intern as the pointee (`922032c0`); go-fiber +7,067 edges once parameters seeded
- [x] Go local seeded with the callee's QNAME as its type (`client → client.NewWithClient`) — return-type capture vs seed fallback, trace first
- [ ] pattern / destructuring bindings (`case (key, inputs)`, `const { a } = …`) seed element types
- [x] targeted recapture (14 census projects, `5c893f20`): +23,995 edges (+1.7%), aggregate 63.83% → 64.90%; go-fiber 55.19 → 63.50, php-laravel 50.46 → 56.15, kotlin-ktor +0.7, rust-tantivy +0.8, java-petclinic +1.0, puppet +0.3, python-superset +0.1; nothing regressed
- [ ] remaining root typing: lambda params from callee params, `this`/self enclosing lookup, Go callee-qname seed, destructuring — the census after M2 still holds untyped_binding 56k + untyped_root 17k on these 14

## M3 — Reachability rungs (generic engine)

Genuinely bare names whose declaration IS in the index.

- [ ] wildcard-import re-export following (`import 'package:test/test.dart'` → `src/expect.dart`; scala `_`, kotlin `*`)
- [ ] transitive `#include` closure + `NamespacelessGlobalRule` reaching supplied external declarations (C `FILE`/`fprintf`: found by name, no rung reaches it)
- [ ] TS import specifier normalization (`node:path` vs `path` — 63% of ts `import_unlinked` are present by qname)
- [ ] PHP namespace `use` + qualified member (`Illuminate\Database\Eloquent` present, member unlinked)
- [ ] inherited implicit-receiver members when the enclosing type's parent is external (pascal `Create`, `AddField`)
- [ ] targeted recapture: dart-serverpod, make-curl, ts-nextjs, php-laravel, pascal-castle-fresh; cascade gate

## M4 — Supply (locators and install state; no engine code)

`name_unknown` where nothing is supplied: zig 10 external files, swift 0, r 5,
fortran 0, powershell 11, fsharp 124.

- [ ] install zig, swift, R toolchains; reindex their projects (rates are gated on install state)
- [ ] route F# to the dotnet stdlib supply that csharp already gets (77k external files)
- [ ] Maven / Gradle `-sources` jars for the java and kotlin corpora (java 86% of unresolved imports are absent from the index)
- [ ] dart pub `path:` workspace packages (serverpod monorepo: `package:serverpod_client` absent)
- [ ] zig `@import("../std.zig")` path resolution as `ImportAxes.import_resolution` profile data
- [ ] vue-vben-admin −8.9 (pnpm symlink layout vs registered prefixes) — trace, then fix in the npm locator
- [ ] targeted recapture per ecosystem touched

## M5 — Extractor attribution gaps (per-language extractor data, no resolver code)

- [ ] pascal: implementation-section bodies attributed to their declaring class (55,467 residue rows have the file-level `unknown` symbol as source)
- [ ] ruby (and any extractor that leaves symbol-less files): emit a file-scope symbol so the file's refs enter the pipeline — today `pipeline.rs:334` skips every ref of a file with no symbols (puppet-core spec files hide ~160k refs, ~32k of them resolvable); binding synthesis deliberately stays under enclosing declarations (`5c893f20`) until this lands
- [ ] python / ruby: `self.x` / `@x` instance attributes assigned in method bodies are contract (paperless −3.9, chatwoot −3.8)
- [ ] pascal visible-refs artifact (castle −1.7, heidisql −3.3): confirm by cause census, document
- [ ] targeted recapture: pascal-castle-fresh, pascal-heidisql, python-paperless-ngx, ruby-chatwoot

## M6 — Member-walk residue (generic engine + externals member tables)

- [ ] `member_missing` on external receivers (typescript 64k): external member tables, declaration merging, overload alternative yields
- [ ] remaining `chain_declined` after M1 recording: trace top cascades by receiver type
- [ ] `alias_opaque`: Union / Intersection member walk where every branch carries the member
- [ ] targeted recapture: react-tanstack-query, ts-trpc, ts-inbox-zero, dotnet-squidex

## Gate — Re-census and full corpus recapture

- [ ] targeted recaptures on the smoke set project ≥ 90.0% edge-weighted corpus-wide (+20 points)
- [ ] one full 262-project recapture; baseline committed; census doc rewritten
- [ ] memory + brain updated with the closeout

## F0 — Occurrence-level evidence for the 99% correctness target

Accepted 2026-09-05 after the independent engine audit. This foundation sequence
is the current implementation priority; M0–M6 above remain the corpus backlog.
The historical edge-weighted gates are trend metrics, not proof of correctness.
Target: ≥99% correct binding recall and ≥99.9% binding precision on an explicitly
supported, independently labelled static-reference corpus. Report extraction
coverage and unsupported/dynamic cases separately; never count guesses as truth.
Strings belong at ingestion, persistence and display boundaries. Semantic work
must carry declaration/binding/type IDs, not re-find declarations by spelling.

- [x] Persist an exhaustive per-file extracted-reference census, including duplicates, primitives, drains, unsupported profiles and missing source attribution.
- [x] Expose occurrence binding coverage separately from graph-edge rates; surface stale/missing measurements and symmetric snippet/generated exclusions.
- [x] Verify the census on same-line repeated calls, symbol-less files, and full/incremental replacement/deletion; preserve existing resolution outcomes.
- [x] Build an independently labelled occurrence-level oracle: correct / incorrect / unresolved / not extracted; confidence is not correctness.
- [ ] Establish per-language baselines and capture semantic retargeting, not just edge-count changes.
  - [x] Seed source-addressed call fixtures for TypeScript, JavaScript, Python and C#; pin independent labels and observed snapshots, and gate per-site regressions/retargeting (2026-09-06; 11 sites, not a representative corpus).
  - [ ] Expand to representative supported-language cohorts and independently validate labels against compiler semantics; retain negative, extraction-gap and ambiguity cases.
    - [x] Add a pinned rustc DefId/source-span target verifier and configuration-aware fresh/cold Rust oracle; independently validate 47 positive targets and five diagnostic-backed negatives across 21 cases, preserve the observed false-positive baseline and gate per-site regressions (2026-09-07; 41 correct positives, six unresolved, zero wrong targets after fixes; authored diagnostic cohort, not representative 99% evidence).
    - [x] Add six compiler-target-labelled owned-receiver fixtures with 23 positive calls and strict fresh/cold occurrence gates; retain original observations and verify static trait declaration targets separately from implementation-body identity (2026-09-07; combined diagnostic cohort 65/70 correct positive targets, five unresolved, zero wrong targets and five correct negatives; 12 compiler-adapter tests; not representative per-language 99% evidence).
    - [x] Add 12 independently compiler-validated trait cases covering 13 positive targets and five diagnostic-backed negatives; preserve the discovered receiver-precedence wrong target in an explicit per-site observation baseline and retain fresh/cold and negative gates (2026-09-07; expanded diagnostic cohort 66/83 correct positives, 16 unresolved, one wrong target and ten correct negatives; the earlier 65/70 cohort is unchanged, not a representative 99% result).
    - [x] Add six independently compiler-validated trait-selection cases with eight positive targets and one negative; protect newly corrected historical sites with strict per-occurrence gates without changing gold labels or old snapshots (2026-09-07; combined 45 cases / 86 of 91 correct positives / five unresolved / zero wrong / 11 correct negatives / 102 extracted, fresh/cold equal; not representative 99% evidence).
    - [x] Add eight independently compiler-validated qualified-call cases with 18 positive targets and one negative; close the three retained qualified misses and preserve every corrected historical site (2026-09-07; combined 53 cases / 107 of 109 correct positives / two unresolved / zero wrong / 12 correct negatives / 121 extracted, fresh/cold equal; not representative 99% evidence).
    - [x] Add eight independently compiler-validated explicit-borrow cases with 20 positive targets and one negative; preserve shared/mutable/nested references, distinct receiver/ordinary output regions, generic/Self values and cross-file namesake isolation (2026-09-07; combined 61 cases / 127 of 129 correct positives / two unresolved / zero wrong / 13 correct negatives / 142 extracted, fresh/cold equal; unchanged earlier cohort, not representative 99% evidence).

    - [x] Add eight independently compiler-validated source-argument cases with 28 positive targets and one negative; close six local-return misses and prove poisoned/empty display operands do not alter argument, qualified or borrow cohort reports fresh/cold (2026-09-07; combined 69 cases / 155 of 157 correct positives / two unresolved / zero wrong / 14 correct negatives / 171 extracted; not representative 99% evidence).

    - [x] Add nine independently compiler-validated local-value cases with 22 positive targets and one negative; close 13 borrow-local/return misses and preserve exact targets under poisoned annotation/display data fresh/cold (2026-09-07; combined 78 cases / 177 of 179 correct positives / two unresolved / zero wrong / 15 correct negatives / 194 extracted; not representative 99% evidence).

    - [x] Add twelve independently compiler-validated place-expression cases with 41 positive targets and one negative; close nineteen field/tuple/dereference operand misses and retain raw-field direct-chain controls, poisoned-display and fresh/cold gates (2026-09-07; combined 90 cases / 218 of 220 correct positives / two unresolved / zero wrong / 16 correct negatives / 236 extracted; not representative 99% evidence).

    - [x] Add seven independently compiler-validated owned enum pattern cases with sixteen positive targets and one negative; close both retained match-payload misses, enforce strict historical-site gates, and cover generic/nested/named/imported/guard cascades fresh/cold (2026-09-07; combined 97 cases / 236 of 236 correct positives / zero unresolved or wrong / 17 correct negatives / 253 extracted; authored diagnostic cohort, not representative 99% evidence).

    - [x] Capture all compiler call expressions in a pinned real-project TypeScript source cohort before engine evaluation; retain unsupported evidence, configuration/source hashes, strict per-site fresh/cold reports and separate declaration-kind disagreements (2026-09-07; Query Core: 24 selected files, 843 calls, 690 labels, zero compiler diagnostics; development baseline only, not representative or held-out 99% evidence).
    - [x] Add independently compiler-validated base-receiver fixtures: 13 TS cases / 25 labels and seven JS cases / nine labels, including generic ancestors, callbacks, abstract exports, namesakes and explicit negatives; retain fresh/cold and poisoned-display gates (2026-09-07; diagnostic cohort, not representative 99% evidence).
    - [x] Add five compiler-validated ambient/global identity cases with 14 positive call targets and one diagnostic-backed negative; preserve a separate observed baseline (12 correct positives, two unresolved, one false positive) and exact fresh/cold regression gates without relabelling failures (2026-09-07; diagnostic cohort, not a completed ambient binder or 99% result).
    - [x] Add five independently compiler-validated configured-program source cases with 12 positive labels covering array-owned callbacks, generic ambient function returns, type/value shadowing and explicit global augmentation; retain compiler declaration coordinates and strict fresh/cold targets (2026-09-07; diagnostic evidence, not real stdlib or representative 99% coverage).
    - [x] Add versioned compiler source-scope capture and explicit legacy/configured-program real-project evaluation without changing the 843-call/690-label Query Core population; persist source-input barriers and strict fresh/cold reports (2026-09-07; configured mode currently abstains on all 690 labels because real program capture is incomplete; this is measured failure evidence, not a correctness-gate pass).

## F1 — Lexical and declaration identity before resolution breadth

- [ ] Replace file-flat local name caches with scoped binding IDs; cover sibling functions, block shadowing, closures and declaration-order rules.
  - [x] Introduce TS/JS syntax scopes and BindingId-keyed typed facts; fix sibling/block/annotated-closure member roots and add 16 compiler-validated call labels (2026-09-06; initial slice, not full lexical semantics).
  - [ ] Carry precise binding/argument spans through contextual callback writes, bare identifier/call binding and legacy symbol-slot correlation; extend supported scope forms and languages.
    - [x] Capture TS/JS callback parameter spans and write contextual TypeIds by BindingId for bare/member/generic/nested calls; preserve annotations and isolate sibling callbacks (2026-09-06).
    - [x] Prebind TS/JS local bare calls by occurrence → BindingId → exact persisted declaration ID; repair scoped symbol-slot correlation and preserve separate callable provenance (2026-09-06; 40 compiler-checked TS call labels; broader scope forms/non-call identifiers remain open).
    - [x] Bind TS/JS named functions/classes, bare constructors and chain roots by declaration ID; retain explicit generic arguments and GenericParamId return templates, close lexical namespace fallthrough, and repair ID-keyed wrapper-return writes/nested call ownership (2026-09-06; 58 compiler-checked call labels; full scope/import/generic semantics remain open).
    - [x] Give named TS/JS function/class expressions private binding scopes and distinct outer-variable provenance; retain generic call/constructor yields, persist lexical-only IDs, and stop DB reload from collapsing same-qname rows/type metadata (2026-09-06; 76 compiler-checked call labels; anonymous expressions, annotation-head binding, imports and full snapshot semantics remain open).
- [ ] Carry NameId → BindingId/SymbolId → TypeId through binding and member lookup without format-and-re-resolve bridges.
  - [x] Capture and persist separate trait declaration/implementation/member IDs, scoped generic-bound and receiver recipes, anonymous trait import BindingIds, negative-impl polarity and shared parent-linked scope environments; preserve provider edits/deletion and filtered/portable/fresh-arena cold metadata (2026-09-07; binding epoch 20 / extraction schema 40; prerequisite inputs only, ordered trait selection, applicability solving, Self/region substitution and qualified calls remain open).
    - [x] Select supported dot-call inherent/trait candidates in receiver order using declaration, member-name, type and generic-parameter IDs; bind trait/impl Self, scoped bounds and method obligations, preserve static targets and return cascades, and keep incomplete providers authoritative (2026-09-07; epoch 21 / extraction schema 41; 2,124 selected core/profile tests and 12 integrations passed; unchanged earlier compiler cohort improves 66/83 to 78/83, not full trait semantics).
    - [ ] Bind qualified trait/UFCS calls and their explicit receiver arguments through source IDs; close the three retained compiler-labelled qualified-call misses, then extend associated/higher-ranked/const obligations and full receiver/coercion evidence.
      - [x] Bind explicit Self/trait qualified calls through persisted source recipes, static member IDs and shared obligation proofs; keep explicit receiver arguments separate from ordinary parameters, preserve generic/Self returns and local cascades, and close the three compiler-labelled misses fresh/cold (2026-09-07; epoch 22 / extraction schema 42; named receiver values use BindingIds and physical signature slots).
      - [ ] Capture explicit borrow/value expression argument provenance, complete implicit-Self UFCS and coercion/region evidence, then extend associated projections and higher-ranked/const obligations; unsupported qualified calls must remain authoritative misses.
        - [x] Capture explicit reference arguments through profile-attested BorrowAt spans, operand BindingIds, physical caller IDs and source mutability; preserve qualified shared/mutable/nested return/local cascades and exact receiver-versus-ordinary region substitution, provider edits/deletion and filtered/portable/cold metadata (2026-09-07; binding epoch 23 / extraction schema 43; 2,139 selected core/profile tests passed, no outlives or borrow-checker claim).
        - [ ] Carry source-addressed argument syntax through remaining bare and terminal-call APIs without consuming legacy display operands; bind borrow-valued local initializers, supported value/place expressions and closure-owned regions before claiming general explicit-borrow coverage.
          - [x] Capture semantic call operands by exact source selector, bind identifier leaves once through BindingIds, and route bare/pretyping/member/namespace/qualified consumers through authoritative source evidence; preserve exact return and region IDs under poisoned display operands, provider edits/deletion and filtered/portable/cold reload (2026-09-07; binding epoch 24 / extraction schema 44; 2,146 selected core/profile tests; borrow-valued locals, general value/place expressions and closure ownership remain open).
          - [x] Replace copy-only local initialization with source-bound read/borrow recipes, lower surviving caller slots to declaration IDs and evaluate position-correct operand facts; preserve nested/mutable/shadowed/imported/Self cascades and refine unknown annotation regions only under exact identity evidence, with provider edits/deletion and filtered/portable/cold checks (2026-09-07; binding epoch 25 / extraction schema 45; 2,154 selected core/profile tests; place expressions, closure ownership, CFG and full region obligations remain open).
          - [x] Share source-addressed named-field, tuple-position and explicit-dereference recipes between arguments and local initializers; lower field NameIds once, enforce declaration-ID access and generic substitution, decode raw field identifiers at ingestion, and preserve provider edits/removal plus filtered/portable/cold checks (2026-09-07; binding epoch 26 / extraction schema 46; 2,163 selected core/profile tests and twelve integrations passed; nested call operands, patterns, closure ownership and full coercion/region semantics remain open).
          - [x] Bind owned enum match payloads through arm-local BindingIds, physical variant/field declaration slots, source-bound enum heads and MemberNameId projection; preserve generic substitution, guard availability, provider type/access edits, removal and filtered/portable/cold identity (2026-09-07; binding epoch 27 / extraction schema 47; 2,170 selected core/profile tests and twelve integrations passed; reference binding modes, complete patterns, closures, nested call operands and CFG remain open).
  - [x] Keep captured Rust bare-call misses authoritative when module/provider evidence is unknown; prevent same-file namesake functions from supplying guessed targets and downstream factory/local types, with compiler-labelled negative and imported positive controls (2026-09-07; binding epoch 18 / extraction schema 38; implicit prelude and remaining legacy external paths stay open).
  - [x] Preserve explicit generic-call CST chains and selector-addressed type recipes through lexical/namespace facts; bind bare, qualified and member zero-argument return cascades with source TypeIds, including same-named type heads and fresh/cold checks (2026-09-07; 2,083 selected core/profile tests and 12 integration tests passed; not full generic, trait or receiver semantics).
  - [x] Index bound nominal members by (owner declaration ID, MemberNameId), traverse bases using that name ID, and reject first-wins direct duplicates/same-depth inherited competitors; retain canonical merged owners and fresh/cold constructor/factory cascades (2026-09-07; 1,944 selected core tests; source-entry interning, unbound legacy paths, overload selection and member privacy remain open).
    - [ ] Carry member-name IDs from source occurrences through remaining member APIs; enforce member/field access, language-specific inheritance precedence and explicit ambiguity propagation through outer fallback stages.
      - [x] Enforce supported Rust inherent-method/named-field access using source declaration IDs, persisted module scopes and source-position ModuleIds; preserve legal descendants, reject sibling/private-owner yields, and carry primary nominal access/ambiguity failures past outer retries (2026-09-07; epoch 10, 48 rustc-checked privacy fixtures and fresh/cold exact-ID tests; not complete language privacy or an independent 99% result).
      - [ ] Bind qualified/imported cross-file impl owners to nominal IDs before member indexing; retain the failing legal descendant-access probe, add enum payload declaration slots, and complete trait dispatch, construction/pattern access checks and remaining legacy/composite fallback barriers.
        - [x] Attach supported local/qualified/forward-renamed inherent impls through source owner BindingIds and same-crate declaration origins; preserve private impl-module access, blanket generic parameter-ID permutations and Self applications across late batches, actual provider edits, deletion and cold reload (2026-09-07; epoch 11, 1,961 selected core tests, 11 additional rustc legality fixtures; type-alias owners and constrained/specialized/lifetime/const applicability remain open).
        - [ ] Bind type-alias impl owners through canonical alias/application recipes, preserve applicability constraints instead of attaching to every bare nominal receiver, and close the retained legal alias-owner probe with compiler-labelled occurrences.
          - [x] Bind plain/chained/permuted/fixed-argument alias impls through canonical TypeIds and per-member receiver patterns; preserve GenericParamId yields, exact signed/unsigned/width primitive identity, source shadowing, same-crate terminal ownership, provider edits and cold reload (2026-09-07; epoch 12, 2,019 selected core tests and 20 rustc legality fixtures; not compiler target labels or an independent 99% result).
          - [ ] Represent reference/pointer/lifetime/const and other exact generic argument shapes losslessly; close the retained compiler-legal reference-alias probe, extend direct specialized/constrained impl recipes, and add independent source-occurrence target labels rather than legality-only checks.
            - [x] Preserve reference/pointer kind, mutability, known static lifetimes and child TypeIds through source recipes, alias applicability, generic substitution, portable cache/arena remapping and provider edits/deletion; close the retained static-reference alias probe fresh and cold (2026-09-07; binding epoch 13 / extraction schema 33; named regions and const arguments remain open).
            - [x] Preserve reference-shaped root/field/return/parameter facts and project only the member-lookup receiver through explicit profile data; keep raw pointers opaque, retain generic reference arguments and verify alias/field/return cascades (2026-09-07; 2,044 selected core/profile tests; 48 alias fixture legality checks agree with rustc, not compiler-provided target labels).
            - [ ] Bind named/elided lifetime parameters and generic argument kinds to source identities; close the retained legal lifetime-alias probe, add outlives/variance and receiver-adjustment evidence, and replace legality-only Rust evidence with independently labelled source-occurrence targets.
              - [x] Bind explicit named lifetime parameters through source BindingIds and GenericParamIds; preserve type/lifetime/const positions, kind-aware impl correspondence, region substitution/inference, portable cache remapping and provider edits/deletion; close the retained named-lifetime alias probe fresh and cold (2026-09-07; epoch 14 / extraction schema 34; 2,056 selected core/profile tests, 62 Rust alias legality fixtures; no outlives/variance solver or independently measured 99% claim).
              - [ ] Bind elided/placeholder regions and omitted generic lifetime slots with scoped provenance; close the retained compiler-legal Alias<'_> call probe without treating Unknown as equality evidence, then add higher-ranked/outlives/variance and independently labelled Rust target evidence.
                - [x] Bind function-item input elision and placeholder regions to owner/source-site GenericParamIds, fill only attested omitted lifetime positions without shifting explicit types, and preserve exact alias/member/yield IDs across provider arity/kind edits, deletion and fresh-arena cold reload; close the retained Alias<'_> input probe (2026-09-07; binding epoch 15 / extraction schema 35; 2,063 selected core/profile tests and 77 Rust alias legality fixtures; output/local/receiver elision and independent Rust target labels remain open).
                - [ ] Bind output elision to the correct input/receiver region identities; close the retained compiler-legal make(p: &Doc) -> Alias<'_> cascade without guessing among multiple input regions, then extend local inference variables and higher-ranked/outlives/variance evidence.
                  - [x] Bind free-function and receiverless associated-function output elision to the unique source input region ID; preserve per-parameter candidate grouping, source alias arguments before expansion, omitted output slots, filtered/portable caches and provider edits/deletion/cold reload; close the retained make(p: &Doc) -> Alias<'_> cascade (2026-09-07; binding epoch 16 / extraction schema 36; 2,069 selected core/profile tests and 20 additional rustc legality fixtures; receiver/local/higher-ranked semantics and independent Rust target labels remain open).
                  - [ ] Carry receiver borrow-region provenance through source-bound method signatures, receiver projection/adjustment and callee-ID substitution; close the retained legal p.make(other).touch() receiver-output probe without borrowing an ordinary argument's region or inventing equality for Unknown.
                    - [x] Capture shorthand/direct-Self reference receivers separately from ordinary parameters, retain direct reference-projection evidence, and substitute callee-owned region IDs for zero/nonzero-argument dot calls; close p.make(other).touch() fresh/cold and preserve provider retargeting/deletion and filtered portable caches (2026-09-07; epoch 17 / extraction schema 37; 2,076 selected core/profile tests and 17 additional rustc legality fixtures; owned auto-borrows, reborrow/outlives proofs, UFCS and independent Rust target labels remain open).
                    - [ ] Assign source-addressed call-site regions to owned-value auto-borrows and model receiver adjustments without confusing identity with outlives evidence; close the retained legal owned p.make(other).touch() probe, then cover shared/mutable reborrows, Self roots and UFCS receiver argument positions.
                      - [x] Capture dot-call selectors and source caller declaration slots, lower them to numeric inference-region identities, and substitute owned shared/mutable receiver outputs without borrowing ordinary argument regions; close direct/local/generic/alias/cross-file cascades and preserve provider edits/deletion, filtered source recapture and arena owner remapping (2026-09-07; epoch 19 / extraction schema 39; 2,089 selected core/profile tests and 12 integrations passed; receiver candidate ordering, reborrows, outlives and UFCS remain open).
          - [x] Fence type-only TS declarations before global value fallback while preserving genuine class/function value initializer BindingIds; keep Rust static aliases behind captured type-space evidence (2026-09-07; new compiler-labelled negative/positive controls, TS scope cohort 67 cases / 136 labels).
  - [x] Bind TS type-space annotations, bare call/constructor arguments, field/return heads and supported alias RHS syntax to declaration/generic IDs; preserve canonical type metadata with the arena across reloads (2026-09-06; 95 compiler-checked call labels; imported/qualified heads, member-call arguments, constraints/default syntax and contextual signature parsing remain open).
  - [x] Bind TS member-call type arguments by selector address and persist source-bound parameter TypeIds; use GenericParamId receiver/explicit/inferred substitution for contextual callbacks and returns without reparsing captured signatures (2026-09-06; 115 compiler-checked call labels; argument identifier occurrence binding, imports, inheritance, constraints/default syntax and overload semantics remain open).
  - [x] Carry TS/JS identifier argument use spans through BindingIds and position-correct type/callable facts; preserve class/function values, protect source-bound returns and bind initializer ownership by exact declaration/call addresses (2026-09-06; 134 compiler-checked call labels; imported arguments, full expression/CFG semantics and legacy bridges remain open).
  - [x] Bind local Rust inherent impl bodies to nominal declaration slots through a generic ScopeId/NameId/BindingId ingestion pass; preserve parent IDs across cold reload and forbid bound nominal misses from borrowing same-qname members (2026-09-07; Rust probe corpus 31/32 versus recorded 30/32, existing AliasDoc constructor failure remains; qualified/imported owners and trait dispatch are not covered).
    - [x] Replace conservative import-bearing-scope abstention in detached-body ownership with precise import-name bindings; retain generic/alias/module isolation and correct function-local impl extraction.
      - [x] Preserve forward parent-ID ancestry through contract filtering and serialized external payloads; invalidate pre-change extraction cache keys (2026-09-07; schema 32, nested local members cannot leak as external contracts).
      - [ ] Bind Rust imported/renamed targets to crate/module/declaration IDs, including AliasDoc constructors; retain type/value/macro namespaces and wildcard-provider ambiguity rather than bare-name alias expansion.
        - [x] Extend the shared module graph with validated source-local nested module IDs, explicit value/type/macro export domains, per-selector domain paths and profile-data wildcard exclusions; preserve ambiguity and fingerprinted cold reload (2026-09-07; graph-input prerequisite, Rust ingestion and AliasDoc remain open).
        - [x] Capture Rust inline modules and scoped explicit/grouped/renamed imports into the shared ID graph; bind qualified constructor/factory cascades and simple declared returns, preserve negative barriers and filtered/cache/cold reload metadata (2026-09-07; binding epoch 4, targeted slice only; Rust integration now 30/32 exposes wrong-owner legacy crate import selection, while crate-wide AliasDoc remains open).
        - [x] Bind configured Cargo crate roots, dependency aliases and out-of-line module declarations before importing their exported declaration IDs; repair the nested SelfProbe constructor's wrong-owner selection without discarding return-type IDs, then close the AliasDoc constructor cascade.
          - [x] Capture custom target paths/names, consumer-scoped path dependencies and out-of-line source recipes; preserve ambiguity and configuration-aware cold reload (2026-09-07; epoch 5, 1,902 selected core tests, 32/32 Rust patterns plus exact constructor/member IDs; SelfProbe fixture's missing crate-root wiring corrected, no independent Rust 99% claim).
          - [ ] Carry Rust bare/import/type-use and initializer occurrences through BindingIds; a rejected constructor must not yield a fabricated downstream local type through legacy synthetic TypeRefs.
            - [x] Migrate ordered Rust local/parameter bindings, supported bare/imported factory calls and simple annotations to source-addressed IDs; stamp call selectors from CST before capture, reject fabricated constructor/local cascades, preserve declared types and verify shadowing/closure/filtered/cold metadata (2026-09-07; epoch 6, 1,914 selected core tests; not complete Rust type-use semantics or an independent 99% gate).
            - [ ] Complete generic/qualified composite type-use recipes and struct/tuple/value-alias initializer ownership; cover pattern projections, match/loop/conditional scopes and remaining legacy inference bridges with independently labelled Rust occurrences.
              - [x] Bind supported Rust nominal applications, alias/field/return recipes and generic declaration parameters through source IDs; capture explicit struct construction and position-correct local value copies, remove the parallel simple-type path, and preserve negative/cold-reload evidence (2026-09-07; epoch 7, 1,923 selected core tests; scoped-field corpus probe promoted to an exact-ID gate, 33/33 legacy patterns; independent Rust labels and remaining composite/initializer forms stay open).
          - [ ] Complete configured module instances, registry/git/stdlib source configuration, wildcard providers, restricted visibility, cfg/features, generated modules and configuration-driven dependent-edge invalidation; preserve unknown/ambiguity throughout.
            - [x] Feed uniquely pinned installed registry manifests into the shared module graph through consumer lockfile edges, compatible version requirements and checksum/source evidence; retain custom library paths/aliases, external contract filtering, exact trait/return targets, provider edits/deletion and fresh-arena cold reload (2026-09-07; external Rust corpus regression repaired without relaxing assertions; named registry/configuration and versioned source instances remain open).
            - [ ] Replace version-collapsing external source addresses with configured package/source-instance identities; resolve simultaneous package versions and named registry/source replacements without namesake fallback or global package leakage.
            - [x] Bind private crate-module paths from legally accessing sibling source modules without making those declarations externally exportable; reproduced with `api.rs` returning `Holder<crate::real::RealDoc>` when the root declares `mod real` rather than `pub mod real` (2026-09-07).
              - [x] Carry requesting-module IDs and private/pub(self)/pub(super)/pub(crate) scopes through module selection, constrain public re-export terminal visibility, retain ambiguity and cold-reload/retargeting barriers; eight visibility fixtures independently checked for rustc acceptance/privacy rejection (2026-09-07; not a Rust symbol-binding oracle).
              - [ ] Complete named ancestor restrictions (`pub(in crate::outer)`), member/field privacy and configured module-instance accessibility; unsupported visibility must remain unknown rather than become public.
                - [x] Bind explicit crate/self/super-rooted ancestor restrictions through direct module declarations and cached ModuleIds; reject imported aliases, non-ancestors, missing/duplicate declarations and public widening; retain fresh/cold constructor/member and composite-return IDs (2026-09-07; epoch 9, 1,937 selected core tests; member privacy/module instances and edition-specific legacy paths remain open).
                - [x] Stop explicit self/super module paths from retrying a missing member as a configured external dependency; keep legal unqualified dependency and explicitly imported module paths working (2026-09-07; 11 additional rustc acceptance/rejection fixtures, 19 total; not compiler-provided target labels).
- [ ] Fix declaration identity across modules/packages and legal merging; unrelated same-named declarations must remain distinct.
  - [ ] Bind configured program-level global type/value identities and legal cross-file ambient declaration groups; close the retained split-interface callback/return misses and exported-module false positive, unify only attested generic-owner IDs, and preserve overlapping-program isolation and fresh/cold/edit/deletion semantics.
    - [x] Capture explicit program/source membership and syntax-owned global declaration inputs; build snapshot-owned program/source/name/group IDs with separate value/type domains, module isolation and incomplete-provider barriers; persist configuration and verify overlapping programs, source edits/deletion, late batches, cold reload and poisoned-display/filtered sources (2026-09-07; epoch 30 / schema 50; input/identity prerequisite only, FileLookup selection, generic TypeInfo materialization and configured ecosystem discovery remain open).
    - [x] Separate configured nominal TypeIds from physical navigation rows; use numeric-only declaration interning/lookup, remint snapshot contexts, and reject foreign/mixed-context member, alias, inference and signature reads; fix display-keyed lookup and stale arena-restore caches (2026-09-07; 18 new tests, 1,176 selected tests passed; program-specific FileLookup/signature materialization and original ambient cascade failures remain open).
    - [x] Select source-hashed program views in FileLookup, materialize source-owned signatures with program-local GenericParamIds, and bind global value/type uses and method selectors through IDs; close all 27 configured ambient/source labels and preserve overlapping/stale/rejected-provider, edit/deletion, poisoned-display and portable/cold-cache barriers (2026-09-07; epoch 31 / schema 51; 1,189 selected tests and five integrations passed; automatic configuration discovery, actual stdlib merge semantics and broader source recipes remain open).
    - [x] Separate source identity capture from the 512 KiB flow-query guard, share fresh/contract restoration logic and preserve large-provider generic callback/return cascades through filtered/portable/cold snapshots; restore the real DOM declaration capture without enabling oversized flow queries (2026-09-07; epoch 32 / schema 52; 1,317 selected tests passed; 55 other incomplete source captures still fence the configured Query Core environment).
    - [x] Repair pinned TypeScript/TSX provider grammar for bare global augmentation, keyof-readonly ownership and generic import-type/postfix syntax; preserve contextual identifiers, source anchors, queries, invalid-syntax and incremental controls with reproducible generated parsers (2026-09-07; epoch 33 / schema 53; 14 independent compiler AST checks, 187 supplied sources parse cleanly, 1,910 selected tests and seven integrations passed; 54 ambient-module captures still fence all 690 configured labels, legacy per-call verdicts unchanged).
    - [ ] Capture ambient external module scopes, imports/exports and nested global augmentation provenance as source/program-owned IDs; close the 54 retained real-provider capture barriers without dropping compiler supply, guessing namesakes or relaxing incomplete-input checks, then validate actual stdlib merging and type recipes.
      - [x] Capture source-local module/namespace/augmentation IDs and var-hoisting boundaries; persist scoped imports, explicit/implicit export surfaces, source evidence and numeric BindingId forwarding, preserving duplicate/incomplete barriers and filtered/portable/cold identities (2026-09-07; epoch 34 / schema 54; seven shared TypeScript compiler fixtures, eleven export sets and two exact variable targets; program-owned provider selection and nested global contribution binding remain open).
      - [x] Bind ambient providers and nested global contributions in source-hashed program-owned module graphs; route configured imports/signatures through IDs, redirect physical augmentations by resolved module identity, and preserve overlapping programs, actual edits/deletion, filtered portable caches and poisoned-display invariance (2026-09-07; epoch 35 / schema 55; seven compiler-validated cases / 16 labels, 1,936 selected tests; Query Core incomplete providers 54 to 14, all 690 configured labels still unresolved and legacy per-site verdicts unchanged).
      - [ ] Represent export-assignment entities and scoped import-equals/internal aliases through source/module/binding IDs, including callable/namespace facets; close the 28 incomplete literal units in the remaining 14 supplied Node providers without approximating default exports or relaxing completeness, then validate real stdlib merging and type recipes.
        - [x] Bind scoped import-require/internal alias and export-assignment recipes with separate declaration/overload/namespace facets, numeric forwarding and cycle/ambiguity barriers; preserve legal interface merges, poisoned-display invariance, overlapping programs, actual edits/deletion and portable/fresh-arena cold reload (2026-09-07; epoch 36 / schema 56; 11 compiler-validated cases / 21 labels, 1,942 selected tests plus seven integrations; all 187 supplied sources now have complete module capture, configured Query Core 354/690 correct, 40 kind-only disagreements and 296 unresolved, fresh/cold equal; legacy per-site report unchanged).
        - [ ] Complete configured signature materialization and legal stdlib merging after source capture: carry source-owned base applications and initializer-derived field/value types, represent computed/index/call/construct member identities and merge compatibility without blanket acceptance; close the traced PromiseConstructor, Subscribable.listeners and inherited FocusManager cascades, then measure every retained configured occurrence again. Broader namespace/entity merging and assigned-module interop remain open.
          - [x] Materialize source-owned configured base applications and numeric ancestor generic environments; bind non-call selectors by IDs and private selectors by lexical declaration IDs, preserving brands, provider retargeting/deletion, overlapping programs and filtered/portable/cold snapshots (2026-09-08; epoch 38 / schema 58; 19 compiler-validated cases / 43 labels, 1,951 selected tests and seven integrations; configured Query Core 354 to 436 of 690 correct, 40 unchanged kind-only disagreements, 214 unresolved, zero snapshot changes and no per-site regressions; the first recapture's 97 private-selector regressions were retained as evidence and repaired, not accepted as a tradeoff).
          - [ ] Complete configured initializer-derived field/value recipes and legal rich-member stdlib merging; retain the traced Set/Promise/Array and Subscribable.listeners barriers. Extend inheritance to global value heads, interface/multiple bases and full generic legality; complete private static/accessor forms and prove configuration/source-instance accessibility before claiming general coverage.
            - [x] Capture source-addressed rich-member inventories with separate property/method/accessor/call/construct/index kinds, signature anchors, modifiers and computed-key BindingId/NameId paths; preserve optional physical rows, filtered/portable/cold metadata and poisoned-display invariance, and stop index parameter names from posing as property identities (2026-09-08; epoch 39 / schema 59; 10,237 member surfaces independently matched to TypeScript ASTs across six fixtures and 59 supplied stdlib files, two compiler-diagnostic index controls, 1,959 selected tests plus seven integrations; configured 436/690 and legacy 557/690 reports unchanged per occurrence and fresh/cold; syntax evidence only, not bound signature recipes or legal rich merging).
            - [ ] Lower captured member/signature type syntax into durable source-owned recipes while source is available, including signature/generic owners without navigation rows; bind computed unique-symbol keys through selected-program value/member identities and validate overload/property/index compatibility before admitting rich merges. Then materialize initializer-derived field/value types and remeasure the retained Set/Promise/Array and listeners cascades without string recovery or weakening negative barriers.
              - [x] Persist source-signature type recipes and row-independent generic owners, materialize per-program/source signature arenas, and isolate call/construct/abstract-method generic scopes; retain constraints/defaults and optional/rest evidence through filtered/portable/cold providers, import retargeting/deletion and poisoned display metadata (2026-09-08; epoch 40 / schema 60; 19 independently compiler-checked generic uses, 1,968 selected tests, 10,237 AST member controls; configured 436/690 and legacy 557/690 remain unchanged per occurrence; supported source recipes only, not complete type syntax, constraint checking or rich merging).
              - [ ] Complete exact intrinsic/literal/operator type recipes and computed unique-symbol key binding in the selected program, then validate rich-member/overload/index/generic compatibility before admitting stdlib merges; keep unresolved evidence distinct from language-level unknown/top types and retain the initializer-derived field/value cascade work.
                - [x] Preserve intrinsic and exact string/number/bigint/boolean literal identities through source recipes, configured signatures and portable/cold arenas; repair bigint grammar classification and bind apparent member receivers through source-owned global wrapper IDs without capitalization or display recovery (2026-09-08; epoch 42 / schema 62; 49 compiler-validated type values, 1,982 selected library tests, eight grammar tests and seven integrations; the two string-method integration regressions were repaired without relaxing assertions, Query Core configured 436/690 and legacy 557/690 remain unchanged per occurrence and fresh/cold; operators, unique-symbol keys, rich merging and initializer cascades stay open).
                - [ ] Complete sibling-consumer compilation verification for the public type-algebra changes once dependency/lockfile compatibility is resolved; preserve AlphaT and Lynx lockfiles until their refresh is authorized.
                - [x] Preserve source-owned keyof/readonly/indexed-access/conditional operator structure and operand IDs through configured signatures, import-dependent rebinding, generic substitution and portable/cold arenas; retain conditional distribution evidence and unknown/applicability barriers (2026-09-08; epoch 43 / schema 63; 12 compiler-validated fixtures / 19 operator nodes / 31 generic targets, 1,991 selected library tests and seven integrations; configured 436/690 and legacy 557/690 remain unchanged per occurrence and fresh/cold; operator evaluation, mapped/infer/template binders, computed unique-symbol keys, rich merging and initializer cascades remain open).
                - [x] Allocate program/source-owned unique-symbol declaration identities and bind supported computed declaration keys through annotated/import/global value IDs, row-independent member signatures and numeric generic substitution; preserve optional/access/duplicate barriers, identical-source isolation, unchanged-consumer import retargeting/deletion and portable/cold inputs (2026-09-08; epoch 45 / schema 65; 15 compiler-validated unique-owner cases and seven exact computed-key targets, 2,002 selected library tests and seven integrations; configured 436/690 and legacy 557/690 remain unchanged per occurrence and fresh/cold; value queries, namespace-valued keys, inferred unique values, canonical merged-member identity, rich compatibility and initializer cascades remain open).
                - [x] Bind source-owned typeof queries and namespace-valued computed-key roots through selected-program declaration/member/export IDs; evaluate forward annotations, aliases and inherited generic arguments with cycle/depth-safe dependency memoization, correct nested CST field ownership and retain stale/overlapping-program, poisoned-display, provider edit/deletion and rowless portable/cold barriers (2026-09-08; epoch 46 / schema 66; nine compiler-validated fixtures, nine exact unique targets, four intrinsic query types and one diagnostic-backed negative; 2,012 selected library tests and seven integrations passed; configured 436/690 and legacy 557/690 unchanged per occurrence and fresh/cold; full callable/structural values, type-only query domains, inferred initializers, rich merge staging/compatibility and the full 99% gate remain open).
                - [x] Privately stage eligible interface groups and admit only stable ID-based property/index/key compatibility proofs; propagate provider rejection through dependent computed keys, project proved repeated properties to one semantic identity without collapsing overloads, and preserve edit/deletion, overlapping/stale-source, poisoned-display and rowless portable/cold barriers (2026-09-08; epoch 47 / schema 67; six compiler-validated fixtures / 15 exact calls, 16 compatibility cases, 2,022 selected library tests and seven integrations; configured Query Core 436 to 446 of 690 correct, 40 unchanged kind-only disagreements, 204 unresolved, zero per-site regressions or snapshot changes; constrained/heritage/richer stdlib groups, merged unique origins, initializer inference and the full 99% gate remain open).
                  - [ ] Close the retained selected-view Set/Map/Promise/PromiseConstructor merge rejection and Array/ReadonlyArray heritage barriers using source-bound proofs; integrate proved repeated-property dependencies into private query staging, then materialize initializer-derived fields such as Subscribable.listeners and remeasure every retained occurrence.
                    - [x] Separate source value-expression binding from pure type declarations and carry runtime/erased/typeof import domains through ID-only module graphs; repair type-only wildcard grammar and explicit-export shadow barriers, admitting the traced Set/Map/Promise/PromiseConstructor groups without weakening merge proofs (2026-09-08; epoch 48 / schema 68; fourteen compiler-validated cases, 2,027 selected library tests, nine grammar tests and seven integrations; configured Query Core 446 to 492 of 690 correct, 40 unchanged kind-only disagreements, 158 unresolved, zero per-site regressions or snapshot changes; legacy unchanged, full 99% gate remains open).
                    - [ ] Bind source-owned interface heritage through type-domain declaration IDs and generic applications, prove inherited/member compatibility before rich-group admission, and close the retained Array/ReadonlyArray extends RelativeIndexable cascade; then integrate repeated-property query proofs and initializer-derived Subscribable.listeners types, preserving fresh/cold/edit/deletion and negative gates.
                      - [x] Preserve supported object/mapped source recipes, property/index key TypeIds, modifier axes and row-independent mapped binder identities; protect bound parameters during substitution and verify nested/self-referential constraints, filtered/portable/cold data, poisoned display and unchanged-consumer import retargeting/deletion (2026-09-08; epoch 50 / schema 70; ten compiler-validated shapes, ten mapped owners, 28 generic targets and four diagnostics; 2,040 selected library tests and seven integrations; configured Query Core remains 546/690 correct with 40 kind-only disagreements and 104 unresolved, unchanged per occurrence; mapped evaluation and the two inherited QueryObserverOptions regressions remain open).
                      - [ ] Evaluate source-bound object/mapped/intersection surfaces and associated generic obligations before admitting structural interface bases; repair both retained QueryObserverOptions regressions without discarding structural branches, borrowing namesakes or treating unresolved operands as equality evidence, then integrate repeated-property query dependencies and initializer-derived fields.
                        - [x] Evaluate supported finite object/mapped/keyof/indexed-access recipes with source-owned alias constraints/defaults and bounded structural relations; preserve intersection declaration identity separately from assignment surfaces, reject unknown/foreign/cyclic evidence and verify portable/cold/poisoned inputs plus provider retargeting/deletion (2026-09-08; epoch 51 / schema 70; 41 compiler-validated assignment cases, 11 obligation cases and 25 merge cases; 2,050 selected library tests and seven integrations passed; configured Query Core remains 546/690, unchanged per occurrence and fresh/cold; nominal structural inheritance, full mapped/operator semantics and both retained regressions remain open).
                        - [ ] Supply source-attested nominal member/key surfaces and mapped-distribution provenance to structural evaluation; preserve numeric/string key equivalence through ingestion IDs, prove remaining generic obligations and retain navigation origins before admitting mixed nominal/structural bases. Recheck both QueryObserverOptions regressions, repeated-property query dependencies and initializer-derived fields without weakening the compiler-backed negative gates.
                          - [x] Materialize source-owned nominal key/member inventories and evaluate supported conditional/infer patterns through branch-local signature/GenericParamIds; preserve lazy selected values, navigation origins, portable/cold/poisoned providers and unchanged-consumer retarget/edit/deletion. The pinned QueryKey constraint now evaluates to ReadonlyArray<unknown> and WithRequired retains both structural/nominal branches (2026-09-08; epoch 53 / schema 71; twelve nominal and sixteen conditional assignment cases, eleven conditional barriers, twenty-six compiler-labelled signature uses; 2,062 selected library tests and seven integrations passed; configured Query Core remains 546/690, unchanged per occurrence and fresh/cold; full mixed-base admission, complete inference and both retained regressions remain open).
                          - [ ] Prove and project the now-evaluated mixed QueryObserverOptions base, retaining every structural branch, effective optional/readonly and generic member values, full callable evidence and source navigation origins. Keep receiver-specific refinements separate from shared parent physical-row types; close both retained regressions, then integrate repeated-property query dependencies and initializer-derived fields. Extend mapped/distributive inference and exact key equivalence under compiler-backed negative gates rather than treating unsupported comparisons as false.
                            - [x] Prove supported mixed nominal/structural bases and publish receiver-owned effective member/signature surfaces without mutating parent physical-row types; retain full base operands, source origins (including rowless signatures), generic/modifier refinements and query/read consistency. Close both retained QueryObserverOptions regressions under exact per-occurrence fresh/cold comparison (2026-09-08; epoch 54 / schema 71; 26 compiler-validated heritage cases, 2,066 selected library tests and seven integrations; configured Query Core 546 to 548 of 690 correct, 40 unchanged kind-only disagreements and 102 unresolved, no other labelled-site changes; new/remapped structural origins, complete callable refinements and the full 99% gate remain open).
                            - [ ] Feed proved repeated-property identities and effective signatures back into private value-query staging before dependent key/type recipes are finalized; then materialize source-owned initializer-derived fields such as Subscribable.listeners and remeasure all retained occurrences. Keep new/remapped structural-key origins, richer callable-property refinement and mapped/distributive/key-equivalence coverage explicit, preserving the two corrected inherited sites and all negative/snapshot gates.
                              - [x] Feed complete named-property proofs and receiver-owned inherited values into bounded private query/signature rounds, covering global and module-local merges plus productive self-dependencies; revoke all dependent evidence on whole-owner rejection and retain rowless/portable/poisoned, retarget/edit/deletion, overlapping-program and cold-snapshot gates (2026-09-08; epoch 55 / schema 71; ten compiler-validated cases, six exact unique-key origins and five diagnostic-backed negatives; 2,074 selected library tests and seven integrations; configured Query Core remains 548/690 and legacy 557/690, both reports identical to their preceding reports except elapsed time, with the two corrected inherited sites preserved; initializer inference and the full 99% gate remain open).
                              - [x] Materialize source-owned initializer-derived field/value recipes in configured views, starting with the traced Subscribable.listeners = new Set<TListener>() cascade. Bind the runtime constructor value, complete construct signatures and GenericParamId arguments rather than assuming a same-spelled type head; preserve annotations, applicability/ambiguity/access barriers, provider changes and portable/cold identities, then remeasure all retained occurrences. New/remapped structural origins, richer callable refinement and complete mapped/distributive/key-equivalence semantics remain explicit backlog.
                                - [x] Evaluate supported source-owned constructor initializers through runtime value IDs, full source signatures, generic defaults/constraints and tri-state argument evidence; preserve explicit annotations, class overload implementation exclusion, inherited constructor parameters, no-base versus unknown-base evidence and rowless/portable/poisoned/overlapping-program/edit/deletion/cold identities (2026-09-08; epoch 56 / schema 72; 25 compiler-checked cases, 17 exact result types, seven diagnostic-backed negatives and one legal abstention; 2,083 selected library tests and seven integrations passed; configured Query Core 548 to 571 of 690 correct, 40 unchanged kind-only disagreements, 79 unresolved, all 23 changes unresolved-to-correct and fresh/cold equal; legacy 557/690 unchanged per occurrence; broader expressions, full overload/contextual inference and the 99% gate remain open).
                                - [ ] Bind legal callable overload groups by source owner/member IDs and retain full candidate signatures, navigation origins and argument/contextual applicability evidence; trace and close the retained Array.find/filter and Promise.resolve/all cascades without collapsing overloads or selecting a first physical row. Add independent selected-signature labels alongside the existing call-symbol navigation oracle, preserve ambiguous/incomplete barriers and remeasure every retained occurrence against 571/690.
                                  - [x] Bind supported nominal method overload groups by canonical owner/MemberNameId, preserve every source signature/navigation origin and select uniquely applicable signatures from source-owned arguments; propagate substituted results through local and inline chains, preserve rowless/portable/poisoned/retarget/edit/deletion/cold evidence, and prevent explicit receiver parameters or incomplete tuple-rest shapes from falsely eliminating competitors (2026-09-08; epoch 58 / schema 74; 18 compiler-checked cases, 16 selected-signature/result labels, two diagnostic negatives and six legal abstentions; 2,089 selected library tests and seven integrations passed; final configured Query Core remains 571/690, whole report unchanged except elapsed time; full contextual/ordered overload semantics and the 99% gate remain open).
                                  - [ ] Carry nested callable signatures, receiver/optional/rest/generic/type-predicate evidence through source-owned callable type identities; evaluate callbacks in candidate-local binding environments before selecting overloads, close the retained ordinary-callback and inferred-predicate compiler probes, and remeasure Array.find/filter and Promise cascades. Keep language ordering/specificity, class-method inventories/access, general variadic tuples, standalone overloads and source-span-only navigation explicit; do not relax the erased-Function proof barrier.
                                    - [x] Preserve nested function-type signatures as configured-context/source/signature identities with parameter source slots, optional/rest/receiver flags, local generic IDs/constraints/defaults and predicate targets; protect local binders during substitution, remap all operands and refresh contexts on hydration, and verify rowless/portable/poisoned/retarget/edit/deletion/cold evidence (2026-09-08; epoch 59 / schema 75; seven compiler-checked cases, eight signatures, eleven parameter positions, three predicates and one diagnostic negative; 2,096 selected library tests passed; configured Query Core remains 571/690 and legacy 557/690, both complete reports unchanged except elapsed time; callable relations, callback inference and the 99% gate remain open).
                                    - [ ] Supply source-owned callback body/return/narrowing recipes to candidate-local binding environments, prove full callable relations with alpha-bound generic IDs and parameter/predicate obligations, and publish contextual writes only after overload selection. Close the retained ordinary-callback and inferred-predicate probes and remeasure the fixed real-project cohort; do not equate callable source origins with structural assignment equality or erase origins to force TypeId equality. Keep standalone values/method projections, receiver/variadic/access rules and language ordering explicit.
                                      - [x] Prove supported source-owned callable argument relations with private generic alpha-binding and receiver-substituted constraint environments, typed variance/nullability policy, nested-callback rules, fixed tuple-rest arity and predicate target positions; use declared callable arguments for overload/result cascades, preserve invalid/unknown/portable/cold/stale-policy barriers and remeasure the unchanged Query Core cohort. Callback bodies, higher-rank contextual instantiation, complete variadics, method projections/access and language overload ordering remain open.
                                        - 2026-09-08 closeout: epoch 60 / schema 75; 51 compiler assignment cases, 23 overload cases, 2,103 selected library tests and seven integrations. Final configured 571/690 and legacy 557/690 reports are wholly unchanged except elapsed time; all 690 fresh/cold occurrences agree. Next: capture source-owned callback bodies and evaluate them under candidate-local BindingIds before ordered overload selection, preserving all retained positive/negative labels.
                                      - [x] Evaluate supported source-owned callback expression/single-return bodies in private BindingId environments, infer ordinary arguments before callbacks and recheck final contexts, preserve primitive predicate/source-slot evidence and public method signature origins, and project nullable receivers only for optional calls. Close the retained ordinary-arrow probe and both onFocus/onOnline callback-to-refetch cascades without declaration-name matching (2026-09-08; epoch 61 / schema 76; 30 compiler overload cases, 2,109 selected library tests and seven integrations; configured Query Core 571 to 575 of 690 correct, 40 unchanged kind-only disagreements, 75 unresolved, all four changes unresolved-to-correct and exact fresh/cold agreement; inferred-predicate overload ordering, broader bodies/CFG and the full 99% gate remain open).
                                        - [ ] Preserve source declaration-group ordering and language subtype/specificity rules for competing applicable overloads; close the retained inferred-predicate arrow fixture without choosing a first physical row. Extend higher-rank contextual instantiation, callable properties, variadics and general optional-chain continuation under compiler-backed negative gates, then remeasure Array.filter and Promise cascades against 575/690.
                                          - [x] Select supported competing nominal overloads using canonical declaring-owner IDs, source parent-group order, literal-annotation specialization and subtype-before-assignment applicability; capture compiler source binding order independently of sorted file IDs, preserve nested-invalid/unknown barriers and verify portable/cold/configuration-only reorder/revocation plus local/inline result cascades (2026-09-08; epoch 62 / schema 77; 47 compiler cases, 41 selected-signature/result labels, six diagnostic negatives and two legal abstentions; 2,118 selected library tests and seven integrations; configured 575/690 and legacy 557/690 unchanged per occurrence and fresh/cold; inferred-predicate probe closed, broader inference/variadics and the 99% gate remain open).
                                          - [x] Close the freshly retained difference<T> cascade from the source-owned new Set(array2) initializer through the callback's excludeSet.has(x) and negated return, before claiming Array.filter completeness; retain Promise receiver/variadic/callable-property gaps and require exact selected signatures plus downstream source targets, not member-spelling counts.
                                            - [x] Infer supported union constructor/call arguments with GenericParamId-owned candidates and compiler-checked fallback priorities; carry source class/interface member names into selected-view MemberNameIds and remove covered display-name aliases. Verify the complete constructor-field-to-local/inline-return cascade with poisoned metadata, portable/cold arenas and provider retarget/edit/deletion controls (2026-09-08; epoch 63 / schema 77; 42 constructor compiler cases, 47 overload compiler cases, 2,120 selected library tests and seven integrations passed; configured Query Core remains 575/690 correct with every retained occurrence and cold snapshot unchanged. Real Set(array2) still stops at readonly-array evaluation and iterable inference; callback negation and exact selected-constructor labels remain open).
                                            - [x] Bind mutable/readonly array roles from grammar-owned NameIds to selected canonical interface IDs, preserve readonly tuple optional arity, and prove element inference plus directional argument relations. Verify poisoned portable/cold constructor-to-local/inline-member cascades, provider selection/edit/deletion, foreign contexts and invalid role controls (2026-09-08; epoch 64 / schema 78; 54 constructor compiler cases, 47 overload cases, 12 operator cases, 2,128 selected library tests and seven integrations passed; configured 575/690 and legacy 557/690 unchanged at every retained occurrence and fresh/cold. Real readonly-array constructor candidate now yields Set<caller T>; the full initializer remains unknown through the iterable candidate).
                                            - [x] Reconcile source-owned constraints/defaults across legal generic interface augmentations before private merge admission, restoring the rejected IteratorObject/ArrayIterator dependency without weakening compatibility checks. Bind compiler intrinsic aliases with configuration evidence and namespace isolation, then complete iterable structural/callable inference, exact constructor selection and callback negation for the retained difference<T> cascade.
                                              - [x] Reconcile effective generic constraints/defaults in private source-owned interface groups, including module-local added defaulted parameters; preserve raw omissions and reject conflicting, cyclic, forward-reference, invalid-order and unsupported-variance evidence. Verify 30 pinned compiler cases in script/module scopes, eight exact cascade target checks, poisoned portable/cold inputs and configuration/edit/deletion controls (2026-09-09; epoch 65 / schema 79; 2,133 selected library tests and seven integrations passed; both real IteratorObject providers are admitted, while ArrayIterator still depends on compiler-intrinsic typing).
                                              - [x] Bind the selected BuiltinIteratorReturn compiler-intrinsic declaration with effective strictBuiltinIteratorReturn configuration and namespace isolation; then prove recursive/covariant iterator heritage and complete iterable inference, exact constructor selection and callback negation before closing difference<T>.
                                                - [x] Bind source-attested compiler intrinsic alias roles with effective strict/non-strict policy, unique type-binding ownership and namespace isolation; preserve missing-policy, duplicate, unsupported and stale-source barriers. Verify 20 pinned compiler cases in script/module scopes (52 alias-type labels and eight exact call labels, including four retained conditional-receiver abstentions), poisoned portable/cold constructor-to-member cascades, policy/provider retargeting, edits and deletion (2026-09-09; epoch 66 / schema 80; 2,138 selected library tests and seven integrations passed; real stdlib BuiltinIteratorReturn now evaluates to undefined, while ArrayIterator heritage and the full Set initializer remain open).
                                                - [x] Prove source-owned method overrides with explicit method-variance policy and complete signature evidence, including covariant returns through selected declared generic ancestors; revoke invalid ancestors and dependents during private settlement. Verify 31 pinned compiler cases in script/module scopes, six exact self-to-read-to-touch call targets, poisoned portable/cold inputs and policy/provider/edit/deletion controls (2026-09-09; epoch 67 / schema 80; 2,142 selected library tests and seven integrations passed; real ArrayIterator and inherited next(): IteratorResult<T, undefined> are admitted, while iterable inference, exact constructor selection and callback negation remain open; configured Query Core stays 575/690 correct with zero snapshot changes).
                                                - [x] Infer structural constructor arguments through selected member keys and full method/callable-property signatures, with independent optional/overload/recursive argument proofs and correlated finite tuple-rest alternatives; retain hidden call/construct facets as completeness barriers. Verify 21 pinned compiler cases in script/module scopes, 24 exact constructor types/signature origins, four downstream call targets, poisoned portable/cold inputs and provider/policy/edit/deletion controls (2026-09-09; epoch 68 / schema 80; 2,144 selected library tests and seven integrations passed; both real Set(array2) candidates and the initializer now yield Set<caller T>; exact constructor selection and callback negation remain open, and configured Query Core stays 575/690 correct with zero snapshot changes).
                                                - [x] Capture source-owned callback negation and validate complete operands before boolean results; infer supported two-sided typeof/truthiness predicates with parameter BindingIds and keep contextual writes private. Verify 22 pinned compiler cases in script/module scopes, 40 exact callback results, 18 predicates, 40 selected-overload/result labels, poisoned portable/cold inputs and provider/edit/deletion/foreign-context controls (2026-09-09; epoch 69 / schema 81; 2,146 selected library tests and seven integrations passed; independent real difference<T> constructor/filter/has signatures and compiler binding order captured without altering the frozen manifest. Configured Query Core remains 575/690 with zero snapshot changes; exact constructor selection and the full local cascade remain open).
                                                - [x] Publish proved source initializer results through exact declaration-token/SignatureId pairs and local BindingIds, preserving annotations, initialization availability, non-union contracts and authoritative constructor misses. Verify nine pinned compiler cases in script/module scopes, 24 local-type labels, four constructor signatures, 14 downstream call labels, poisoned portable/cold inputs and provider/consumer/edit/deletion/stale-source controls (2026-09-09; epoch 70 / schema 82; 2,148 selected library tests, the real source probe and seven integrations passed. The retained difference<T> filter and has now reach exact compiler declarations; configured Query Core improves 575 to 581/690 with six unresolved-to-correct changes, unchanged kind disagreements and zero snapshot differences, including originalQueue.forEach and collection cascades. Exact constructor dispatch remains open; agreeing return types do not establish the selected signature).
                                                - [x] Select supported constructor overloads with canonical owner/source origins, compiler-attested declaration and inherited-provider order, literal specialization and shared subtype-before-assignable phases; preserve implicit origins, incomplete competitors and result consensus without a fabricated winner. Verify 23 pinned compiler cases, 20 result types, 18 source signatures, two implicit origins, 21 downstream call labels, portable/poisoned/navigation-row-absent/cold inputs and configuration/edit/deletion controls (2026-09-09; epoch 71 / schema 82; 2,150 selected library tests, seven integrations and the exact real constructor/filter/has probe passed. Frozen and independently ordered Query Core cohorts both retain 581/690 correct, 40 kind disagreements and zero snapshot changes; all 690 observations are unchanged. The retained difference<T> cascade is closed; constructor variance, variadics, callable properties and Promise cascades remain open).
                                - [ ] Extend configured source-owned call/IIFE/object/arrow/array initializer and return recipes, local binding publication, variadic/rest evidence and caller-sensitive constructor access; retain the traced environmentManager IIFE/object-return failure, structural callable-property/intersection origins and contextual/narrowing gaps as separate causes. Complete mapped/distributive/key-equivalence semantics under compiler-backed negative gates.
                                  - [x] Preserve source-owned anonymous object member/signature navigation origins and infer supported IIFE return objects without executing their bodies or erasing closure BindingIds; close the environmentManager initializer-to-imported-member cascade, then remeasure the frozen cohort and separately trace local assignment aliases such as originalQueue.
                                  - [x] Evaluate supported source-bound conditional alias operators when used as member receivers; the compiler-intrinsic fixture retains four exact compiler call labels where configured member_applicability::expand_inner currently leaves the conditional operator unevaluated. Preserve invalid/unknown and recursive-alias barriers before admitting this cascade.
                                  - [ ] Prove full construct-signature variance for legal interface heritage before constructor dispatch; the pinned constructor_variance_heritage_retained_gap fixture is compiler-valid but still rejected by existing inheritance equality checks. Preserve incompatible, unknown and cyclic-base barriers, source signature origins and configuration/edit/deletion/cold parity.
                                    - [x] Preserve additive own/inherited constructor overload inventories and prove supported structural constructor arguments with ordinary callable variance, independent of method bivariance; close constructor_variance_heritage_retained_gap without requiring variance policy for inventory-only inheritance. Verify 43 pinned compiler cases in script/module scopes, 172 inventory signatures, 30 exact constructor origins/results/downstream calls and 46 assignments, including portable/poisoned/navigation-row-absent/cold inputs and policy/provider/consumer/edit/deletion controls (2026-09-09; epoch 74 / schema 83; 2,161 selected library tests and seven integrations passed; configured Query Core 585/690 and legacy 557/690 remain unchanged at every occurrence with zero snapshot changes. The parent stays open: generic_constraint_domain_retained_gap and unbounded_rest_variance_retained_gap are compiler-valid retained abstentions, and call facets/constructor-facet inference still require separate complete proofs).
                                    - [x] Prove configured unbounded mutable-array rest variance by repeating the canonical array element across ordinary positional comparison while preserving finite tuple-rest correlation, strictness, arity and missing/stale/foreign/namesake identity barriers; close unbounded_rest_variance_retained_gap through portable provider sources in callable and constructor matrices (2026-09-09; epoch 75 / schema 83; 61 callable cases, 43 constructor-heritage cases, 2,161 selected library tests and seven integrations passed; configured Query Core remains 585/690 and legacy 557/690 with exact fresh/cold parity and zero snapshot changes. The parent stays open for generic_constraint_domain_retained_gap, mixed finite/unbounded tails and separate call-facet/constructor-inference proofs).
                                    - [x] Instantiate generic source call/construct signatures contextually from target parameter, predicate and return positions while retaining rigid target constraint domains, source constraint validation, binder reordering/dependencies and conservative conflicting-candidate barriers; close generic_constraint_domain_retained_gap without erasing GenericParamId identity (2026-09-09; epoch 76 / schema 83; 76 callable cases, 43 constructor-heritage cases, 2,161 selected library tests and seven integrations passed; configured Query Core remains 585/690 and legacy 557/690 with exact fresh/cold parity and zero snapshot changes. The parent stays open for contextual finite/mixed rest tails, more complex repeated candidates and separate call-facet/constructor-inference proofs).
                                    - [x] Infer direct generic source rests constrained by configured array domains from the target's complete fixed, optional or array remainder; preserve full tuple/array candidates, compiler constraint fallback, rigid target domains and source origins, and validate invalid rest evidence before broad targets. Close generic_rest_constraint_retained_gap in callable and construct matrices (2026-09-09; epoch 77 / schema 83; 95 callable cases, 72 cross-kind rest cases in two scopes, 44 constructor-heritage cases, 2,161 selected library tests and seven integrations passed; configured Query Core remains 585/690 and legacy 557/690 with exact fresh/cold parity and zero snapshot changes. The parent stays open for tuple/dependent source constraints, mixed fixed/array tails, conflicting return candidates and separate call-facet/constructor-inference proofs).
                                    - [x] Infer direct generic source rests constrained by finite tuples and tuple unions from complete target remainders; preserve optional and readonly tuple evidence, the original generic-rest fixed-prefix minimum, rigid target domains, constraint fallback and valid rest/return correlation, and compare concrete tuple results without widening empty shapes. Close generic_tuple_rest_constraint in callable, cross-kind rest and constructor matrices (2026-09-09; epoch 78 / schema 83; 110 callable cases, 87 cross-kind rest cases in two scopes, 45 constructor-heritage cases, 2,161 selected library tests and seven integrations passed; configured Query Core remains 585/690 and legacy 557/690 with exact fresh/cold parity and zero snapshot changes. The parent stays open for dependent constraints, mixed fixed/array target tails, unconstrained generic targets, conflicting array-rest return candidates and separate call-facet/constructor-inference proofs).
  - [x] Bind supported TS interface/interface and class/interface groups by lexical type BindingId and physical declaration IDs; preserve type/value separation, generic owner IDs, scoped namesake isolation and source-fingerprinted cold reload/edit/deletion behavior (2026-09-07; six new fixtures / 30 compiler-checked labels, module cohort 79 labels; namespace/ambient augmentation, complete merge diagnostics, generic constraints/default compatibility and inheritance migration remain open).
- [ ] Normalize imports at ingestion and bind reachable module/export IDs; do not add global-name fallbacks.
  - [x] Bind TS/JS named/default imports and supported explicit/local/wildcard re-exports through BindingId/ModuleId/export-name IDs; retain distinct declaration/type identities, missing/ambiguity fences and overload alternatives without target selection (2026-09-06; 13 cross-file fixtures / 28 compiler-checked labels, separate from the existing 134; namespace objects, package policies, complete export domains and other languages remain open).
  - [x] Persist source-fingerprinted module inputs and import-dependent signature recipes; test late batches, changed-barrel retargeting, generic-template refresh, deletion and cold reload, and restore declaration bindings after external contract filtering/cache hydration (2026-09-06; full dependent-edge invalidation and configuration fingerprints remain open).
  - [x] Bind TS/JS namespace import/export selections by source-addressed BindingIds and numeric module/export paths; preserve qualified type/alias/return heads, nested namespace identity, generic callback contexts, ambiguity and cold-reload/deletion behavior (2026-09-06; six additional fixtures / 21 compiler-checked labels, module cohort now 49 plus existing 134; namespace-as-value flow, computed selectors, declaration merging, package policies and invalid-code navigation parity remain open).

## F2 — Semantic completeness and snapshot correctness

- [ ] Connect CFG narrowing, assignments, joins and contextual lambda/generic inference to binding IDs.
- [ ] Select overloads from argument types and language rules; preserve ambiguity instead of choosing the first declaration.
- [ ] Track semantic dependency fingerprints; incremental results must equal a fresh snapshot after signature, export, inheritance and deletion changes.
  - [x] Restore DB-loaded enclosing type IDs from numeric containment edges before merge/extension rebinding; preserve fresh facts and namesake isolation, reject missing/conflicting ancestry, and remove all 314 fresh/cold Query Core call differences without changing fresh targets (2026-09-07; cold correct 243 to 552 of 690, 2,191 selected tests passed; broader dependency/configuration parity remains open).
  - [x] Close five compiler-labelled Query Core superclass-call wrong targets using source-attested receiver kinds and parent declaration IDs; retain fresh/cold parity and distinguish 40 declaration-kind-only disagreements from different declaration locations.
    - [x] Persist source-bound base heads/applications and compose inherited GenericParamId environments; prevent legacy rebuild/prepass overwrites, restore abstract-class value/type scopes, and verify edits/deletion/filtered portable caches (2026-09-07; epoch 29 / schema 49; Query Core 557/690 correct, 40 kind-only disagreements, 93 unresolved, zero snapshot changes and no per-site regressions; 2,232 selected tests plus 12 integrations passed).
- [ ] Serve definition/reference occurrences by declaration ID and source span; fix same-line losses and query-cache limit/snapshot isolation.
  - [x] Persist called-identifier byte anchors and add a selector-addressed oracle cohort; keep nested same-spelling calls distinct without name-based duplicate keys on migrated calls (2026-09-06; IDE query API migration remains open).
- [ ] Expand language/external-supply coverage under precision and negative non-interference tests, then run the independent 99% gate.

## F3 — Product evidence and cross-service flows

- [ ] Benchmark AI task quality, wall time and tokens against grep/glob and competing tools on a held-out corpus.
  - [x] Prevent disabled trace macros from evaluating diagnostic operands, including cloned type structures and formatted aliases; add a failing-before/passing-after operand-evaluation regression (2026-09-07; removes known instrumentation overhead, not a wall-time/token benchmark result).
  - [ ] Amortize file binding environments across sequential initializer candidates without losing prior-declaration updates; measure prelude cost on large source files.
- [ ] Rebuild UI → service → database flows on bound call/data-flow edges plus explicit route, message, DI and database-mapping evidence; preserve uncertainty across dynamic boundaries.
- [ ] Validate IDE navigation, change-impact analysis and AI context retrieval against the same semantic snapshots.
