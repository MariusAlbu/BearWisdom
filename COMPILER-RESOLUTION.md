# Compiler-grade resolution — source of truth

**This document is the single source of truth for the resolution/type-inference
engine's path to compiler behavior.** It supersedes the status sections of
`RESOLUTION-EXTERNAL-ROUTING.md` (its §9/§10 describe the reroute apparatus that
was **deleted** in `a3815b4e`) and folds in the remaining items of
`TYPE-INFERENCE-ROADMAP.md`. When those disagree with this file, this file wins.

Established 2026-05-26; **restructured 2026-06-02** around the per-language 99%
gate and the one-engine architecture gate (below). Keep the **Done** table
current; move tasks into it as they land. Tag every task `[generic]` (one engine
algorithm) · `[profile]` (per-language data) · `[hook]` (per-language code) and
status `❌ not started` · `⚠️ partial` · `✅ done` · `🔬 decision needed`.

---

## Thesis — what "compiler behavior" means here

The resolver is a generic compiler **front-end**: *scope-directed*, *origin-blind*,
over **one uniform symbol/type table** with dependencies **lazily loaded** into it,
and **all language specifics are data** fed to one algorithm.

**Invariants (the bar every task is measured against):**

1. **Origin-blind.** internal / workspace / stdlib / third-party are rows in one
   table. The resolver never branches on origin; "external" is where an import
   points, not a control-flow branch.
2. **Scope-directed, never grep.** A name binds via scope/import rules or it is
   **unresolved**. No bare-name match across the whole program binding to a
   coincidental hit.
3. **Lazy metadata load.** Dependencies decode into the *same* table on demand
   (the compiler's decode-`.rmeta`/`.d.ts` step), not as a sidecar.
4. **Language specifics are data**, not algorithm — `LanguageProfile` /
   `FlowConfig` / extractor output first; `LanguageEngineHooks` only when data
   can't express it.

**Scope boundary (NOT building):** the back end — no borrow-check, trait
coherence, monomorphization, codegen. Only front-end resolution + *enough* type
inference to bind every reference to its declaring symbol.

**Definition of done (the gate this roadmap is measured against):**

1. **Every supported language reaches ≥99% internal resolution precision** — per
   QUAL-5's three-state metric (`external_known_unhydrated` excluded from the
   denominator). 99% is the bar for *every* language, not the corpus average; a
   language below 99% is an open task, not a rounding error.
2. **Reached through the one generic engine.** A language hits 99% by routing onto
   the shared algorithm with its specifics expressed as `LanguageProfile` /
   `FlowConfig` / `ChainConfig` **data** — not by growing a bespoke per-language
   resolver. A % gain bought with a new per-language algorithm is a *regression*
   against this bar even when the number goes up.

**Architecture gate (hard rule — applies to every task below):**

- **No new per-language resolution algorithm.** New language behavior lands as
  (a) profile/config **data**, (b) a generic-engine capability all languages
  share, or (c) a *minimal* `LanguageEngineHooks` delta — in that order. A new
  `walk_<lang>_chain` is forbidden.
- **Net resolution code trends down, not up.** The bespoke chain walkers
  (~3,800 LOC, QUAL-2) are debt to retire onto `resolve_via_chain`, not a pattern
  to extend. Every per-language task states whether it *adds* engine code or
  *moves* behavior into shared data.
- **Generic-first decomposition.** Before any `LANG-*` task, reduce it to the
  smallest generic-engine change that closes it for all languages at once; only
  the irreducible remainder is a hook.

This reframes the remaining work. The corpus gap does **not** close by "writing
the missing per-language rules" — it closes by finishing the generic capabilities
(§B), retiring the per-language forks onto them (§F QUAL-2), and feeding each
language its data. Incremental per-language % that drifts from this gate is
explicitly rejected.

---

## 0. Done (committed)

| id | what | commit |
|----|------|--------|
| — | Lift `BW_TS_FLOW` gate — TS flow active (phantom hang) | `502955d4` |
| QUAL-1a | Remove `ranked_candidates` + `unique_internal_name` from the default binder ladder | `019fc8a7` |
| — | Fix blast-radius tests that asserted bare-name resolution (now structural) | `5b73a311` |
| BIND-2a | SvelteKit `$lib`/`$app` alias resolution via `svelte.config` (`ts_tsconfig_alias` 31→5,680 edges) | `7fb058e7` |
| LANG-SVELTE-1 | Svelte `$store` auto-subscribe desugar at embedded splice (`$t` 370→13) | `2b4a08cf` |
| LANG-DART-1a | Drop bare Dart library-prefix refs (`i0`/`i4`) across all usage kinds (Dart unresolved 4,291→3,216) | `4be1cb83` |
| QUAL-2a | Delete the dormant `BEARWISDOM_COMPILER_RESOLVE` internal/external reroute apparatus | `a3815b4e` |
| BIND-2b | tsconfig `extends` chains for path aliases | `1471eab7` |
| EXT-2 (slice 1) | `parse_return_type_from_signature` handles `-> T` / `=> T` (Python/Rust/TS-arrow) in addition to `): T`; arrow externals populate return/field maps via the build.rs/augment.rs signature fallback. Unit-tested (`chain_walker_tests.rs`) + end-to-end through `build_with_context` (`mod_tests.rs::signature_derived_return_type_arrow_form`) | `1f03bec9` |
| QUAL-1b (H.2) | Remove the internal whole-program `by_name` family (`{c,kotlin,swift,dart,elixir}_by_name`, `rust_global_name_*`, …) | `c4252e8b` |
| QUAL-3 (H.1) | Delete the import-blind `*_synthetic_global` ext-bare-name grep family (Option A — honest floor) | `bf46353b` |
| EXT-4 | Delete the eager `seed_demand_from_user_refs` — externals load lazily via the expand loop | `aad62601` |
| — | Large-stack parse pool (`build_parse_pool`, 128MB) + iterative `scope_tree`/`recurse_for_object_types` so deep generated `.d.ts` unions don't overflow; full-corpus recapture (honest floor) | `c25ff7d9` |
| EXT-1 | Scope-directed external routing: module-scoped demand for import-qualified externals (`ChainMiss.module` → `SymbolLocationIndex::locate`), recovers the externals EXT-4 dropped, no cross-package grep | `29a055a4` |
| EXT-3 | Supertype walk over external bases: `build_explicit` includes pulled `ext:` files, so `walk_up_with_args` climbs external hierarchies and composes generics across them (reachability-bounded) | _(this change)_ |
| QUAL-5 | Three-state external model: `ResolutionBreakdown` surfaces `external_known_unhydrated` (internal `external_refs`) + a `precision` field apart from `resolved`/`unresolved_unknown`; precision excludes the unhydrated bucket. No schema change — the three states already lived in `edges`/`external_refs`/`unresolved_refs`; the gap was metric visibility | _(this change)_ |
| QUAL-2b-ts | Retire `walk_typescript_chain` (480 LOC); TS on `resolve_via_chain(&TS_CHAIN_CONFIG)`; irreducible deltas as a `ChainExtensions` data-carrier (4 bools + 1 fn-pointer). −106 prod LOC | `58ad2b24` |
| QUAL-2b-csharp | Retire `walk_csharp_chain`; extension-method probe → `extension_method_fallback` bool + shared helper; 0 new fn-pointers. −175 | `329c5a51` |
| QUAL-2b-go | Retire `walk_go_chain`; embedded promotion via shared `walk_inheritance`; 0 new fn-pointers, `chain.rs` untouched. −190 | `c406434e` |
| QUAL-2b-java | Retire `walk_java_chain` + local `find_enclosing_class`; `namespace_lookup=WildcardOnly`; 0 new fn-pointers. −146 | `22c4adc7` |
| QUAL-2b (py/ruby/php/c) | Retire 4 walkers; PHP `root_type_access` bool; C typedefs via generic `expand_aliases` (no fn-pointer); `expand_current_type` rewrites without an env for non-generic aliases. −546 | `73a578b3` |
| QUAL-2b-rust | Retire `walk_rust_lang_chain` — the **9th** walker, initially missed by the batch; `RUST_CHAIN_CONFIG` reuses the existing `normalize_type` (`::`→`.`) + `walk_inheritance`; 0 new fn-pointers. −131 | `b2c20fa3` |
| INFER-5 | Structural assignability over the member table — shape-based, with matched-member param/return types compared and missing-type-info → `Unknown`. Unblocks LANG-GO-1 for project-internal interfaces. | `d7626b76` |
| LANG-GO-1 (structural soundness) | `build_structural` routes each `(candidate, iface)` pair through INFER-5's `is_assignable_to_typed_with`, adding the edge only on `SubtypeResult::Yes` (replaces the unsound name+kind `candidate_satisfies`). One generic substitution in the shared builder — no Go code, no walker; every `Structural`/`Both` language inherits the soundness upgrade. Threads `&SymbolTypeMap` + `primitive_mapping` into `SupertypeGraph::build`. Strict ship (Unknown=no edge); positive Go edge now gated on the Go extractor interning method param/return TypeIds (currently `None`/empty). ⚠️-partial. | _(this change)_ |
| LANG-RUST-1 (Case B — trait default methods) | `build_explicit` reroutes an impl-container inheritance edge to the IMPLEMENTING type: a `Namespace` source symbol can't be a subtype, so its `Inherits`/`Implements` edge is an impl/extension container whose real child is the implementing type named in its `impl <Type>` signature (data the Rust extractor already emits). `impl_container_child_qname` decodes the base name + resolves via `types_by_name`; absent the marker, child keys on the source qname (all other languages byte-identical). `class("C") → class("Trait")` now exists so the existing `find_on_chain` walk reaches a default method filed under the trait. No new `ExtractedRef` field (no 755-site churn), no hook, no walker; reuse-only chain side. Case A (impl-body methods) already worked, untouched. ⚠️-partial — receiver-typing (autoref/deref), blanket impls, assoc-type/generic-impl composition, and external-trait default-method bodies deferred. | _(this change)_ |
| LANG-RUST-1 (fork #1 — autoref/autoderef receiver typing) | `c.foo()` where `c: &C`/`&mut C`/`&'a C`/`Box<C>`/`Rc<C>`/`Arc<C>` types the receiver as inner `C` before the member + trait-default walk. Two generic changes + one profile-data axis: (a) `intern_type_str` strips a LEADING reference sigil (`&`+optional lifetime+`mut`) via `strip_reference_sigil` (interior `&` = TS `A & B` intersection untouched); (b) `chain.rs::peel_receiver_wrappers` peels a single-arg `Apply` whose base is in the new `LanguageProfile::single_inner_wrappers` axis to `args[0]` at the chain root, fixpoint-looped (so `&Box<C>`→C). Rust axis = `["Box","Rc","Arc"]` (pure Deref-to-inner); `Vec`/`HashMap` deliberately absent (negative test proves no peel). 94-literal sweep, default `&[]` inert. The existing CASE-B trait-default reroute then binds the inner type for free. No walker, no hook. INDEX-path closed: `DefaultRootResolver`'s two `arena.class` receiver sites (the `field_type_str` member path + the `field_type_name` scope-walk path) now route through `intern_type_str`, so a generic-application field/local receiver decomposes into `Apply` at the root and the peel + `bind_apply_args` fire end-to-end, not only for a pre-interned `FixedRoot`. A plain name interns byte-identically to `arena.class`; the generic-param branch short-circuits before the intern, so a prior flat `Class("T<U>")` can only turn a miss into a structural hit, never a false bind. Deferred deeper: user `Deref` impls (assoc-type family — fork #3's family), autoref candidate ORDERING (BearWisdom over-resolves; out of a navigation tool's scope), RefCell/Cow/Pin (their `.borrow()`/`.lock()` member set is the GUARD's, not the inner's — peeling them would be UNSOUND, so the peel set stays pure-Deref Box/Rc/Arc). 2 new types tests + 3 new chain tests; 6530 lib tests, 0 regression | `2ac5b04f` |
| INFER-10 | Generic type-alias expansion in the shared walker — `expand_aliases` flipped for Rust/Python/Scala (TS/C already on); Go head-only alias-target emission for true `=` aliases (defined types non-expandable). Kotlin deferred (extractor emits no alias TypeRef). | `a948e965` |
| INFER-4 (prereq, slice 1ab) | Recursive `CallArg` variants (`Ternary`/`ArrayLiteral`/`Await`/`Spread`/`IndexAccess`/`Binary`) + TS extraction | `32104a9d` |
| INFER-4 (slice 1c) | Conservative expression-typing arms in `resolve_arg_type` — ternary/array/await/spread/index/binary, Unknown over a guess; sound `&&`/`\|\|`/`??` operand-join | `8b5fd790` |
| INFER-8 | Argument-driven generic inference at a terminal call — `unify_into` (structural inverse of `substitute`) + the `chain.rs` G2 branch; binds a callee's type params (own + owner) from resolved arg types, `is_none`-guarded so the receiver binding stays authoritative. Terminal-call scope (per-segment args + bare-name site are follow-ons). | `828c869e` |
| INFER-3 (capture) | `return_query` keyed by `strategy_prefix` (`return_query_for`) → `FlowMeta::flow_return_lhs`; TS direct-body returns of named fns/methods, nested-callback returns not misattributed | `b77b5587` |
| INFER-3 + INFER-2 (fixpoint) | Harvest return-type candidates → `join_inferred_returns` (conflict-skip, cross-file-qname-collision-skip via db_id) → `set_inferred_return` gap-fill (never overrides declared) → bounded re-resolve in `full.rs` (cap 3, zero-cost when no inferable returns). Cross-file inferred returns; `u.greet()→User.greet @1.0` proven end-to-end | `c810e6a3` |
| INFER-8 (bare-name site) | Extends arg-driven generic inference from terminal chain calls to chain-less calls. In-chain G2 unify loop extracted to shared `bind_call_arg_generics`; new `ChainWalker::infer_bare_call_yield` binds a resolved bare callee's own type params from arg types and substitutes the return (strict-improvement-only: None unless the result is a non-`Unknown`, non-bare-`Generic` type). `Engine::resolve` fills the yield on both the bare-resolver and hook-fallback paths via `fill_bare_call_yield`, gated to chain-less calls with args. `const x = makeThing(u); x.greet()→User.greet @1.0` proven cross-file; arg-untyped A/B drops the edge to zero (binding is load-bearing). 5 unit tests; 4-lens adversarial review (0 bugs) | `6746b048` |
| INFER-4/8 (breadth) | Recursive `CallArg` variant extraction wired in 10 languages (rust, java, csharp, kotlin, dart, go, php, python, groovy, ruby) — each `extract_arg` a depth-guarded recursive helper, node kinds verified per grammar, lacking shapes left `Other`. Lights up INFER-4 expression typing + INFER-8 arg-driven binding for all 10 (TS already had it → 11 total). Per-grammar nuance handled (Kotlin if-as-expression, Dart unary>await nesting, Go variadic_argument/composite_literal, Groovy real kinds). ~67 sibling tests; 6,252 lib tests, 0 failures; manual per-grammar cross-check (2 cursor-lifetime + 1 await-nesting fix). c_lang deferred | `c78c6d86` |
| EXT-foundation | `ExtractedRef` gains `is_import_binding` + `is_reexport` (per-ref booleans, default false), swept across all 755 construction sites (733 `ExtractedRef {` literals + 22 via the javascript `ExtractedRef as Ref` alias), compiler-verified no-op. Isolates the large mechanical diff from the consumers. ExtractedRef keeps its no-`Default` exhaustiveness discipline | `07e0c6bd` |
| EXT-2 (slice 2) | Drop the `r.module.is_some()` TypeRef filter; exclude only `is_import_binding` (set at the 3 ecmascript `emit_clause_refs` binding sites). Module-tagged external USAGE TypeRefs now feed `field_type`/`return_type`. Guard test + 6,269 lib tests, 0 regression | `c0ec6ce2` |
| BIND-2e (slice 1) | Gate the `reexport_map` builder on `is_reexport` (was: every `Imports` ref with a module, incl. private import statements — a name could follow through a module that merely imports it). TS `export … from` refs tagged `is_reexport=true`; TS barrel/deep-chain tests pass with the map now genuine-re-export-only. Slice 2 (Rust `pub use` tagging + generic rung) mapped. 6,271 lib tests, 0 failures | `454e8b01` |
| BIND-2e (slice 2) | Generic re-export following: `follow_reexports` lifted to `type_checker/core/reexport.rs` (generic, `kind_compatible` parameterized) + new `DefaultResolver::resolve_via_reexport_following` rung after `file_import`. Rust `pub use` tagged via `detect_visibility` threaded through `walk_use_tree`. Skip swapped from TS `is_bare_specifier` to generic relative-or-resolves-internal (non-`ext:`) — Rust `crate::bar` re-exports now follow, TS barrels unchanged. Invariant #2 held (extractor/walker/rung tests). 6,283 lib tests, 0 failures; 4-lens adversarial review, 0 confirmed | `1f134142` |
| CODEGEN-1 (slice 1) | Post-extract symbol-synthesis seam `LanguagePlugin::synthesize_symbols` (no-op default, invoked in `parse_file` before the embedded splice; FK-safe ids) + first recognizer: Java Lombok `@Data`/`@Value`/`@Getter`/`@Setter` → `getX`/`setX`/`isX` from class/field symbols + annotation refs (no re-parse). Annotation-vs-like-named-field-type discriminated by `byte_offset` at `@`. 13 Java tests incl. e2e bind. 6,293 lib tests pass (2 pre-existing ecosystem-discovery parallel-isolation flakes, green isolated) | `4f2dae31` |
| CODEGEN-1 (Lombok @Builder) | `@Builder` → `builder()` + nested `{Class}Builder` class + fluent setter per field + `build()`. `make_synth` generalized over `SymbolKind`; dedup'd; same `@` discriminator. 7 new Java tests incl. e2e `builder()` bind. Full suite green. Chain-typing of synthesized members (so the fluent chain resolves past hop 1) is the headline follow-on, generalizing to getters | `bb138d66` |
| CODEGEN-1 (chain-typing) | `synthesize_symbols` returns `Synthesized { symbols, refs }`; each synthesized method emits a return-type `TypeRef` (getter→field type, `builder()`/fluent→builder qname, `build()`→class), rebased on splice in `parse_file`. Synthesized methods type like real ones → getter + builder fluent chains resolve end to end. 4 new tests (3 ref-emission + 1 index return-type map). Full suite green; 2-lens review, 0 blocking (generic-element typing `List<User>`→`List` is a generic-engine follow-on, not Lombok) | `fdf8d952` |
| CODEGEN-1 (Rust derives) | Third recognizer on the seam: `rust_lang/derives.rs` reads the derive `TypeRef`s the extractor emits at struct/enum symbols and synthesizes the common-derive methods (`Default/Clone/From/Debug/PartialEq/PartialOrd/Ord/Hash/Into/AsRef/AsMut/Serialize/Deserialize/Error/Display`); Self-returning `default`/`clone`/`from` carry a return-type ref to the owning type so `Config::default().name` chains through, non-Self returns emit none, hand-written member wins. Deletes the prior inline `decorators::synthesize_derive_methods` (emitted no refs — chains did not type through) + its orphaned `collect_derive_names`/`collect_idents_from_token_tree`. Env-gate cleanup: deletes `ecosystem/cargo_expand_runtime.rs` (gated `BEARWISDOM_CARGO_EXPAND`, shelled `cargo expand` — violated locators-only) + its 4 `ecosystem/mod.rs` sites. Net resolution code down. 10 derives tests incl. e2e index chain-through; rust_lang (198) + ecosystem (772) green, 0 regression. C# source-generators remain (separate slice) | (this commit) |
| CODEGEN-1 (C# positional records) | Fourth recognizer on the seam: `csharp/source_gen.rs` synthesizes `Deconstruct(out T1 P1, …)` for positional records. Discriminator is the `record` keyword as a whole word before the type name (record extracts as `SymbolKind::Class` — indistinguishable by kind/signature, mirrors Kotlin `is_data_class`). Positional properties already emitted by the extractor; the recognizer keeps only those inside the parameter-list span (body properties of `record Person { … }` excluded — they sit after `{`). Void return → no return-type ref; hand-written `Deconstruct` wins. The `dto.Category.Id` chain types through with no recognizer work (rides the property signature). Deferred: `with`-expr flow typing, derived-record base members, `[GeneratedRegex]` (no synthesizable surface), arbitrary ISourceGenerator (out of scope). Zero engine/profile/parse_file edits, net resolution code flat. 11 C# tests incl. e2e index bind; csharp (190) green, 0 regression | (this commit) |
| BIND-2c | Monorepo re-export seam: workspace-package entry resolution — when the Node resolver returns `None` for a bare specifier naming a workspace package (`workspace_pkg_by_declared_name`), `module_to_file` gains its `<root>/index.*` entry, feeding the existing generic `follow_reexports` (`project → workspace pkg → npm` now terminates). No per-language code. | `4d48f862` |
| DOC-3 | `$t` long-tail closed by correction — the 13 plain-`.ts` `$t` are local params/`const` literally named `$t`, not store auto-subscriptions; the SFC store-desugar is correctly gated behind `host_is_svelte` (running it here would unbind the param). Regression-locked; no production change. | `57a47b85` |
| EXT-2 (slice 3) | JVM bytecode-descriptor decoder — `parse_return_type_from_jvm_descriptor` (`chain_walker.rs`) decodes method `(…)Ret` / field / array descriptors (array → ELEMENT type) and is wired as a JVM-gated (`java/kotlin/scala/groovy/clojure`) `.or_else()` rung in both `build.rs` and `augment.rs`. Maven/JVM externals carried descriptor signatures but yielded zero return/field types; they now hydrate. Unblocks EXT-5(JVM/PHP) + INFER-6. | `5b9e0ab4` |
| EXT-2 (slice 4) | Non-JVM cross-ecosystem finish. C aggregate-return peel — `parse_return_type_positional` (`chain_walker.rs`) now peels a leading C `struct`/`enum`/`union` keyword (`is_c_aggregate_keyword`) and reads the next token as the return type, instead of rejecting it via `is_decl_keyword_or_modifier`; a bare keyword with no following type token still yields None. C externals returning `struct curl_slist *`-style aggregates now hydrate (the non-aggregate leading form already worked). Pure shared string logic, no language-id branch. R verified as a no-code ceiling (no return-type syntax to parse; recorded for DOC-4). Unit + end-to-end tests; 53 chain_walker tests green. | _pending_ |
| BIND-2d | Dart `ModuleResolver` (relative `./`/`../`/bare + `package:<self>/`) — self-package name folded from pubspec into `package_names`, threaded via `all_resolvers_with_manifest_data`. Project-local Dart imports (`i2.X → ./x.dart`) now resolve; `dart:`/foreign `package:` decline to `classify_external`. | `9a38cc6a` |
| FACADE-1 (slice 1) + BIND-4 | `SymbolView` (`type_checker/core/symbol_view.rs`) unifies `SymbolInfo` + `SymbolTypeMap` + `ContainingScope` behind one read accessor — honest tri-state `param_types(): Option<&[TypeId]>` (`None`=unhydrated, `Some(&[])`=zero params), Roslyn `containing_type`/`containing_namespace`. Assignability filter extracted to facade-backed `arg_assignable_candidates`, shared by the chain dispatch path + the new bare path (byte-equivalent; `select_multi_arg` keeps `most_specific` + receiver fallback). First consumer **BIND-4 bare-name overload**: two-phase like a compiler — scope-filter candidates to the first-match's own file + `scope_path`, then unique-survivor arity/type, so a whole-program homonym can't hijack a 1.0 bind. First impl reverted by review (cross-scope hijack + dead Unknown gate); re-landed scope-filtered. | `2ae689c4` |
| FACADE-1 finish + `ContainingScope` source-of-truth | Type-half: 7 `symbol_types.get(id).<field>` sites onto `SymbolView` accessors + `type_data()` for the existence-gated whole-record (`subtype.rs` variance); borrowed accessors now data-lifetime (`'a`). Containment: the two flattened `enclosing_*_qname` maps **deleted** for one `FxHashMap<String,ContainingScope>` — `ScopeFrame` carries qname, `containing_type_qname`/`containing_namespace_qname` are projections, `SymbolLookup::containing_scope` exposes the structure (string→concrete-type, like the TypeId migration). `default_resolver::enclosing_type` + `augment.rs` member-grouping moved onto the structure; `sym_package` (dead) / `sym_module` (parent==module filter) / file-level / external / parser-level sites correctly left. ScopeArena threading rejected as duplicate. Full lib 6410 green. | `e74d51b8` + doc |
| QUAL-6 (enable) | Generic resolver is the default — `resolve_all_with_profile` (full 18-strategy ladder, kind-gated) replaces the 6-of-18 `resolve_bare`; `bare.rs` + the `engine_primary` axis deleted; per-language `resolve_ref` demoted to a flagless `.or_else` residue tail. Net −822 | `c800c5ea` |
| QUAL-6 (JVM+Go) | Java/Groovy/Kotlin/Scala/C#/PHP onto `ChainQualification::SamePackageAndImports`; Go onto the new `PackageShortName` (+`resolve_via_package_short_name`); 8 `*Resolver`s + their `resolve_ref` deleted | `b0894426`·`960aafdc`·`41d6df7f` |
| QUAL-6 (builtin/sep) | `builtin_skip` profile seam (drain make + 10 primitive-skip langs); `qname_separator` threaded into the ladder (drain swift/zig/hare) | `8daee82b`·`54e40699` |
| QUAL-6 (import/anchor) | Generic `resolve_via_import_path` + `ImportResolution` data (drain 7 template langs); jinja/heex/templ/dockerfile via reuse + `KindTable` data; `resolve_via_module_anchor` + `resolve_via_external_by_import` (drain dart/ruby/python) | `d37e8de2`·`43e8c957`·`1b3232e7` |
| QUAL-6 (axes+misc) | `NameNormalization` (cobol/vba), `namespace_decline`/`ambient_namespace_prefixes` (c_lang/bicep), `module_anchor` ByFileStem/MemberOfModuleType + `package_by_directory` (ocaml/odin/fortran), `wildcard_match`/`ext_match`/`decline_qualified…` (prolog/puppet/erlang/scss/pascal/nim), pure-delegation deletion bucket (clojure/haskell/fsharp/lua/perl/matlab/gdscript/vbnet) | `6329e0cf`·`622e1253`·`8a762e91`·`958c1310`·`0b45e0a9` |
| QUAL-6 (new strategies) | `resolve_via_head_alias` + `resolve_via_file_scoped_import`/`AliasDecode` (drain hcl/robot); rust via `module_anchor`(ByNameUnderModuleDir) + `::` separator + sysroot-prelude ambient + `pub use` `is_reexport` tag; starlark via `StarlarkModuleResolver` + bazel-builtins ambient | `734e73d7`·`fcf85722`·`46ff3391` |
| LANG-SWIFT-1 (part a) | Scope-directed explicit-member import: generic `resolve_via_explicit_member_import` rung (before `file_import`), gated by new `explicit_member_import` bool profile field (Swift-only opt-in). Binds the unique internal symbol named by an `import struct\|class\|enum\|protocol\|func Module.Symbol` (dotted `module_path` whose last segment == imported name == target); decline on 0/multiple. Plain `import Foundation` (no dot) stays inert — strictly narrower than the reverted `resolve_all()` fallback. No Swift code; `swift_by_name` grep not reintroduced. Part b (protocol extensions / retroactive conformance / `some`·`any`) + whole-module import boundary map deferred. 2 ladder tests; default_resolver (144) + swift resolve/profile + registry green | _(this change)_ |
| QUAL-6 (TS-family) | typescript + vue/svelte/astro/mdx/angular drained together (the coupled `TypeScriptResolver` unit); third seam retired — `root_resolver()` → `SelfReceiverDiscovery` profile data via `ProfileRootResolver`; `vue/root_resolver.rs` deleted. lib 6441/0, gate met (`resolve_ref==0` + `root_resolver()==0` ×6) | `98b96555` |
| QUAL-6 (elixir) | `resolve_via_alias_module_qname` rung (mirror of `head_alias`) + const-data `alias_module_qname: bool` axis bind a bare alias to the module symbol whose qname IS the import's full path (`alias MyApp.Foo` → `Foo`); `ElixirResolver` + the `resolve_ref` override + the re-export deleted, `build_file_context`/`classify_external`/flow detectors kept. `resolve_ref==0` for elixir; only sql/ada residual | _(prior change)_ |
| QUAL-6 (sql) | All three SQL `resolve_ref` steps → data + one generic rung: (1) `is_sql_builtin_type` → `builtin_skip` seam (prisma precedent); (2) `sql_kind_compatible` → `SQL_KIND_TABLE` + `kind_compatible_table` (bicep precedent); (3) the whole-program first-match by-name `TypeRef` bind → new `namespaceless_global_type_lookup: bool` axis + new `resolve_via_namespaceless_global` rung (flat-global, internal-only, NON-unique first-match, dead-last). `SqlHooks::resolve_ref` + `sql_kind_compatible` deleted; `classify_external`/`build_file_context`/`is_sql_builtin_type` kept. `resolve_ref==0` for sql; rung serves any namespaceless DDL/config lang. Net SQL resolution code → 0. Only ada residual | _(prior change)_ |
| CORPUS-2 (matlab/r, partial) | Two pure-data drains onto rungs the engine already ships. matlab: `MATLAB_PROFILE.package_by_directory = true` (same-folder function sibling binds first via `resolve_via_same_dir`, MATLAB path precedence) + `namespaceless_global_type_lookup = true` (dead-last `resolve_via_namespaceless_global` first-match-binds a cross-dir unique; toolbox intrinsics decline, internal-only). r: `R_PROFILE.namespaceless_global_type_lookup = true` (flat top-level R functions bind first-match to the project-internal definition, SQL-identical; testthat/base builtins + `pkg::func` qualified calls stay external). Zero engine code, three bool flips. bicep/vbnet DEFERRED: bicep needs an intrinsic-name source (new ecosystem locator — architect gate) or documented-borderline; vbnet is NuGet externals-hydration (stale index), no resolution change applies. | _(this change)_ |
| CORPUS-2 (vbnet) | Externals-parity drain — the "stale-cache" diagnosis was wrong; .NET externals already hydrate for vbnet (NuGet `ManifestMatch` + dotnet-stdlib `LanguagePresent("vbnet")` + `collect_dotnet_project_files` all match `.vbproj`), the gap was resolution-side MATCHING. (1) `VBNET_PROFILE.chain_qualification` `None`→`SamePackageAndImports` so the generic `qualify_current_ty` rewrites a bare receiver to its FQ namespace and hard-binds (1.0) to the hydrated `ext:dotnet` symbol; (2) `VbNetHooks::classify_external` override mirroring fsharp, reusing the shared data-driven `csharp::hooks::is_manifest_external_namespace` (NuGet deps + System/Microsoft namespace facts — not an API table; two-prefix fallback inlined to avoid widening csharp's `pub(super)` helper); (3) `dominant_dotnet_language` mislabel `"vb"`→`"vbnet"` (the registered plugin id). No `resolve_ref`, no vbnet externals code (pipeline reused unchanged). vbnet-compactgui recapture **56.22%→89.46%** (internal_unresolved 936→132). Deferred (same ceiling as csharp): wildcard-import bare types land as `external` not 1.0 (no per-wildcard-prefix probe in the generic walker); `Imports <alias> = X` not threaded; `My.*`/`Option Strict Off` honestly unresolved (no on-disk artifact). 27 vbnet tests + full lib 6525/0, 0 csharp/fsharp drift. | _(this change)_ |
| QUAL-6 (ada, partial) | Ada is a genuine special case — `resolve_ref` stays non-zero. CI axis drained as DATA reusing the existing `name_normalization` (no new field): `ADA_PROFILE.name_normalization = NameNormalization::Spec{case_insensitive}`, plus `norm` threaded into four byte-exact rungs (`resolve_via_qname_exact`/`same_namespace`/`imported_namespace`/`namespace_import`), each gaining a case-folding fallback gated on a non-identity spec (identity-borrow → inert for the other 93 langs; all case-insensitive langs gain CI dotted-qname resolution). Drained `ada_same_file_ci` (→ `resolve_via_same_file` under CI). Modular-type primitives (RM 13.7) moved from the bespoke `is_ada_modular_primitive`+`by_name("Interfaces.*")` scan into `classify_external` (→ `Interfaces`, the `Standard`-numeric pattern). IRREDUCIBLE & KEPT as `AdaResolver` code: with/use parent-child visibility, three-path rename substitution, generic-instantiation chasing, ancestor-package walk, variable-type field-chain dispatch, partial-qualification + last-segment-import shorthand — no generic-ladder shape. | _(this change)_ |
| INFER-6 | Container accessors type through the element: `walk_with_root` projects a subscript via the existing `index_into` and a `container_accessors`-named accessor call via the new structural `project_container_slot` (reads receiver `Apply` args — `Sequence` element=`args[0]`, `Map` value=`args[1]`), both before member lookup. New `ContainerShape`/`AccessorSlot` enums + `container_accessors` profile axis (TS: `pop`/`shift`/`at`/`find`/`get`; all other langs `&[]`). No method→type table, no hook. 4 chain tests. | _(this change)_ |
| INFER-9 (slice 1) | `TypeArena::intern_type_str` preserves `Type::Function` parameter types — the arrow branch (`type_checker/core/types.rs`) parses the last top-level parenthesized param group, comma-splits at depth zero, peels each piece's `name:` annotation, and interns the params instead of hardcoding `params: Vec::new()`. A callback signature `(value: T) => U` now carries its param `T` through interning. `substitute`/`unify_into`/`format_type`/`rebind_class_params` already iterate `Function.params` — no ripple. | _(prior change)_ |
| INFER-9 (slice 2) | `SymbolTypeMap::build_from_parsed_files` replaced its file-level `ext:` blanket skip with a per-symbol gate (`is_external && is_type_defining(kind)`): external CALLABLE and value-bearing kinds now record their interned `param_types`/`return_type`, while type-DEFINING externals stay skipped (preserves the `arena.class` self-yield write-storm guard). `symbol_types.get(member_id)` returns the callback's `param_types` for an external callable. Zero new arena writes for callables. | _(prior change)_ |
| INFER-9 (core) | Un-annotated lambda param typed from the higher-order method's callback-parameter signature. New `CallArg::Lambda { params }` (the lambda's own param names); TS `calls.rs` populates it for arrow / function-expression args (single-param `parameter` field + parenthesized `formal_parameters`). New engine `ChainWalker::seed_lambda_params` (`chain.rs`) at the binding fire-site: reads the resolved callee's `param_types[i]`, `rebind_class_params` + `substitute(env)` (env already holds the receiver-bound element via `bind_apply_args`), and when the result is `Type::Function` records each lambda param's name → `format_type(callback_param)` into the forward cache the next ref reads. Element type flows from the real signature + bound env — no method-name table. Skips a still-`Generic` callback param (unbound receiver). 2 walk tests + 3 extractor tests; 476 TS + 388 type_checker-core tests green. Other-language `CallArg::Lambda` population is the documented follow-on. | _(this change)_ |
| INFER-9-breadth | `CallArg::Lambda` extraction fanned out to 10 languages — **zero engine edits** (the seeding seam is already language-agnostic), one extractor arm per `calls.rs` + a per-language param-name helper. python `lambda` · csharp `lambda_expression`/`anonymous_method_expression` · java `lambda_expression` (bare/inferred/formal) · go `func_literal` (multi-name decls) · php `arrow_function`/`anonymous_function` (strip `$`) · dart `function_expression` (+ wired `extract_dart_call_args` into the selector-form call path that dropped all args) · rust `closure_expression` · scala `lambda_expression` (single ident + `bindings`) · kotlin trailing `annotated_lambda`/parenthesized `value_argument` lambda (synthesizes implicit `it`; `append_trailing_lambda` handles call_expression-child AND nav-sibling shapes; removed the no-`value_arguments` early-return) · swift named-param `lambda_literal`. Per-grammar node kinds verified individually. Non-identifier binding → empty slot (TS contract). ⚠️ swift `$0` shorthand + js whole-pipeline + ruby blocks + scala outer-ref emission deferred (documented forks). Failing-first tests: python/csharp/kotlin/dart `_tests.rs`. 353 calls tests + all 10 touched-language suites green, 0 regression. | _(this change)_ |
| LANG-PY-1 | Python MRO via C3. `AncestorOrder { Bfs(default), C3 }` profile axis + generic `SupertypeGraph::linearize_with_args` (Bfs arm = `walk_up_with_args` verbatim; C3 arm = C3 merge over the BFS-reachable set, reattaching the BFS pass's composed generic args). `find_on_chain` reads `profile.ancestor_order`; Python=`C3`, 95 positional literals swept to `Bfs`, spread/test profiles inherit `Bfs` from `DEFAULT_PROFILE`. No hook. Total-walk soundness: inconsistent-MRO + external-base remainder degrade to BFS order (deferred forks). 5 supertype tests (BFS-identity, C3-divergent-diamond, totality, generic-arg composition, override-selection); supertype/members/chain/profile suites green, 0 regression. | _(this change)_ |
| LANG-CPP-1 (ADL) | C++ argument-dependent lookup as a generic engine probe + one opt-in profile bool. New `Engine::resolve_via_adl` runs only when `profile.argument_dependent_lookup`, the ref is a bare (`.`/`::`/`/`-free) `Calls`/`Instantiates` call with args, AND the structural ladder declined — wired `.or_else(self.resolve_via_adl(...))` into both bare-name arms after `resolve_generic`, before `resolve_bare_post` (Invariant #2 precedence preserved). The probe types each argument via the existing `resolve_arg_types`, takes each argument-type qname's declaring-namespace prefix (`rsplit_once('.')` — uniform because `scope_tree::qualify` `.`-joins every language's qnames and `c_lang/hooks.rs` normalizes `::`→`.`), probes `{ns}.{target}` for kind-compatible (`function`/`method`/`constructor`) candidates, then binds: a single unique candidate directly @1.0, several via BIND-4's `arg_assignable_candidates` arity/type filter (unique survivor only — zero/multiple decline). One generic method + `declaring_namespace_of` helper; `argument_dependent_lookup: bool` axis true only in `C_LANG_PROFILE`, swept false across the other 93 literals + DEFAULT_PROFILE + the registry test. No hook, no `walk_cpp_*`. 2 engine tests (positive `swap(a,b)→mylib.swap`, profile-gated-off negative). Deferred forks (documented below): associated-namespace widening (bases/template-args/enclosing), operator-call ADL (extractor gap), confidence-down-weight. | _(this change)_ |
| CORPUS-1 (HTML) | Plain-HTML custom-element binding through the existing declaration-keyed selector map (NO new resolver/walker, binder untouched). New `extract_custom_element_defines` recognizer (`typescript/selectors.rs`) harvests `customElements.define('tag', Class)` / `window.customElements.define(...)` → `(tag, class_qname)` pairs (the web-platform analog of `@Component({selector})`); `parse_file.rs` selector gate widened to also harvest `define()` for `javascript`; `HTML_PROFILE.selector_resolution` flipped on (`[Calls]`, `PascalToKebab`); `html/extract.rs` emits a `Calls` ref per custom-element/PascalCase tag (mirrors `astro::process_element`, generated-HTML early-return preserved). `<user-card>` binds ONLY when a project `define()` registered it; a library tag (`<ion-button>`) with no define declines the map and falls to None (every HTML bare-name rung inert). Soundness proven by negative test. Deferred: project-wide-vs-per-file registry scope (project-wide kept — matches the global `CustomElementRegistry`), inline-`<script>` define() embedded-region propagation. 13 html-module + 8 selectors tests green. | _(this change)_ |

ts-immich: grep-baseline 6,179 → **4,627** unresolved, with honest structural edges.

> **Reality check on the deleted apparatus.** `RESOLUTION-EXTERNAL-ROUTING.md`
> §9/§10 logged a routing spine + externals veto + `external_unique_symbol` +
> arrow-return hydration as "landed." All were gated behind
> `BEARWISDOM_COMPILER_RESOLVE` = **off** — validated in A/B but **never shipped**.
> `a3815b4e` removed them. Their *goals* (D2/D4/D5/S5b below) remain valid and
> are re-anchored here on the clean binder-first path, not the reroute.

---

## A. Binder discipline (name resolution)

### BIND-1 — Lexical shadowing precedence  ·  [generic]  ·  ✅
**Compiler feature:** local shadows param shadows enclosing-member shadows import shadows global.
**Done:** `resolve_via_same_file` now yields (returns `None`) when a non-wildcard import binds the target name — so an explicit import wins over a coincidental file-level sibling. True lexical locals still win because `resolve_via_scope_visible` runs first (#1). Gating, not reordering, keeps the change surgical.
**Deps:** —

### BIND-2 — Complete import/module resolution  ·  [profile]+[generic]  ·  ⚠️
**Compiler feature:** resolve every import specifier to a module (alias, re-export, prefix, package entry, wildcard), then bind the imported name.
- **2a** SvelteKit `$lib`/`$app` — ✅ (`7fb058e7`).
- **2b** tsconfig `extends` chains — ✅ `parse_tsconfig_paths_with_extends` (`ecosystem/manifest/npm.rs`) follows `extends` (relative + `node_modules` package presets, array form, child-wins, cycle-guarded); the npm manifest reader now uses it for `path_aliases`. Inline test module split to `npm_tests.rs` per the sibling-test rule.
- **2c** Re-export / barrel chains (TS/JS) — ✅ multi-hop **works** (TS relative barrels recurse to depth 5, `typescript/aliases.rs:293`; generic external-npm BFS to depth 4 with cycle guard, `engine/index/classify.rs:110`); the **monorepo seam** (`project file → workspace package → external npm`) is closed by **BIND-2c** (`4d48f862`) — a bare specifier naming a workspace package resolves to its `<root>/index.*` entry into `module_to_file`, feeding the generic `follow_reexports`. The *cross-language* generalization is **2e** (✅).
- **2d** Import-prefix → module binding (the Dart `i0.X` case, see LANG-DART-1b) — ✅ (`9a38cc6a`): a Dart `ModuleResolver` resolves relative + `package:<self>/` specifiers.
- **2e** **Generic re-export following across languages** — ✅ (slice 1 `454e8b01`; slice 2 `1f134142`).
  - **Slice 1 (soundness gate) ✅:** the `reexport_map` builder (`build.rs:646`) now gates on `is_reexport` — previously it collected EVERY `Imports` ref with a module, including private import-statement refs, so a walk could follow a name through a module that merely imports it. The TS extractor tags its `export … from` refs `is_reexport=true` (named/wildcard/bare-import re-export; the triple-slash directive stays false). TS's `follow_reexports` keeps working with the map now restricted to genuine re-exports; private import-statement refs correctly excluded. 6,271 lib tests, 0 failures.
  - **Slice 2 (the generic consumer) ✅ `1f134142`.** Two pieces landed: (a) **Rust `pub use` tagging** — `extract_use_names` (`rust_lang/calls_imports.rs`) computes `is_reexport = detect_visibility(use_decl) != Private` once and threads it through `walk_use_tree` to every emitted `Imports` ref; `pub`/`pub(crate)`/`pub(super)` use re-exports tag true, private `use` stays false (and `pub use ...::*` tags the wildcard ref). The already-gated `reexport_map` builder admits them automatically. (b) **the generic rung** — `follow_reexports` lifted out of the TS plugin into `type_checker/core/reexport.rs` (`pub(crate)`, `kind_compatible` parameterized as a fn-pointer), consumed by `DefaultResolver::resolve_via_reexport_following`, wired into `resolve_all` after `resolve_via_file_import`. The per-entry skip moved from the TS-only `is_bare_specifier` to a generic `is_relative_specifier(source) || resolve_module_from(source) resolves to a non-`ext:` file` — relative specs are internal by construction and always followed (preserving TS barrel behavior exactly), bare specs are followed only when they resolve to a project-internal file (Rust `crate::bar` does; `react` resolves to `ext:`/nowhere and is left to the externals stage). **Soundness (Invariant #2):** proven at three levels — extractor (`pub` vs private tag), walker (empty `reexports_from` → no hop), rung (private-use import → no resolution). 6,283 lib tests, 0 failures; 4-lens adversarial review, 0 confirmed findings. **Aliased Rust `pub use X as Y`** resolves via the existing alias-synthesis pass (`build.rs:160`), not the walk — single-hop covered, multi-hop-aliased-chain is a known long-tail gap, not a regression. **Python still deferred** — re-export detection there is genuinely ambiguous without scanning `__all__` (treating every `__init__.py` relative import as a re-export over-follows).

### BIND-3 — Visibility / access-control awareness  ·  [generic]  ·  ✅ (over-resolve)
**Compiler feature:** won't bind `obj.privateMember` from outside its class.
**State (corrected — NOT a uniform hint):** visibility is INCONSISTENT today. It is a **hard gate** in C#/Go/Java/Kotlin/PHP — `is_visible` filters out a `private` target whose file differs from the ref's (`csharp/hooks.rs:199`, `go/hooks.rs:197`, `java/hooks.rs:208`, `kotlin/hooks.rs:232`, `php/hooks.rs:222`). It is a **ranking hint** (`+50` public / `-200` private) only on the ranked-candidates path (`default_resolver.rs:901`). It is **ignored** in the chain walker (`chain.rs` reads visibility nowhere) and for TS/JS/Rust. Net: `obj.privateMember` from outside resolves in TS/JS/Rust and through any chain, but fails in the 5 gated languages.
**Done — over-resolve (deliberate, non-compiler divergence).** BearWisdom is a navigation tool; go-to-definition must reach private members. The audit undercounted — gating existed in **7** languages (C#/Go/Java/Kotlin/PHP **+ Rust + Scala**), all now `is_visible → true`, so visibility never blocks a bind and every language is consistently visibility-blind. This is intentional — do not re-add a visibility gate later thinking the inconsistency is a bug. Two resolution tests that asserted private-cross-file non-resolution were inverted; `java`/`php` analogues still pass because the bare private name isn't import-reachable (legitimate scope behavior, not a gate). The `+50/-200` ranking skew (`default_resolver.rs:901`) is left as-is — it can't block resolution, only mis-order disambiguation.
**Deps:** —

### BIND-4 — Overload disambiguation (arity + parameter type)  ·  [generic]  ·  ✅ (`2ae689c4`)
**Compiler feature:** when N symbols share a name in scope, bind the call to the **one** whose signature matches the call's arity and argument types — not "a symbol of that name."
- **Member / method chains — ✅ generic, already landed (was untracked).** `members.rs:315 find_on_chain` selects `type_hit → arity_hit → first`; `dispatch.rs:93 select_multi_arg` + `most_specific_index` + `select_return_type` pick the most-specific overload by resolved arg types; wired through `chain.rs:388-440`. This is the correct generic shape — reuse it.
- **Bare-name function / static-method calls — ✅ (`2ae689c4`, scope-filtered, via FACADE-1).** Was: `foo(a,b)` with several same-name `foo` bound the **first** name+kind match at confidence 1.0, violating Invariant #2 in spirit. Now two-phase like a compiler — `Engine::select_bare_overload_override` → `ChainWalker::select_bare_overload`: (1) restrict candidates to the resolved first-match's own scope (same file + `scope_path`) so a whole-program homonym can't hijack the bind, (2) keep only when the facade-backed `arg_assignable_candidates` filter (arity, type-refined when args are concrete) leaves a **unique** survivor. Strategy `bare_overload_arg_typed`, confidence unchanged. The first impl was reverted by review (cross-scope hijack via the global `by_name`; a dead Unknown-arg gate) and re-landed scope-filtered.
**Follow-on:** most-specific among multiple same-scope **same-arity** survivors (currently conservative unique-survivor only) — reuse `most_specific_index` on the bare path once a corpus case demands it. The per-language `erlang_*_arity` hooks can now fold onto this generic path.
**Deps:** INFER-4 ✅ (arg types) · FACADE-1 ✅ (`SymbolView`). **LANG-CPP-1's overload clause consumes this; it does not own it.**

---

## B. Type-checker breadth (generic inference — the spine)

These are what make the engine a *checker*, not a binder-with-generics. All
`[generic]` — each lifts every language at once. **This is the block that closes
the corpus gap without per-language code; finish it before any `LANG-*` work.**

### INFER-1 — Control-flow-graph-based narrowing  ·  [generic]  ·  ✅
**Compiler feature:** narrow across the whole flow graph — if/else merge, loop back-edges, reassignment *kills* a narrowing, `&&`/`||` short-circuit, exhaustiveness. Decision: **CFG-native** — consumers query the CFG directly via `fact_at(name, ref_byte)`; real union types at joins; the chain walker dispatches across `Union` members for member lookup.

**Landed (commits `afedb777` · `d45e21dd` · `402756cc` · `16940b8b` · `e405c157` · `c78db287` · `6bda447b` · `ab0d12fb`):**
- **Foundation** — `indexer/flow_cfg.rs` carries the per-function CFG: `BasicBlock { byte_range, defs }` + `Edge { from, to, guard: FactMap }` + `Cfg { blocks, edges, entry, in_facts }` + `FileCfg { functions }`. `Fact = Single(String) | Union(Vec<String>) | Never` with canonical-sorted unions; `FactMap::join` drops names absent on either side (a narrowing must hold on every reaching path) and unions disagreeing types. `run_dataflow` is a worklist forward pass with a **visited-bit** in `compute_in_fact` so loop back-edges don't pessimistically drop untouched names on the first pop. `fact_at` / `fact_string_at` give consumers the in-fact at a program point, with block-local defs applied up to the cursor.
- **Consumer wiring** — `FlowMeta.cfg: FileCfg`; `run_flow_queries` builds it (dispatch via `cfg_node_kinds_for` keyed by `strategy_prefix`); `LocalTypeCache.cfg` stores it; `LocalTypeCache::lookup` consults `fact_string_at` first, falling back to the interval `narrowings` vec when the CFG is absent or returns a `Union`/`Never` the consumer cannot yet represent. `SymbolLookup::install_local_cache` plumbed across the trait + impl + the `loop_body.rs:340` call site.
- **Four narrowing cases**:
  - **if/else join merge** — `build_if` emits the diamond with peeled `else_clause`; the join's in-fact is the pointwise merge of predecessors. TS-specific `condition_to_true_guard` recognizes `typeof` / `instanceof` shapes; **`&&`** composes both sides on the true edge, **`||`** unions them (producing real `Fact::Union` when the operands disagree on a name's type).
  - **loop back-edge fixed point** — `build_loop` emits `pred → header → body → header` + `header → exit`. A def in the body propagates back through the worklist; a use BEFORE the def textually but inside the loop is correctly NOT narrowed (the interval model misses this because it's byte-ordered). A body that doesn't touch the name preserves the narrowing through the fixed point.
  - **switch** — `build_switch` emits `pred → scrutinee → { case, ..., default } → exit`. Cases are disjoint blocks (no fall-through modeled). Post-switch join correctly drops a narrowing if any case reassigns the name.
  - **def-resets-fact** — generalizes the slice-1 interval truncation: a def inside a block kills any in-fact for that name from the def byte forward. Block-local in `fact_at`, and propagated through the worklist's per-block kill set.
- **Per-language `CfgNodeKinds` tables** — fourteen languages: TS · JS · Java · Python · C# · Rust · Go · C · PHP · Lua · Groovy · Scala · Kotlin · Ruby · R. Narrowings inherit per-language via `guards_for_range(meta.narrowings, body_range)` — every language whose `type_guard_query` lands narrowings on guarded scopes gets CFG-native narrowing through the shared pipeline, no per-language condition parsing.
- **Generic widenings to the kind table** that unblocked the harder grammars: `switch_kind`/`switch_case_kind`/`switch_default_kind` → slices and `switch_body_field` → `Option` (Go's `expression_switch_statement` + `type_switch_statement` differ structurally and have no body wrapper); `block_kind` → `block_kinds` slice (Ruby uses three: `body_statement` for the method body, `then` for if, `do` for while/until; R uses `braced_expression`); `transparent_kinds` slice for pass-through wrappers (Go's `statement_list`, Lua's `variable_declaration`, Kotlin's `function_body`); `find_function_body` descends one level through `transparent_kinds` so Kotlin's `function_declaration > function_body > block` is reached without a per-language hook.
- **Chain walker Union dispatch** (`ab0d12fb`) — the final loop close. `Cfg::fact_union_at` and `FileCfg::fact_union_at` project `Single → [s]`, `Union → branches`, `Never → None`. `LocalTypeCache::lookup_union` mirrors `lookup()` returning `Vec<String>`; `SymbolLookup::local_type_union` is the new trait method (default impl wraps `local_type` for backwards compat); the `LOCAL_TYPE_CACHE` thread-local overrides it. `DefaultRootResolver`'s Identifier branch now calls `local_type_union` and intern's a `Type::Union(member ids)` for the multi-branch case, returning that as the chain root. The existing Union arm in `core/members.rs:245` handles member lookup — every branch must carry the member (the safe semantic: a runtime value could land on any branch). The architect's "real union types at joins" decision is now observable end-to-end at the chain walker.
- **Tests**: 35 new tests (foundation Fact math, if/else, `&&`/`||`, loop back-edge, switch, def-kill, 12 per-language smoke tests — three narrowing assertions for Groovy/PHP/Ruby `instanceof`/`is_a?`, structural-only for Rust/Kotlin/Scala/C/Lua/R/Go/Java/C#/Python — plus two end-to-end Union dispatch tests in `core/chain_tests.rs`: `cfg_union_narrowing_resolves_member_present_on_every_branch` and `cfg_union_narrowing_drops_when_member_missing_on_a_branch`). **6022 bearwisdom lib tests, zero regressions**.

**Architectural decisions (resolved):**
1. **CFG data structure** — hand-rolled `Vec<BasicBlock>` + edge list (no petgraph dependency).
2. **Build location** — extract-time, serialized into `FlowMeta`.
3. **Fixed-point strategy** — worklist with `iter_cap=8` per block; visited-bit skip on unvisited preds; widen-by-drop on non-convergence.
4. **Coexist vs. replace** — ✅ replace; consumers query the CFG directly. Interval `Narrowing` is the fallback for languages without `CfgNodeKinds` and for `Union`/`Never` facts the consumer can't yet consume.
5. **Fact representation at joins** — real `Fact::Union` in the CFG (inline enum, not a type-arena variant — avoids touching the arena until the chain walker is ready to dispatch across unions).

**Closed-out items (no further INFER-1 work):**
- **Interval `Narrowing` vec retirement** — investigated 2026-05-28 and kept. `meta.narrowings` feeds both the CFG (as edge guards) and the interval fallback in `LocalTypeCache`; the interval path covers top-level narrowings outside any function body (Python `if isinstance(x, T):` at module scope is the canonical case) and non-CFG-native languages. Documented as a fallback rather than retired.

**Adjacent items that belong elsewhere (NOT INFER-1):**
- **Rust if-let narrowing** — needs ADT/payload lookup (`Option<T>` → `T`, `MyEnum::Variant(P)` → `P`). Now unblocked by the chain-walker Union dispatch consumer surface, but the actual feature lives in a future LANG-RUST-1 / type-system slice.
- **Go runtime `flow_config()` is `None`** (OOM workaround in `languages/go/mod.rs`) — `GO_CFG_KINDS` is wired and the type-switch path is structurally sound (proved with a synthesized-narrowing smoke test), but until the assignment_query OOM is fixed Go inherits no actual narrowings at indexing time. Belongs to a Go-investigation slice.
- **Go `discriminant_guard_query`** — empty in v1; expression-switch case discriminants would narrow the scrutinee per case. Belongs to LANG-GO-1.
- **Remaining ~10 languages without `CfgNodeKinds`** — Dart, Swift, the smaller-volume tree (Bash, Perl, Haskell, Elixir, Erlang, OCaml, Hare, …). Per-language tables, ~30–60 lines + smoke test each. Lower yield since most have no `type_guard_query`. Belongs to a long-tail expansion slice.

**Deps:** — (foundational; INFER-2/3/4 depend on this)

### INFER-2 — Interprocedural type propagation  ·  [generic]  ·  ✅ (inferred return types)
**Done.** An *inferred* return type now crosses the file edge. The resolve orchestrator (`full.rs`) harvests per-pass return-type candidates (`ResolutionStats::inferred_returns`), gap-fills them into the cached `SymbolIndex` (`set_inferred_return` — fills `type_info[qname].return_type` only when absent, so a declared/signature return is never overridden), and re-resolves, iterating to a fixpoint bounded by `MAX_RETURN_ITERATIONS=3` and exiting the instant a pass fills nothing new. A function's inferred return is then read by chains in OTHER files on the next iteration. Built jointly with INFER-3 (capture `b77b5587`, fixpoint `c810e6a3`).
**Scope:** carries inferred *return* types (INFER-3's product). Inferred *local* types still live in the per-file `LocalTypeCache` (within-file forward flow); external-chain interplay (an inferred return unlocking a fresh external demand) is a follow-on — the fixpoint runs after the externals loop with no expand step.
**Deps:** INFER-3 ✅.

### INFER-3 — Body-based return-type inference  ·  [generic]  ·  ✅ (TS; direct-body returns)
**Done.** An un-annotated function's return type is inferred from its `return <expr>` expressions and propagated through the INFER-2 fixpoint.
```ts
export function makeUser() { return new User(); }   // ✅ return inferred User (from `new User()`), no annotation
const u = makeUser(); u.greet();                     // ✅ u:User via forward-cache → u.greet() → User.greet @1.0, CROSS-FILE
function pick(c) { return c ? a() : b(); }           // ⚠️ return nested in nothing here, but a return inside if/for is NOT captured (direct-body only)
items.forEach(x => { return x.id; });                // ✅ inner-callback return NOT misattributed to the enclosing fn (direct-child query scope)
```
Capture: a `return_query` keyed by `strategy_prefix` (`flow.rs::return_query_for`, TS only) matches a `return <expr>` that is a DIRECT statement of a named function/method body → `FlowMeta::flow_return_lhs`. Harvest (`loop_body.rs`) records `(qname, fn_db_id, resolved_yield)`; `join_inferred_returns` infers the one type all of a function's returns agree on (conflict → skip), skipping qnames owned by >1 function (cross-file collision) and the `unknown` / bare-generic sentinels.
**Scope / follow-ons (sound conservative misses):** TS named functions/methods, direct-body returns only. Not yet — returns nested in `if`/`for` (widen the query), arrow-const bodies, other languages (per-language `return_query`).
**Deps:** INFER-4 ✅.

### INFER-4 — Expression-level type inference  ·  [generic]  ·  ✅ (TS + 10 languages; c_lang deferred)
**Compiler feature:** type an arbitrary RHS expression.
**Prerequisite — done.** `CallArg` (`types.rs:435`) gained recursive variants (`Ternary`/`ArrayLiteral`/`Await`/`Spread`/`IndexAccess`/`Binary` with `Box<CallArg>` children); the TS extractor populates them. `resolve_arg_type` (`dispatch.rs`) grew conservative recursive arms — ternary commits only when both branches agree on a non-`Unknown` type; array literals yield `Apply<Array,[T]>` only for a single homogeneous element type; `await`/spread peel only known async/iterable wrappers; index-access types `Array<T>[_]→T` / `Map<K,V>[_]→V` / tuple-by-int-literal / class-by-string-key; binary ops route relational→`Bool`, short-circuit→operand-join, arithmetic/`+`/bitwise→numeric/Str only when both operands qualify. Object-literal-as-synthetic-`Class` was rejected as **unsound** (a memberless `Class` is more permissive than `Unknown` in `args_assignable`). Every arm is Unknown-over-a-guess.
**Breadth done (`c78c6d86`):** the recursive variants are now extracted in 10 more languages — rust, java, csharp, kotlin, dart, go, php, python, groovy, ruby — each `extract_arg` a recursive helper with a `MAX_ARG_DEPTH` guard, node kinds verified per grammar. Shapes a language genuinely lacks stay `Other` (e.g. Rust/Go/Java no call-site spread; Go/Java/Kotlin no await). Per-grammar nuance: Kotlin `if_expression → Ternary` (if-as-expression); Dart `await` nests under `unary_expression` (transparent peel); Go `variadic_argument → Spread`, `composite_literal → Ident(type)`; Groovy uses real `ternary_expression`/`array_literal`/`array_access` kinds. Lights up INFER-4 expression typing AND INFER-8 arg-driven generic binding for all 10. So INFER-4/INFER-8 now fire across 11 languages (TS + these 10).
**Remaining:** `c_lang` deferred (no existing arg extractor — net-new wiring, no generics, only 3 applicable shapes). The long-tail languages without a `calls.rs` arg extractor are extraction work, not resolution drift.
**Deps:** — (prerequisite landed; it also unblocked INFER-8). Verified 2026-06-02 (workflow `wmtkasf8r`).

### INFER-5 — Structural assignability / shape matching  ·  [generic]  ·  ✅ (`d7626b76`)
**Done.** `is_assignable_to` (`subtype.rs`) gains a shape-based positive arm over the member table (`MembersIndex` + `SymbolTypeMap`): a Class is structurally assignable to another when it carries every target member with a compatible kind AND assignable param/return types. Gated to (Class, Class), runs after the nominal arms, emits only Yes/Unknown (never No). **Soundness:** a missing member, an incompatible member type, or absent type data → Unknown, never a false Yes (a false "assignable" would mis-select overloads and mis-resolve Conditional aliases across all languages). Member-type recursion bounded at depth 4. **Coverage ceiling:** `MembersIndex` skips `ext:` files, so external interfaces (e.g. Go stdlib `io.Reader`) yield Unknown — this satisfies project-internal interface targets only; external structural satisfaction awaits hydration. Unblocks LANG-GO-1 (implicit interface satisfaction) for internal interfaces.
**Deps:** — (was the prerequisite for LANG-GO-1)

### INFER-6 — Built-in container generic semantics  ·  [generic]+[profile]  ·  ✅
**Compiler feature:** `Array<T>.pop():T`, `Map<K,V>.get():V`, `await Promise<T>:T`.
**Done.** A chain past a container accessor types THROUGH the element. Two forms route in `walk_with_root` before member lookup, both projecting the element from the receiver's `Apply` ARGS structurally (never a method→type table):
- a subscript `arr[i]` (`SegmentKind::ComputedAccess`) → the existing INFER-4 `index_into` (`Array<T>[_]→T`, `Map<K,V>[_]→V`, tuple-by-int, class-by-key);
- a built-in accessor call `arr.pop()` / `m.get()` whose name + receiver shape match a `container_accessors` entry → `project_container_slot`.

The element source is **structure**: the `container_accessors` axis (`&[(method, ContainerShape, AccessorSlot)]`) is profile DATA — it names which method projects which slot (`Sequence` element = `args[0]`, `Map` value = `args[1]`), and EXT-2 hydration is what populates the `Apply` args on external stdlib containers. The container heads (`Array`/`ReadonlyArray`/`Set`/`Map`) are synthetic builtins with no project members, so projecting before member lookup never shadows a real member; an empty axis projects nothing (byte-identical chain miss). TS carries `pop`/`shift`/`at`/`find`→element + `get`→value; JS left empty (`has_generics:false` → no `Apply` element to read); every other language `&[]`. No hook. `await Promise<T>:T` was already covered by the existing async-wrapper peel. 4 chain tests (Array pop, Map get, ComputedAccess, empty-axis identity).
**Deps:** EXT-2 ✅ (supplies external `Apply` args).

### INFER-7 — Turbofish + cast/assertion adoption  ·  [generic]  ·  ✅ (re-scoped)
**Done, for what it actually covers.** Turbofish consumed (`chain.rs:449`); cast/type-assertion adopted as root and mid-chain type (`chain.rs:322,363`; extractor emits it, `typescript/calls.rs:926`); the LHS annotation seeds `local_type` forward so any later chain rooted on the annotated var gets the declared type (`loop_body.rs:347`). The only unused slot is `expected_return` for overload selection (`DispatchQuery`, always `None` from `loop_body.rs:456`).
**Scope correction (was titled "expected-type / bidirectional" and marked closed):** that title overstated. Turbofish + cast adoption is the *explicit-annotation* expected-type path. The **dominant** bidirectional flow — an expected callback-parameter type seeding an un-annotated lambda parameter (`arr.map(x => x.foo)`) — is **not** built here; it is **INFER-9**. Read INFER-7 as closed only for the explicit cases, not as "bidirectional inference is done."

### INFER-8 — Argument-driven generic inference  ·  [generic]  ·  ✅ (terminal + bare-name call)
**Done.** New `unify_into` (`generics.rs`) — the structural inverse of `substitute`: it matches a declared parameter type (a generic-bearing pattern) against a resolved argument type and *produces* `GenericEnv` bindings. A new **G2** branch in `resolve_via_chain` (`chain.rs`, after the G1 turbofish branch) runs on a terminal call with no turbofish: it rebinds the param-blind declared `param_types` (`Class("T")`) to canonical `Generic` ids via `owner_param_type_map` — **the same map `yield_type_of` rebinds the return with** — then unifies each against the resolved arg types.
```ts
// class Repository<T> { findOne(filter: T): T }   const u = repo.findOne(user);  u.name
repo: Repository<Account>; repo.findOne(user)  // ✅ bind_apply_args pins T→Account FIRST → unify_into skips (env.get(T)=Some) → yields Account [receiver authoritative]
repo: Repository;          repo.findOne(user)  // ✅ T unbound → unify_into binds T→User → yields User → records u:"User" → u.name resolves [the gain]
box.wrap<Admin>(user)       /* wrap<T>(x:T):T */ // ✅ G1 turbofish pins T→Admin; G2 gated off (type_args present) → Admin
box.wrap(mystery)           /* mystery untyped */ // ✅ arg→Unknown → unify_into skips → yields unbound T (byte-identical to pre-INFER-8)
```
**Soundness (conservative, widening-only).** `unify_into` binds a slot **at most once** (first concrete position wins), **never to `Unknown`**, and recurses only into matching constructors (`Apply` same-base+arity, `Function`, `Tuple`, single-inner wrappers, param-side `Optional` peel); `Union`/`Intersection`/shape-mismatch are silent no-ops leaving the slot opaque. Both the method's own params **and the owner's** are bindable — the `env.get(p).is_none()` guard + `bind_apply_args` running first keep the receiver's binding authoritative, so an argument can only **fill** an owner param the receiver left unbound, never override a pinned one. (Owner params were initially excluded; the adversarial review flagged that as a missed-inference of the dominant repository pattern — the `is_none` guard already prevents the clobber the exclusion was guarding against. Locked by `receiver_binding_wins_over_arg_driven_inference`.)
**Scope boundary.** Two of the original three call shapes resolve. Terminal chain calls (`repo.findOne(u)`) — the original landing. Chain-less calls (`const x = genericFunc(u)`, which route to the bare-name resolver, not the chain walker) — the **bare-name site** (`6746b048`): `Engine::resolve` fills the yield via `infer_bare_call_yield` on the bare-resolver and hook-fallback paths. The within-file `const x = recv.genericMethod(args); x.member` path is covered via the `loop_body.rs` forward-inference cache. **Remaining follow-on: per-segment `ChainSegment` call args** — `call_args` lives on the terminal `ExtractedRef`, so the single-expression mid-chain walk-past (`repo.find(user).name` in one expression) is uncovered; closing it needs a `ChainSegment` field + per-language chain-builder change (~30 construction sites across 14 languages). Its only marginal coverage over the two landed shapes is that single-expression form — deferred pending DOC-4 evidence it is a real corpus contributor (the multi-statement and bare-call forms it would otherwise overlap are already covered). 16 `unify_into`/`infer_bare_call_yield` unit tests + 6 chain integration tests; 6,180 lib tests, 0 regressions.
**Deps:** INFER-4 (arg expression types) ✅.  **Corpus leverage: high** — generic helpers, repository/DAO methods, factory functions in every language with generics (where the call is the chain leaf).

### INFER-9 — Contextual callback-parameter typing  ·  [generic]+[profile]  ·  ✅ (core; TS-first extraction)
**Compiler feature:** type an **un-annotated** lambda parameter from the expected callback-parameter type of the method it is passed to — `arr.map(x => x.foo)` types `x` as the array element. The single highest-frequency inference case in JS/TS/Kotlin/Swift/C#.
**Done (core seam, `[generic]` engine + TS-first `[extractor]`):** the element type flows **structurally** from the callee's signature, not a method-name table.
- `CallArg::Lambda { params }` (`types.rs`) carries the lambda's own positional parameter names (the seed keys). The exhaustive-match ripple is one site (`dispatch.rs` — a lambda arg has no class type for overload dispatch → `Unknown`).
- TS `calls.rs` populates `CallArg::Lambda` for `arrow_function` / `function_expression` args: the bare single-param arrow's `parameter`-field identifier and the parenthesized `formal_parameters` (`required_parameter`/`optional_parameter` `pattern` identifiers). A non-identifier binding (destructuring/rest) contributes an empty slot.
- `ChainWalker::seed_lambda_params` (`chain.rs`) at the binding fire-site (after `bind_call_arg_generics`, before `yield_type_of`): the receiver's `Apply` args are already bound into `env` by `bind_apply_args` (`T→User` for `User[].map`). For each `CallArg::Lambda` at position `i`, read the callee's `param_types[i]` (the slice-2-admitted callback `Function` type), `rebind_class_params` (`Class("T")`→`Generic(T)` via `owner_param_type_map`) + `substitute(env)`; when the result is `Type::Function`, record each lambda param name → `format_type(callback_param)` into the per-file forward `LOCAL_TYPE_CACHE` the next chain ref reads through its root resolver. A still-`Generic` callback param (unbound receiver — raw / untyped array) is skipped, so an unbindable receiver is a silent no-op, never a wrong bind.
**Why no profile field:** the brief proposed a `LanguageProfile` axis declaring which stdlib higher-order methods carry element-typed callbacks. It is **unnecessary** — the element type derives from `param_types[i]` + the receiver's bound generic env (the data-first path the gate prefers), exactly as INFER-6 made container element typing structural. Zero new per-language resolver/walker; net new engine surface is one method + one enum arm + one extractor arm.
**Scope (TS-first):** the original slice emitted `CallArg::Lambda` only from the TS `calls.rs` extractor (the dominant case). The seeding engine is language-agnostic; populating `CallArg::Lambda` in the other languages' `calls.rs` extractors is the incremental follow-on tracked as INFER-9-breadth below.

### INFER-9-breadth — per-language `CallArg::Lambda` extraction  ·  [extractor]  ·  ✅ (10 langs; swift partial; js/ruby deferred)
**Compiler feature:** the engine seam from INFER-9 is language-agnostic — `seed_lambda_params` keys purely on `CallArg::Lambda` regardless of source language. This slice is pure extractor breadth: recognize a lambda/closure/arrow/block passed as a call argument in each language's `calls.rs` and capture its **own positional parameter names** into `CallArg::Lambda { params }`, mirroring the TS arm. A non-identifier binding (destructuring / typed pattern / anonymous) contributes an empty-string slot to keep positions aligned with the callback signature — identical contract to TS.
**Done — zero engine edits.** No `LanguageProfile` field, no `ChainConfig`, no hook. Each language gains one (or two) extractor arm feeding the existing generic seam; per-grammar node kinds were verified individually against the installed grammars (the INFER-4/8 fan-out precedent).

| Lang | Node(s) recognized | Param shape | Marker |
|---|---|---|---|
| python | `lambda` | `parameters: lambda_parameters → identifier` | ✅ |
| csharp | `lambda_expression`, `anonymous_method_expression` | `implicit_parameter` (single) \| `parameter_list → parameter.name` | ✅ |
| java | `lambda_expression` | bare `identifier` \| `inferred_parameters → identifier` \| `formal_parameters → formal_parameter.name` | ✅ |
| go | `func_literal` | `parameter_list → parameter_declaration` (multi-name per decl) | ✅ |
| php | `arrow_function`, `anonymous_function` | `formal_parameters → simple_parameter.name` (strip `$`) | ✅ |
| dart | `function_expression` | `formal_parameter_list → formal_parameter → identifier` | ✅ |
| rust | `closure_expression` | `closure_parameters → identifier` (non-ident pattern → empty slot) | ✅ |
| scala | `lambda_expression` | `parameters: identifier` (single) \| `bindings → binding.name` (multi) | ⚠️ |
| kotlin | trailing `annotated_lambda`/`lambda_literal` sibling + parenthesized `value_argument` lambda | `lambda_parameters → variable_declaration` ident; absent → synthesized `it` | ✅ |
| swift | trailing/parenthesized `lambda_literal` | `type: lambda_function_type → lambda_parameter.name` | ⚠️ |

**Kotlin (the load-bearing Tier-B case):** the dominant form is the trailing lambda `list.map { it.foo }` / `{ x -> ... }`, an `annotated_lambda` sibling of the call — NOT a `value_argument`. The Calls ref is built from either the wrapping `call_expression` (lambda is a direct child) or, when the callee is `obj.method`, the inner `navigation_expression` (lambda is a following sibling under the `call_expression` parent); `append_trailing_lambda` handles both shapes. The implicit single parameter `it` is synthesized as `params=["it"]` when `lambda_parameters` is absent — sound, the grammar guarantees `it`. Also fixed: `extract_call_args` no longer early-returns when there is no `value_arguments` node, so a trailing-lambda-only call still appends the lambda.
**Dart side-fix:** the selector-form call path (`extract_inline_call_from_statement`) hardcoded `call_args: Vec::new()`, dropping ALL args (not just lambdas) for the dominant `obj.method(...)` shape; now wired to `extract_dart_call_args`. Minimal, in-scope (the lambda cannot land otherwise).
**⚠️ Scala — arm landed, end-to-end ceilinged by a pre-existing extractor gap.** The `lambda_expression` arm and `bindings` multi-param path are structurally correct against the verified grammar (single-param confirmed end-to-end). But Scala's `call_expression` arm does not always emit the OUTER Calls ref for `obj.method(lambda)` in expression-body / `;`-sequenced-block positions, so multi-param coverage can't be confirmed e2e for those shapes. This is a pre-existing Scala call-ref emission gap, not part of this slice; fixing it is forbidden here (would be a bespoke per-language call-emission path against the one-engine gate). Deferred — see forks.
**⚠️ Swift — named-param closures only.** `{ x in x.foo }` captures `["x"]`. The anonymous-shorthand `{ $0.foo }` declares no parameter identifier and yields an empty list; seeding `$0` requires an architecture decision on whether `$N` becomes a first-class synthetic local name. Deferred — see forks.

**Deferred forks (documented, not guessed):**
1. **Swift `$0`/`$1` anonymous-shorthand closures** — the closure declares no param identifier; seeding needs either synthesized `$0..$n` names taught to the chain walker as usable local-type keys, or `$0`-reference rewriting. Architecture choice for the architect; not guessed.
2. **JavaScript whole pipeline (Tier C)** — `javascript/calls.rs` has NO arg-extraction at all (every ref is built with `call_args: Vec::new()`). Wiring JS lambdas means a reuse decision (shared `languages/common` arg-extractor vs duplicate-from-TS vs delegate-to-TS) plus threading `call_args` onto ~6 ref-emission sites. Separable; the one-engine gate prefers a shared helper. Deferred to its own slice.
3. **Ruby blocks** (`arr.map { |x| ... }` / `arr.each do |y| ... end`) — the block is a `block`/`do_block` sibling of the `call` node with a `block_parameters (|x|)` child (verified shape), not an `argument_list` member. Like Kotlin trailing-lambdas this needs a second call-site path. Ruby's resolver is sensitive (R-collapse history); deferred pending a careful wiring pass.
4. **Scala outer-ref emission** — see ⚠️ above; pre-existing, fixing it is out of this slice's scope.
**Known extraction ceiling (separate follow-on, NOT this slice):** the end-to-end `arr.map(x => x.foo)` second hop additionally needs the lambda **body's** member access (`x.foo`) emitted as a ref. The TS extractor does not emit a ref for a member access in arrow expression-body position (nor a bare property-access statement) — only assignment-RHS / call / type positions emit member refs today. So the seed lands `x→User` correctly (proven by the `chain_tests.rs` walk gate `lambda_param_seeded_from_callback_signature`), but `x.foo` itself has nothing to resolve until arrow-body member-ref extraction lands. That is a distinct extractor gap, recorded here, not part of contextual callback-param typing.
**Prereq slice1-fn-params ✅:** `TypeArena::intern_type_str` now preserves `Type::Function` parameter types — the arrow branch (`type_checker/core/types.rs`) parses the last top-level parenthesized param group, comma-splits it, and peels each piece's `name:` annotation before interning, instead of hardcoding `params: Vec::new()`. So a callback signature like `(value: T) => U` now carries its param `T` through interning, which is the type the seeding step reads. `[generic]`, one engine impl; `substitute`/`unify_into`/`format_type`/`rebind_class_params` already iterate `Function.params`, no ripple.
**Prereq slice2-ext-callables ✅:** external method/function parameter types are now queryable. `SymbolTypeMap::build_from_parsed_files` (`type_checker/core/symbol_types.rs`) replaced its file-level `ext:` blanket skip with a per-symbol gate (`is_external && is_type_defining(kind)`): external CALLABLE kinds (Method/Function/Constructor) and value-bearing kinds (Field/Property/Variable) now record their already-interned `param_types`/`return_type` into the map, while type-DEFINING externals (Class/Struct/Interface/…) stay skipped to preserve the `arena.class` self-yield write-storm guard. So `SymbolView::param_types(member_id)` returns `Some(&[…])` for an external callable instead of `None` — the data the seeding step reads through `symbol_types.get(member_id)`. `[generic]`, one engine builder; zero new arena writes for callables (they take the `(Some, false)` arm). `MembersIndex` skip (`members.rs`) unchanged.
**Deps:** INFER-8 (generic-arg binding, to know the element type), INFER-6 (container element types).  **Corpus leverage: high.**

### INFER-10 — Generic type-alias / typedef expansion in the shared walker  ·  [generic]  ·  ✅ (`a948e965`)
**Done.** `expand_current_type` (the generic `expand_alias` path in `resolve_via_chain`) was already wired and env-optional (from the C migration); INFER-10 flips `expand_aliases=true` on the configs with type aliases AND a populated `alias_target` — **Rust, Python, Scala** (TS and C were already on). An alias-typed value's `.member` now binds for all six. Pure `ChainExtensions` data; no per-language resolution code.
**Go subtlety (the one extractor change, soundness-corrected in review):** Go alias-target emission is **head-only** and fires **only for true aliases** (`type Foo = Bar`). Defined types (`type Foo Bar`, no `=`) synthesize **no** `AliasTarget` — they don't share the underlying method set, so rewriting them would be wrong; their own methods (qname `Foo.M`) keep resolving against `Foo`. Composite RHS (`= []Item` / `map[K]V` / `func(...)`) emit no head (avoids taking an inner element as the alias target). Two soundness holes (wrong-head on composites; defined-type/alias conflation) were caught and closed before commit.
**Kotlin deferred:** its `typealias` extractor emits no target `TypeRef`, so flipping `expand_aliases` would be a pure no-op — needs a separate scoped extractor change (mirror `scala/symbols.rs:389`).
**Soundness:** expansion fires only for a real alias with a known target; otherwise no-op — widening-only. The C `dereference_typedef` hook was already retired into this generic path during the C migration.

---

## C. Externals as first-class (the biggest *type-aware* unlock)

Re-anchors the routing doc's D2/D4/D5/S5b on the binder-first path. **This is the
#1 corpus-leverage block** — chain-walking through Prisma/ORM/tidyverse/Flutter
return types is gated here, and it is what makes external types ordinary table rows.

### EXT-1 — Scope-directed external routing (D2)  ·  [generic]  ·  ✅
**Compiler feature:** decide external-ness *first* by scope/import, not as the residual.
**Done.** `ChainMiss` gained an optional `module`; at the Tier-1.5 external classification (`loop_body.rs`), an `ext:<module>`-classified ref now records a **module-scoped demand** (`ChainMiss { current_type:"", target_name: <leaf>, module: Some(<module>) }`). `expand::locate_via_symbol_index` resolves a module-scoped miss via `SymbolLocationIndex::locate(module, name)` — the file defining the name *inside that package*, with **no `find_by_name` cross-package fallback** (a miss under the module is a genuine gap, not licence to grep). A re-resolve upgrades the opaque `external_ref` into a real edge. The pull is reachability-bounded for free: `locate` only answers for `(module, name)` pairs the demand-driven index already carries (npm/cargo/go_mod/etc. `build_symbol_index`), so builtin/primitive namespaces and unscanned modules locate to nothing and stay `external_ref`, exactly as before. No env gate. Recovers the import-qualified externals EXT-4 dropped. Verified: ts-rallly +29 binds (no regression); go-fiber rate flat.
**Scope note (intentional):** only the *records-demand* half landed. The *short-circuit-internal-strategies* half is intentionally NOT done — post-QUAL-1 the internal strategies are scope-directed and cannot coincidentally bind an import name, so running them first then classifying external as residual is functionally equivalent AND avoids the lexical-shadowing risk a blind short-circuit would carry (a true inner-scope binding must still win over the import per BIND-1).
**Deps:** QUAL-1 (no grep to pre-empt) ✅

### EXT-2 — Emit external return/field types (D4 / S5)  ·  [generic]+[profile]  ·  ✅ (slices 1+2+3+4 — colon/arrow + module-tagged TypeRef + JVM-descriptor decode `5b9e0ab4` + C aggregate-return peel; R is verify-only by language design)
**Slice 1 done (`1f03bec9`):** `parse_return_type_from_signature` (`chain_walker.rs:591`) now recognizes `-> T` (Python/Rust) and `=> T` (TS-arrow) on top of `): T`, with angle-depth guard, Rust `where`/block stripping, and trailing-`:` handling. External methods using those forms populate the return/field maps through the signature fallback build.rs/augment.rs already run. Covered by `chain_walker_tests.rs` (colon/arrow/generic/where/block) + end-to-end `mod_tests.rs::signature_derived_return_type_arrow_form` (Python `-> User` external → `return_type_name` via `build_with_context`).
**Slice 2 done (`c0ec6ce2`):** the `r.module.is_some()` TypeRef filter (`build.rs:238`, `augment.rs:128`) is gone — it dropped module-tagged external TypeRefs wholesale. Now `ExtractedRef.is_import_binding` (foundation `07e0c6bd`) marks an import STATEMENT's binding ref (set at the three ecmascript `emit_clause_refs` TypeRef sites — the only place bindings are TypeRefs; every other language emits them as `Imports`, already kind-gated), and the filter excludes only `is_import_binding`. Module-tagged USAGE TypeRefs (a field/param/return typed as an imported symbol) now feed `field_type`/`return_type`. Guard test `module_tagged_usage_typeref_feeds_field_type_binding_excluded` (binding placed first still yields the usage's type); 6,269 lib tests, 0 cross-language regression.
**Slice 3 done (`5b9e0ab4`):** the real remaining hole was the JVM walker writing raw bytecode descriptors (method `(Ljava/lang/String;)Lcom/foo/Bar;`, field `Lcom/foo/Bar;`) that no return-type scan could read, so Maven/JVM externals carried signatures but yielded zero return/field types. `parse_return_type_from_jvm_descriptor` (`chain_walker.rs`) decodes method/field/array descriptors (array → ELEMENT type, so the chain types through the element like `List<T>`) and is wired as a JVM-gated `.or_else()` rung in both `build.rs` and `augment.rs`. The earlier "measurable only at closeout recapture" claim was wrong — coverage is single-project-verifiable via the QUAL-5 `external_known_unhydrated` bucket.
**Slice 4 done (C aggregate-return peel):** the non-JVM cross-ecosystem remainder split into one verify-only side (R) and one narrow generic fix (C). C externals already hydrate via the leading-form `parse_return_type_positional` rung (`CURL curl_easy_init(void)` → `CURL`), but a function returning `struct Foo`/`enum Bar`/`union Baz` produced a signature whose first depth-0 token is the aggregate keyword, which `is_decl_keyword_or_modifier` rejected — dropping precisely the chainable libcurl-style returns. `parse_return_type_positional` (`chain_walker.rs`) now peels a leading `struct`/`enum`/`union` keyword (via `is_c_aggregate_keyword`) and reads the next token as the type, falling through to the existing reject when no following type token exists (a bare `struct` forward-decl mints no phantom return). Pure string logic, no language-id branch — the peel is harmless for any language that never emits a `struct `-led return. Covered by `chain_walker_tests.rs` (positive `struct`/`enum`/`union` cases + bare-keyword negatives) + end-to-end `mod_tests.rs::c_external_struct_return_hydrates`. **R is a verified ceiling, not a gap:** R is dynamically typed with no return-type annotation syntax — `r_lang/extract.rs` emits `name <- function(params)` signatures that carry no `): T` / `-> T` / `=> T` form, and the CRAN locator (`cran.rs` `walk_r_root`) captures only top-level NAMES from each package's NAMESPACE file, never signatures or types. There is nothing to parse, so the R second-hop (`mutate()`/`ggplot()` member chains) is an extraction/language-semantics ceiling recorded for DOC-4, not a resolver task; emitting an R return type would be a synthetic fake.
**Compiler feature:** `repo.find().email` walks past the first hop because the external method's return type is known.
**Gap (re-scoped after tracing the pipeline — most of the infrastructure already exists):**
- TypeRef-derived return/field types: `build.rs:223-391` (full) + `augment.rs:121-209` (incremental/hydrated). The demand loop (`full.rs:743-790`) **does** augment the cached index with hydrated files via `augment.rs` before re-resolving, so hydrated externals *do* flow through the map-build. The earlier "demand-hydrated files don't enter the maps" claim was wrong.
- Signature-based return-type fallback already runs in **both** paths for *any* method lacking a TypeRef return type (`build.rs:362-374`, `augment.rs:185-200`) — not just .NET, despite the comments.
- **The real remaining hole is `parse_return_type_from_signature` (`chain_walker.rs:591`): it only recognizes the `): T` colon form** (TS methods, Kotlin `fun`). It does **not** handle Python/Rust `-> T` or TS-arrow `=> T`, so externals whose signature uses those forms get no return type. Secondary: external symbols must actually *carry* a signature for the fallback to fire, and the `r.module.is_some()` TypeRef filter (`build.rs:238`, `augment.rs:128`) drops module-tagged external TypeRefs (signature fallback sidesteps this).
**Fix (bounded first slice):** extend `parse_return_type_from_signature` to the `-> T` and `=> T` return forms (careful: load-bearing across all languages, gate behind thorough sibling tests + regression cases for the existing colon/generic forms). Then verify external method symbols carry signatures. Failing test first: `PrismaClient.findUnique().email` resolves the second hop; a Python `def f() -> User` external second hop.
**Deps:** EXT-3
**Corpus proof:** `PrismaClient.findUnique/findMany`, R `mutate/ggplot`, C `curl_easy_*`.

### EXT-3 — Lazy member hydration, origin-blind (D3/S4)  ·  [generic]  ·  ✅
**Done.** `build_explicit` (supertype.rs) no longer skips `ext:` files, so a pulled external base's OWN `Inherits`/`Implements` edges enter the `SupertypeGraph`. `walk_up_with_args` then climbs the external hierarchy and composes generic args across those hops; the existing `members.lookup_with_binding` → `env.bind_positional(owner_args)` → `yield_type_of` path (chain.rs:432-475) substitutes the bound args into an inherited member's return type — so a method on a deep external base resolves with the concrete type, not an unbound `T`. The generic-substitution machinery already existed; the only blocker was the missing external edges. Reachability-bounded for free: the graph is built from `parsed`, which holds only the externals the demand loop pulled (post-EXT-4), never the whole dep tree. **Verified:** new `build_explicit_includes_pulled_external_base_edges` test (failing→green) + 317 type_checker tests green; ts-rallly / java-spring-petclinic / ts-nestjs-realworld regression-free. Corpus gain is shape-dependent (OO inheritance from pulled external bases) — measurable at closeout, not on the functional/ceiling/sources-absent samples here.
**Earlier state (for reference):** `qualified_member_lookup` already resolved external *members* origin-blind (the `members.rs:68` `ext:` skip was never the blocker); the supertype-edge gap above was.

### EXT-4 — Delete the eager seed (D5/S6)  ·  [generic]  ·  ✅
**Done.** Deleted `seed_demand_from_user_refs` + `_inner` + `enqueue_named_target` + `follow_inheritance_closure` (`stage_link.rs`, ~370 lines) and its `full.rs` call. Externals now load lazily through the Stage-2 chain-miss → `expand` → re-resolve loop only — no eager pre-pull of every import-qualified external up front (that was the dominant reindex cost on dep-heavy projects). **Tradeoff (honest floor):** import-qualified externals on demand-driven ecosystems (npm/TS) that don't surface a chain miss may stay unresolved until **EXT-1** records that demand on the scoped path; chained externals are already covered by the expand loop.
**Deps:** EXT-1 (to recover the bare import-qualified external coverage)

### EXT-5 — Ambient framework globals via real packages  ·  [profile]  ·  ⚠️ (TS/JS ✅; JVM/PHP gated on EXT-2)
**Compiler feature:** `expect`/`it`/`describe`/`assertTrue`/`hasSize` resolve to the test framework's declared types.
**Scoping correction (2026-06-02 proposal):** the **TS/JS half is already done** via real-package hydration exactly as required: the npm Ecosystem retains globals-declaring test frameworks even when never imported (`externals.rs` `any_install_declares_globals`), `scan_declare_global_blocks` extracts `expect`/`describe`/`it` from `declare global` blocks, `backfill_declare_global_symbols` registers them under `__npm_globals__.X`, and three strategies bind them (`ts_npm_globals`, `resolve_via_ambient_package`+`is_ambient_path`, ambient markers). **The JVM/PHP half is NOT an ambient-globals problem** — junit/scalatest/mockk resolve through static **wildcard imports** (`import static org.junit.Assert.*`), and those binders already exist (`java_wildcard_import`, `scala_wildcard_import`/`scala_implicit_import`). Whether they bind is purely a function of Maven **hydrating** the framework signatures = **EXT-2** (⚠️) + EXT-3 (✅). **Net-new EXT-5 code = 0**; do NOT add a JVM "ambient globals" concept (it would duplicate the wildcard binders — a fork the gate forbids). Verify at closeout once EXT-2 hydration lands.
**Deps:** EXT-2 (⚠️ slice-1 only — the real gate) / EXT-3 (✅)

---

## D. Codegen / synthetic-symbol synthesis  ·  the third symbol source

The symbol table has three sources of declarations: parsed project source, decoded
dependencies (§C), and **symbols a generator/macro/preprocessor emits that never
appear in parsed text.** A compiler resolves against post-expansion symbols; today
this is N hardcoded one-offs with **no generic surface.**

### CODEGEN-1 — Generic post-extract symbol synthesis  ·  [hook]  ·  ⚠️ (seam + Java Lombok + Kotlin data class + Rust derives + C# positional records done; generic method-return element typing done across colon/arrow `3a8491df`, leading-form Java/C# `a18c704d`, Go `[]` + C# head + externals `0481fec0`; arbitrary ISourceGenerator output remains out of scope)
**Compiler feature:** a per-language recognizer emits synthetic declared symbols (and their member/return types) into the same table, so refs to generated members bind like any other symbol.
**Slice 1 done — the seam + first recognizer (`4f2dae31`).** The seam is `LanguagePlugin::synthesize_symbols(source, symbols, refs) -> Vec<ExtractedSymbol>` (no-op default, mirrors `embedded_regions`), invoked in `parse_file` after `filter_local_refs` and before the embedded splice — at EXTRACT time, so synthesized symbols get FK-safe ids (reconciling the roadmap's "LanguageEngineHooks" wording with `engine.rs:99-104`, which reserves the resolve-time hook for no-DB-id signals; the seam went on `LanguagePlugin`). Returned symbols carry `qualified_name` + `scope_path`; member parenting is rebuilt from `qualified_name` so `parent_index` stays `None`. First recognizer: **Java Lombok** (`4f2dae31` getters/setters, `bb138d66` `@Builder`) — `@Data`/`@Value`/`@Getter`/`@Setter` synthesize `getX`/`setX` (`isX` for a primitive boolean) per field; `@Builder` synthesizes `builder()` + the nested `{Class}Builder` class + a fluent setter per field + `build()`. Read purely from extracted class/field symbols + annotation refs (no re-parse; the field type rides in its `signature`); a hand-written member of the same qname wins. Type-level only (field-level `@Getter` is ambiguous at the ref level — a field-level annotation and a field whose type is named `Value`/`Getter` are both a field-sourced `TypeRef`; the discriminator is that an annotation ref's `byte_offset` points at the `@`). 20 Java tests incl. two e2e binds.
**Chain-typing of synthesized members — ✅ `fdf8d952`.** `synthesize_symbols` now returns `Synthesized { symbols, refs }`; each synthesized method emits a return-type `TypeRef` (getter→field-type head, `builder()`/fluent setter→builder qname, `build()`→class qname; void setters + primitive returns emit none), and `parse_file` rebases their `source_symbol_index` on splice. So a synthesized method types like a real one (the index builder reads a method's last `TypeRef`) and `user.getName().x()` + `User.builder().name(y).build()` resolve end to end (index-level proof: `return_type_name` populates `builder()→User.UserBuilder`, `build()→User`, etc.). **Generic method-return element typing — ✅ for colon/arrow-form languages (`3a8491df`).** A method-call segment now binds the generic element args of its return type, so `getRepo(): Repository<User>` then `.findOne()` types through to `User` (`Repository<T>{ findOne(): T }`). Capture: `build.rs`'s method arm treats a parseable signature as authoritative ONLY when it yields a clean generic application (`parse_type_head_and_args` head is a bare/dotted name) — head→`return_type`, args→the method qname's `type_args` slot; otherwise it falls back byte-for-byte to the prior last-`TypeRef` head, so the forms this parser does not read see zero change. Consume: `resolve_and_enter_generics` dropped its `is_field` gate and enters the generic context from `field_type_args` for both field and method yields. Covers TS/Kotlin/Rust real generic returns + the Kotlin synth below; a structural-type guard keeps tuple/union/function returns (`[A, B<C>]`) out of the generic branch. **Leading-form Java/C# + Lombok — ✅ Part B (`a18c704d`).** `parse_return_type_positional` (a build.rs-only `.or_else` fallback after `parse_return_type_from_signature`, non-constructors) reads the first depth-0 signature token as the return type — our signature builders omit modifiers, so for `RetType name(params)` the first token IS the return type (`List<User> getItems()` → `List<User>`; generic method `T <T>get()` → `T`). Guarded against param-lists, `void`, and decl-keyword/modifier/aggregate-keyword first tokens (`func`/`static`/`struct`/…), so it fails safe to the prior `last()` head and never fires for params-first languages (their explicit returns are caught by the colon/arrow parse first; their unannotated methods are keyword- or paren-led). The shared `parse_return_type_from_signature` is unchanged (3 callers; its leading-form-None contract is asserted by existing tests). The prerequisite split landed too: `TypeInfo.return_type_args` (+ `return_type_arg_ids`, interned/round-tripped like `type_args`) is a distinct slot from field `type_args`, and `resolve_and_enter_generics` regained `is_field` to read the right one — so a same-name field+method generic pair (legal in Java) no longer collides. The Lombok generic getter binds its element via its leading-form synth signature. **Go `[]` + C# head + externals — ✅ (`0481fec0`).** `parse_return_type_trailing` reads Go's post-`)` return; `parse_type_head_and_args_bracket` splits `[]`-generics (`List[User]`) for go/scala — only with a non-empty head and nothing trailing `]`, so a slice `[]User`, tuple `[A,B]`, or compound `map[K]V` is left to `last()`. The else-branch now prefers a plain signature return (`is_plain_type_name`) over `last()`, fixing the C# return-first-ref-list case where `last()` lands on the last param (safe because sig text and the return TypeRef share the same raw AST node and both pass through `resolve_type_name_in_scope`). `augment.rs` (externals) mirrors the full capture, so external library method returns bind their element through project→external chains. `parse_type_head_and_args` refactored onto a shared `split_application`. **Residual (niche):** Go func-typed returns (`func() error`) and `map[K]V` element typing; Scala by-name/structural types. **Kotlin `data class` — ✅ (`3a8491df`).** Second recognizer on the seam: `copy()` (returns the class → chains type through), `componentN()` per `val`/`var` primary-ctor property, `equals`/`hashCode`/`toString`; colon-form synth signatures flow through the capture path above; detection scans the class node span for `data` before `class` (covers annotated classes), property types read column-anchored so a name that is a prefix of an earlier one types correctly. **Other follow-ons:** `@*Constructor`; C# source-gen / records; then OPTIONAL consolidation — migrate the inline Python `@dataclass` / Rust derive / C `#define` one-offs onto the seam.
**Gap:** there is **no synthesis hook** on `LanguagePlugin` (only `extract` + `embedded_regions`). Every codegen path is hardcoded inside a language's `extract()` or an on-disk walker: Python `@dataclass → synthesize_dataclass_init` (`python/symbols.rs:684`), Svelte `$store` desugar (`svelte/hooks.rs:40`), C/C++ `#define` expansion (`c_lang/macro_catalog.rs` + `salvage_macro_expanded_decls`). These prove the shape; nothing generalizes it.
**Reference implementation (present, untracked):** C/C++ `#define` expansion is the most mature — `macro_catalog.rs` reads the project's own macro bodies from sibling/parent headers, honours token-paste `##` and stringify `#`, and `salvage_macro_expanded_decls` re-runs the declaration scanners over the substituted body (`c_lang/extract.rs:158`). The recognizer + re-scan shape is exactly what generalizes.
**Fix:** a `LanguageEngineHooks` method `synthesize_symbols(&self, file) -> Vec<ExtractedSymbol>` invoked post-extract, fed by per-language recognizers. Case space (all currently unresolved, all deterministically synthesizable **without running the real generator**):
- **Java/Kotlin Lombok** — `@Data`/`@Getter`/`@Builder` → `getX()`/`setX()`/`builder()` from field declarations. High frequency in real Java corpora; **no Java codegen path exists today.**
- **Rust derive / proc-macro — ✅ common derives.** Third recognizer on the seam (`rust_lang/derives.rs`): `#[derive(Default/Clone/From/Debug/PartialEq/PartialOrd/Ord/Hash/Into/AsRef/AsMut/Serialize/Deserialize/Error/Display)]` synthesize the methods those traits generate, read from the derive `TypeRef`s the extractor already emits at the struct/enum symbol (no re-parse). Self-returning methods (`default`/`clone`/`from`) carry a return-type `TypeRef` to the owning type so `Config::default().name` chains through; non-Self returns (`fmt`/`eq`/`hash`/`cmp`/…) emit none. A hand-written member of the same qname wins. Replaces the prior inline extract-time synth that emitted no refs (so chains did not type through). `bitflags!`/`prost`/`sqlx` proc-macro accessors remain out (not a fixed trait→method map).
- **C# source generators — ✅ positional records (bounded cut).** Fourth recognizer on the seam (`csharp/source_gen.rs`): a positional record `record Point(int X, int Y)` synthesizes the compiler-generated `Deconstruct(out int X, out int Y)`. A record is extracted as `SymbolKind::Class` with a `"class {Name}"` signature — indistinguishable from a real class by kind or signature, so the discriminator is source text (the `record` keyword as a whole word before the type name, mirroring Kotlin `is_data_class`). The positional properties are already emitted by the extractor as `Property` with `signature: "{type} {name}"`; the recognizer reads only those whose source position falls inside the record's positional parameter list (the body properties of a `record Person { ... }` share kind + scope_path but sit after the `{`, so they are excluded). Deconstruct returns void → no return-type `TypeRef`; a hand-written `Deconstruct` of the same qname wins. The chain `dto.Category.Id` types through with no recognizer work — it rides the positional property's signature the extractor already produces. **Deferred forks (architect decision; not silently picked):** (1) the `with` expression result type is `with_expression` SYNTAX, not a `.With()` method call — synthesizing one would be dead surface; typing `a with {}` as the receiver's type is a flow-typing concern, not symbol synthesis. (2) A derived positional record's Deconstruct excludes the base record's members (a real C# rule), requiring an inherits_map walk — deferred until a corpus case demands it. (3) `[GeneratedRegex]` has no synthesizable surface — the user-callable partial-method declaration (`static partial Regex MyRegex()`) is already extracted with its `Regex` return type; only the generated body (not a symbol) is emitted by the generator. (4) Arbitrary `ISourceGenerator` output (MediatR, AutoMapper, Mapperly, source-gen'd DI, `[JsonSerializable]` contexts) has no deterministic in-memory analog to the fixed record→Deconstruct map — it requires running the actual generator and is out of scope, the same way `bitflags!`/`prost`/`sqlx` proc-macros are excluded from the Rust derive recognizer.
- **Generalizes** the existing Python/Svelte/C one-offs onto the shared hook.
**Cleanup (no-env-gate rule) — ✅.** `cargo_expand_runtime.rs` (which gated expansion behind `BEARWISDOM_CARGO_EXPAND` and shelled `cargo expand`) is deleted: it violated the locators-only + no-machine-driven-activation ecosystem contract, and the common-derive recognizer above covers its deterministic purpose without invoking the compiler.
**Deps:** —  **Corpus leverage: high** — Lombok getters, derive impls, source-gen members are a large unresolved slice in Java/Rust/C#.

---

## E. Language-specific resolution rules

Per-language name/method-resolution a compiler hard-codes — expressed as **data on
the shared engine**, per the architecture gate. **A `LANG-*` task that adds a new
per-language walker is a gate violation; it must reduce to engine data + the
irreducible hook.** Ranked by corpus volume.

### LANG-RUST-1 — Trait method resolution  ·  [generic]+[profile data]  ·  ⚠️ (trait-default + single-inner-wrapper receiver typing fully landed incl. INDEX-path; blanket / assoc-type / user-Deref forks deferred)
The roadmap's blanket "`.method()` is name-only" was too broad. Two sub-cases of `c.foo()` where `c: ConcreteType C` and `foo` comes from `impl Trait for C`:

- **CASE A — method provided IN the impl body** (`impl Trait for C { fn foo(){...} }`, and all inherent-impl methods). ✅ Already resolved. `calls.rs` sets `impl_prefix = C`, so `foo` files with `scope_path = "C"` → `MembersIndex` keys it as a direct member of `class("C")`; the generic chain walker types the receiver and `MembersIndex.lookup` finds it directly. High-volume case, never the gap.
- **CASE B — trait DEFAULT method inherited through an empty/partial impl** (`trait T { fn foo(){..default..} }` + `impl T for C {}`). ❌ Was the real gap. `foo`'s `scope_path` is the trait (`"T"`), so it's a direct member of `class("T")`, not `class("C")`. Resolving `c.foo()` needs the supertype walk `C → T`. But `extract_impl` emits the `Implements` edge from the `<impl C@N>` Namespace container symbol, so `build_explicit` keyed the edge on `class("<impl C@N>")` — a dead node nothing is ever typed to. `C` never gained the `→ T` edge and the default method was unreachable.

**Bounded cut (landed, `[generic]`, no hook, no new walker):** `build_explicit` (`type_checker/core/supertype.rs`) now reroutes an inheritance edge whose source symbol is an impl-container to the IMPLEMENTING type. A `Namespace` can't be a subtype, so any `Inherits`/`Implements` edge sourced from one is structurally an impl/extension container; its real child is the implementing type. That type is read from the container's **self-`TypeRef` edge** — the impl-container symbol already emits a `TypeRef` to its implementing type (`calls.rs:90`, `target_name` = the raw implementing-type node text `C` / `C<T>`). `impl_container_child_qname` finds the sibling `TypeRef` with the same `source_symbol_index`, decomposes its target through the arena's existing `Apply { base, args }` decode (`type_base_qname`) to the bare base, and resolves it via `types_by_name`. Absent that TypeRef (no marker), the child keys on the source's own qname — every other language's classes/traits are byte-identical to before. After the reroute `class("C") --Implements--> class("T")` exists and the EXISTING generic `find_on_chain` / `linearize_with_args` walk lands on `T` and binds the default method. Reuse-only on the resolution side: confidence, substitution, inheritance-walk, `MembersIndex.lookup` are all unchanged.

**Data-carrier fork — resolved to the existing structural TypeRef (no signature parsing in the engine, no new ref field).** The spec enumerated two ways for the generic engine to learn the implementing type: (1) a new opaque `ExtractedRef` field the Rust extractor stamps and the engine reads blindly, vs (2) the engine parsing the container's `impl <Type>` signature string. Option (1) as a *new field* would churn ~800 `ExtractedRef` construction sites (no `Default`); option (2) leaks Rust `impl`-syntax shape into shared code AND its angle-bracket string scan mishandled bounded-generic impls (`impl<T: Display> Wrapper<T>` — a first-`>` split stopped inside the bound). Landed instead via the **already-emitted self-`TypeRef`**: it is the opaque carrier option (1) wanted (the engine reads `target_name`, never interprets `impl`), with zero new field and zero construction-site churn, and the generic `Apply`-decode handles nested/bounded generics structurally. The earlier `strip_prefix("impl")` + `split_once('>')` signature parse is removed.

**Fork #1 — Autoref/autoderef receiver typing (bounded sound cut LANDED, `[generic]`+`[profile data]`, no hook, no walker).** `c.foo()` where `c: &C` / `&mut C` / `&'a C` / `Box<C>` / `Rc<C>` / `Arc<C>` now types the receiver as the inner `C` before the existing member + trait-default walk runs. Two generic-engine changes plus one profile-data axis, mirroring the documented single-inner-wrapper / Optional-peel precedent:
- **Reference-sigil strip `[generic]`** (`types.rs::intern_type_str` + `strip_reference_sigil`). A LEADING `&` (then an optional lifetime `'a` and `mut`) is consumed and the referent re-interned: `&C`→C, `&mut C`→C, `&'a C`→C, `&Box<C>`→Box<C>. A `&`-led string is never a valid class name in any language, so this is universally sound; only byte 0 is treated as a sigil, so a TS `A & B` intersection (interior `&`) is untouched and stays on the union/intersection fallback. Fixes the formerly-dead `Class("&C")` interning for the segment-`declared_type` path.
- **Smart-pointer peel `[generic]` + `[profile data]`** (`chain.rs::peel_receiver_wrappers`, invoked at the chain root alongside `expand_aliases`/`narrow_union_by_discriminant`). A `Type::Apply { base, args }` with one arg whose `base` simple-name is in the new `LanguageProfile::single_inner_wrappers` axis is replaced by `args[0]`, looping to fixpoint (so `&Box<C>` → strip `&` at intern, then peel Box → C). Box/Rc/Arc each `Deref` to their single inner, so the `args[0]` projection IS the one Deref hop; the existing `find_on_chain`/`linearize_with_args`/`MembersIndex.lookup` + the CASE B trait-default reroute then bind `c.foo()` against `C` for free. The wrapper HEADS are per-language DATA (Rust = `["Box","Rc","Arc"]`); the peel is the structural `args[0]` projection — never a method→type table. `Vec`/`HashMap` are deliberately ABSENT (a negative test proves `Vec<C>.method` stays on `Vec`), so the peel is allowlist-gated, not a blanket arity-1-Apply peel. Default `&[]` is inert for the other 93 languages (the 94-literal sweep + `..DEFAULT_PROFILE` test profiles are byte-identical).

  *INDEX-path root interning — ✅ LANDED (`[generic]`, no hook, no walker).* The staged follow-on closed: `DefaultRootResolver`'s two `arena.class(&type_name)` receiver sites — the `field_type_str` member path and the `field_type_name` scope-walk path (`chain.rs`) — now route through `arena.intern_type_str`. A receiver whose declared/field type is a generic application (`Box<C>`, `Repository<User>`) decomposes into `Apply { base, args }` at the root, so `bind_apply_args` composes its args and `peel_receiver_wrappers` strips a single-inner wrapper — the end-to-end INDEX path now resolves `b.method()` for a `b: Box<C>` field/local, not only a `FixedRoot`-typed unit fixture. Soundness boundary: `intern_type_str` decomposes ONLY a balanced `Head<…>`/`Head[…]` application; a plain name interns byte-identically to `arena.class`, and any malformed string falls back to `Class(input)` — so a previously-flat `Class("Repository<User>")` (which no `MembersIndex` key could ever match) can only turn a prior miss into a structural hit, never a false bind. The generic-param branch (a bare param name → canonical `Type::Generic`) short-circuits before the intern, so `t: T` still binds via its upper bound. Failing-first test `box_field_receiver_resolves_inner_via_default_root` drives the real `DefaultRootResolver` scope-walk (not a `FixedRoot`); full lib + resolution-gate + all externals suites green.

  *Deeper Deref sub-cases (documented, not covered):* user-defined `Deref` impls beyond the std set (`impl Deref for MyWrapper { type Target = Inner }`) need a `Deref::Target` lookup over the impl graph — an associated-type projection, same family as fork #4. Method-resolution autoref ORDERING (`&self` vs `&mut self` vs `self` candidate ranking among same-named receivers) is not modelled — BearWisdom is over-resolve (BIND-3) and binds the named member, out of a navigation tool's scope. `Pin`/`Cow`/`MutexGuard`/`RefCell`/`Cell` are additional single-inner std wrappers but `RefCell`/`Cell`/`Mutex` expose the GUARD's member set via `.borrow()`/`.lock()`, not the inner's — adding them to the peel would be UNSOUND, so only the pure-Deref Box/Rc/Arc set ships; the rest is a one-line DATA edit when corpus evidence appears. Generic composition through the wrapper (`Box<C<T>>`) is COVERED — the peel yields `C<T>`'s `Apply` intact so `bind_apply_args` still composes `T`.

**Deferred forks (architect decision; not silently picked):**
2. **Blanket impls** (`impl<U: Bound> Trait for U`) attach trait methods to EVERY type satisfying `Bound`, not a named concrete type. Wiring them needs bound-driven membership over the whole type space (a different graph-population strategy than a concrete `C → Trait` edge) — deferred.
3. **Associated-type projection** (`Self::Output`, `<C as Trait>::Item`) — resolving a method's return/assoc type through the impl's `type Output = …` binding is type-inference work beyond locating the method symbol — deferred. Generic-param composition for `impl<T> Wrapper<T>` rides the same fork (the reroute attaches the bare base `Wrapper`; the impl's `<T>` binding through to the trait is not composed).
4. **External trait default methods** (`impl Display for C` where `Display` is a dependency-crate trait). The `C → Trait` edge still forms, but the trait's default-method body lives in an `ext:` file that `MembersIndex` skips, so the member stays out of the set — gated on external member hydration (same gating as LANG-GO-1's positive edge), not on this cut.

### LANG-CPP-1 — ADL (+ overload via BIND-4; template-param binding ✅)  ·  [profile]+[generic]  ·  ⚠️ (bounded sound ADL cut landed; widening forks deferred)
**Template-parameter binding — ✅ (was mis-marked ❌).** `templates.rs:84-93` folds template param names into the signature (`<T,charT>`) feeding the generic-param strategy; `default_resolver.rs:526` resolves self-declared params as `engine_generic_param` (passing test `default_resolver_tests.rs:1062` resolves `OutputIt`). `T1`/`T2`/`OutputIt`/`iterator_t`/`charT` are no longer the gap.
**Overload resolution** — owned by the generic BIND-4 (`select_bare_overload` + `arg_assignable_candidates`), not here. ADL only FINDS the candidate set; BIND-4 PICKS within it.
**ADL — ✅ bounded sound cut (was the only true C++-specific remainder; landed as `[generic]`+`[profile]`, no hook).** `Engine::resolve_via_adl` (`type_checker/engine.rs`) is a strict fallback after the structural ladder declines a bare `Calls`/`Instantiates` call: it types each argument via `resolve_arg_types`, takes each argument-type qname's declaring-namespace prefix (`rsplit_once('.')`), probes `{ns}.{target}` for kind-compatible callables, and binds the unique survivor (single candidate directly @1.0; several → BIND-4 arity/type filter, unique-or-decline). The C++-ness is entirely the opt-in `argument_dependent_lookup: bool` profile axis (true only in `C_LANG_PROFILE`); the mechanism is shared engine code any `.`-qualified-namespace language could enable. Covers the canonical `swap(a,b)` / `begin(c)` / `process(w)` shape where the function lives in the same namespace as a typeable argument.

**Deferred forks (architect decision; not silently picked):**
1. **Associated-namespace widening.** Real ADL also pulls the namespaces of an argument type's BASE classes, its template type-args, and its enclosing namespaces. The cut does the argument-type's-own declaring namespace only. Climbing bases (via `SupertypeGraph`/`inherits_map`) and unwrapping `Apply` type-args is a reach-vs-false-namespace-probe tradeoff — deferred.
2. **Operator ADL (`a + b`, `os << x`).** A large real-world ADL use, but BearWisdom does not emit operator calls as resolvable `Calls` refs with a function target. Whether to extend the C++ extractor to emit operator-call refs is a separate extractor decision, out of this cut.
3. **Precedence vs a future C++ ambient/hook rung.** The cut is fixed as "after the structural ladder, before `resolve_bare_post`". If a C++ ambient-globals or hook post-pass is later added, ADL's order relative to it is a precedence call — flagged so it isn't silently reshuffled.
4. **Confidence of a unique-by-namespace hit.** Shipped @1.0 (the namespace match IS structural evidence, matching the rest of the engine's structural binds). Down-weighting ADL edges to distinguish them is a deferred confidence decision.

The C/C++ `#define` leg of CODEGEN-1 also lands under this language.

### LANG-GO-1 — Implicit interface satisfaction + embedded promotion  ·  [generic]  ·  ⚠️ (sound structural cut landed; positive edge gated on Go method type-data)
- **Embedded promotion** — ✅ chained `t.BaseMethod()` now resolves. Two fixes were needed (the earlier "inherits_map populated" claim was wrong for full builds): both `inherits_map` child-kind filters excluded `Struct` (only Class/Interface/Trait), so Go struct embedding never populated the map — `build.rs` and `augment.rs` now include `Struct`; and `walk_go_chain` Phase 3 now climbs via `find_member_via_inheritance` (strategy `go_chain_inheritance`, conf 0.90). Bare-name promoted refs from inside the struct already resolved via `resolve_via_enclosing_member`.
- **Implicit interface satisfaction** — ⚠️ The structural pass already existed and is live: `SupertypeGraph::build_structural` (`type_checker/core/supertype.rs`) creates `concrete-T → interface-I` edges for `SupertypeDiscovery::Structural`/`Both` languages, and `find_on_chain` walks them, so both directions (I-typed call resolves / concrete T accepted where I expected) ride the same edge. The remaining gap was SOUNDNESS: `build_structural` matched on member NAME+KIND only, so two structs with a same-named-but-differently-typed method both falsely satisfied an interface. **Fixed generically:** `build_structural` now routes each `(candidate, iface)` pair through INFER-5's sound `is_assignable_to_typed_with` (`subtype.rs`) and adds the edge ONLY on `SubtypeResult::Yes` (member name + kind + assignable param/return in correct variance). One generic-engine substitution of a sound check for an unsound one — no Go code, no new walker — shared by every `Structural`/`Both` language (Lua/Zig/OCaml/Clojure/Nix/TS inherit the soundness upgrade). Internal-only is automatic: `MembersIndex` skips `ext:` files, so an external interface enumerates to empty → `Unknown` → no edge.
  - **Why ⚠️, not ✅:** the sound check returns `Unknown` (no edge) when a matched method lacks recorded `SymbolTypeData`. The Go extractor records the method signature as a STRING but does not yet intern param/return TypeIds onto `ExtractedSymbol` (`languages/go/symbols.rs`/`types.rs` set `return_type: None`, `param_types: Vec::new()`). So Go structural edges form only once the extractor hydrates method types. The live member-resolution direction (a call to a struct method resolved on the struct's own body) is unaffected — only the structural supertype edge is gated. Re-enabling the positive Go edge is extractor work (intern method param/return TypeIds), not engine work.
**Deferred forks (architect decision required for full ✅):**
  1. **Tighten-vs-widen on absent type-data** — chosen **strict** (Unknown = no edge), the sound ship. The hybrid "fall back to name+kind when type data absent" partially re-admits the unsound path and is rejected. This is a corpus-affecting change (Go internal-resolution numbers move down until extractor type-data coverage catches up); a corpus recapture is owed at closeout, not now (no-mid-refactor-recapture rule).
  2. **Extend to `SupertypeDiscovery::Both` (TypeScript)** — DONE in the same generic change (the routing is shared); TS structural typing now uses the sound check too. Flagged here because it changes TS structural-satisfaction behavior; the soundness direction is strictly safer (no false edges), so it ships with Go.
  3. **Variance vs exact-match for Go** — INFER-5 is variance-aware (method-param contravariance + return covariance); Go interface satisfaction requires exact signature identity. The assignable check is SOUND-but-loose for Go (it may accept a covariant-return method Go would reject) but never produces a FALSE edge that breaks resolution — it only over-links in rare covariant cases. Accepted as the bounded sound ship. An exact-match profile axis for Go is a future `[profile]` bool if the architect wants strict Go exactness.
**Deps:** INFER-5 (✅ — wired into `build_structural` by this cut; positive Go edge now gated on Go method-type extraction, not INFER-5).

### LANG-KOTLIN-1 — Extension functions + smart-casts  ·  [profile]+[hook]  ·  ✅ (extension fns; smart-casts deferred)
`x.ext()` resolved by receiver type to a top-level/member extension fn; smart-casts (roadmap L2 deferred — grammar-version-dependent); companion objects.
**Done (`daddefe7`).** No new code — the extractor folds the extension receiver into the signature as a leading `(this <Recv>` parameter (positional detection; kotlin-ng has no receiver field), and the profile flips `extension_method_fallback: true` so the **existing** generic `signature_is_extension_on` fallback (the one C# already drives) binds `s.shout()` → `fun String.shout()` by receiver. Scope: identifier/field/declared-type receivers. Not modelled: literal receivers (`"hi".shout()` — `build_chain_inner` has no string-literal root), generic-receiver extensions (`fun <T> T.x()`). Smart-casts untouched. Member-extension over-resolve is the same last-resort tradeoff C# accepts.
**Scoped (the earlier why-it's-hard note, for reference):** for `fun String.shout()` the extractor emits the receiver `String` as a **TypeRef** (`kotlin/coverage_tests.rs:464`), and the symbol qname is just `shout` — so Phase 3's `current_type.last` lookup misses. Unlike C# (where `signature_is_extension_on` reads `this <recv>` from the signature inside the dedicated `walk_csharp_chain`), Kotlin (a) routes through the **shared** `chain::resolve_via_chain` and (b) has no signature marker — the receiver is a position-dependent TypeRef indistinguishable from a first-param type. Clean fix needs extractor-level receiver tagging (a dedicated field / `extension_receiver` map) + a config-gated Phase-3 fallback in the shared walker (reusable for Scala). Multi-part.

### LANG-CSHARP-1 — Extension-method search across usings  ·  [hook]  ·  ⚠️
**Extension methods** — ✅ `walk_csharp_chain` Phase 3 now, after instance-member lookups miss, searches `by_name(method)` for a static method whose signature's first parameter is `this <current_type>` (`signature_is_extension_on`, strategy `csharp_extension_method`, conf 0.85). The `this`-param is read straight from the stored signature text — no extract-time tag needed. Visibility-blind (no `using`-scope gate), consistent with the over-resolve model (BIND-3). **Partial classes** — ✅ work incidentally: `members_by_parent` accumulates members under the shared parent qname across files (`build.rs:153`). **Source-generated members** — ❌ → this is **CODEGEN-1**, not a C#-specific fix; do not build a bespoke C# walker for it.

### LANG-PY-1 — MRO  ·  [profile]+[generic]  ·  ✅ (C3 linearization; inconsistent-MRO + external-leaf forks deferred)
Method resolution order for multiple inheritance. (Dynamic `__getattr__`/duck typing out of scope.)
**Landed as the scoped [profile]+[generic] cut.** One `LanguageProfile` axis `ancestor_order: AncestorOrder { Bfs (default), C3 }` + one generic `SupertypeGraph::linearize_with_args(start, arena, order)`: the `Bfs` arm delegates to `walk_up_with_args` byte-for-byte (every Bfs/default language unchanged), the `C3` arm computes C3 linearization over the BFS-reachable node set and reattaches each node's composed generic args from the BFS pass (so arg-composition across hops is preserved). `find_on_chain` (`members.rs`) consults `profile.ancestor_order`; Python sets `C3`, the other 95 positional profile literals were mechanically swept to `Bfs` (precedent: every axis this campaign; the spread-based test profiles inherit `Bfs` from `DEFAULT_PROFILE`). No hook, no `languages/python` resolution code. Failing-first test: asymmetric diamond `O ; A(O) ; B(A) ; C(O) ; D(B,C)` — BFS(D)=`D,B,C,A,O`, C3(D)=`D,B,A,C,O`; a member overridden on both A and C resolves on C under Bfs and A under C3, proving order selects the override. Tests also assert the C3 walk is total (same node SET as BFS) and that a `Generic<Concrete>` edge still composes the concrete arg to the deep node under C3.

**Deferred forks (documented, not picked silently):**
- *Inconsistent-MRO failure policy.* C3's merge can stall (no candidate is a valid head). The cut degrades to BFS order for the unresolvable remainder — sound (total walk, no dropped ancestor). Whether to instead emit a diagnostic or skip the offending node is a louder behavior that an agent must escalate, not pick; degrade-to-BFS is the safe ship.
- *External/unindexed base in the C3 merge.* A parent present as an edge target but absent as a graph node (no recorded parents) participates as a leaf — matches BFS reachability. Excluding it instead would require an external-resolution step inside the linearizer, which the cut deliberately does NOT add.
- *Defaulted-field-vs-sweep* — RESOLVED, not deferred: followed the campaign precedent (non-defaulted positional field swept across all literals + added to `DEFAULT_PROFILE`).

### LANG-SWIFT-1 — Swift import strategy + protocol extensions  ·  [generic]+[profile]  ·  ⚠️ (part a done; part b deferred)
- **part (a) — scope-directed explicit-member import** — ✅. Generic strategy `resolve_via_explicit_member_import` on the shared ladder, slotted just before `resolve_via_file_import`, gated by the new `explicit_member_import` bool profile field (default false; only `SWIFT_PROFILE` opts in). Fires only for an `ImportEntry` whose `module_path` is **dotted** and whose last segment equals both `imported_name` and the bare target — the `import struct|class|enum|protocol|func Module.Symbol` form. Binds the UNIQUE internal `by_name(target)` symbol of compatible kind (reusing the `unique_internal_name` filter/dedup convention); zero or multiple candidates decline. The dottedness is the soundness boundary: a plain whole-module `import Foundation` (no dot) leaves the strategy inert, so this is strictly narrower than the reverted `resolve_all()` blanket fallback (`69d35851`/`17afa91d`). No Swift code — `SwiftHooks::build_file_context` already produces the dotted-module `ImportEntry` the strategy consumes; the deleted `swift_by_name` grep is NOT reintroduced.
- **part (b) — protocol extensions / retroactive conformance / `some`·`any`** — DEFERRED (forks below).

**Deferred forks (b + the whole-module fork of a):**
- *Protocol-extension default methods* — should `extension SomeProtocol { func foo() {} }` attach its members to the protocol symbol (so a conforming type inherits `foo` via the existing `SupertypeGraph`/`MembersIndex` walk), or stay a free-standing extension decl? The Swift extractor emits extensions as separate decls; attaching them is an extractor/graph-shape decision with corpus impact — not guessed.
- *Retroactive conformance* `extension T: P {}` — whether the extractor emits an `Implements` edge from the extension decl back to `T` (so `T` gains `P`'s default methods). Extractor decision.
- *`some P` / `any P` opaque/existential receiver typing* — how an opaque/existential value dispatches members through `P`. Type-inference decision.
- *Whole-module import binding* (`import Foundation` → bare `URLSession`) — binding a bare ref to a same-module top-level symbol requires a SwiftPM target → `Sources/<Target>` boundary map. Without it the bind degrades to whole-program by-name — the exact unsound path that got `69d35851` reverted. Building a manifest-driven SwiftPM module-boundary index is a genuine architecture decision; the bounded cut deliberately handles only the explicit-member form where the import IS the scope.

### LANG-TS-1 — Remaining type-level operations  ·  [profile]  ·  ⚠️
Structural matching (INFER-5); conditional-beyond-decidable; mapped-beyond-transparent (roadmap TS2 — rare); template-literal types; `satisfies`; `infer`; declaration merging / module augmentation. Mostly low corpus value — keep deprioritized.

### LANG-DART-1 — Dart binding  ·  [hook]  ·  ⚠️
- **1a** bare prefix-ref drop — ✅ (`4be1cb83`).
- **1b** prefix → library binding — ✅ (`000be634` + `9a38cc6a`). The extractor keeps the import URI (`collect_dart_import_aliases`) and `bind_prefixed_refs` stamps `namespace_segments=[prefix]` + `module=Some(uri)` onto the name ref, so a prefixed external ref (`i0.Value` → drift/`dart:`) is **branded external** via `classify_external` instead of dropping the prefix and staying `unresolved_unknown` (the QUAL-5 three-state win). A prefixed ref that misses its module **declines** (never falls through to the same-file bare-name strategy — that would mis-bind a coincidental local symbol @1.0). The project-local case (`i2.X` → `./x.dart`) is **closed by BIND-2d** (`9a38cc6a`): a Dart `ModuleResolver` resolves relative (`./`/`../`/bare) and `package:<self>/` specifiers (self-package name folded from pubspec into `package_names`, threaded via `all_resolvers_with_manifest_data`); `dart:`/foreign `package:` decline. Cross-language `module_to_file` first-write-wins for bare same-named neighbours in different dirs is a pre-existing limitation, not a Dart regression.
- **1c** Flutter SDK externals — ✅ (was mis-marked ❌). `FlutterSdkEcosystem` (`ecosystem/flutter_sdk.rs`) walks `packages/flutter/lib/src`; `demand_pre_pull` surfaces `ListTile`/`IconData`/`EdgeInsets` (`flutter_sdk.rs:77`); registered + tested (`flutter_sdk_tests.rs`).

### Deferred per-language (recorded, low value)
- Kotlin smart-cast / Rust `if let`/`match` discriminant (roadmap L2/L3) — grammar-uncertain.
- Haskell generic-param bounds (roadmap L1) — params outside any bracket clause, near-zero payoff.

---

## F. Correctness, confidence — and the one-engine consolidation

### QUAL-1 — Bar the whole grep family (D1/D9)  ·  [generic]+[hook]  ·  ⚠️ (H.1+H.2 ✅, H.3 remains)
**Invariant:** never bind a bare name to a coincidental same-name symbol.
- **1a** `ranked_candidates` + `unique_internal_name` removed from default ladder — ✅ (`019fc8a7`).
- **1b** ✅ for the two grep buckets: the ordering bugs are fixed (Appendix), **H.2** (internal whole-program `by_name` — `{c,kotlin,swift,dart,elixir}_by_name`, `rust_global_name_*`, …) deleted in `c4252e8b`, and **H.1** (the `*_synthetic_global` ext-bare-name family) deleted in `bf46353b` (= QUAL-3). Verified: 0 occurrences of any of those strategy literals remain. **Remaining = H.3 only** (~20 borderline: same-file/module fallbacks whose risk is ladder order, or `by_name` scoped by a coarse path/prefix rather than a qualified-name lookup). Each needs reorder-after-imports or a tighter filter — none is a whole-program bare grep, so this is precision-tuning, not invariant-breaking.
**Deps:** BIND-1 ✅ (so removing same-file grep doesn't drop legitimate scope hits)

### QUAL-2 — One generic chain walker, all languages on it (D9)  ·  [generic]  ·  ✅ — **the central architectural deliverable, done**
**This task decided whether BearWisdom is "one engine + data" or "94 resolvers."** Done: all **9** bespoke `MemberChain` walkers are retired onto `resolve_via_chain(&<LANG>_CHAIN_CONFIG)`.
- **2a** reroute apparatus deleted — ✅ (`a3815b4e`).
- **2b — retire the bespoke chain walkers — ✅.** The 9 forks (`walk_typescript_chain` 480L, `walk_csharp_chain`, `walk_go_chain`, `walk_java_chain`, `walk_python_chain`, `walk_ruby_chain`, `walk_php_chain`, `walk_c_lang_chain`, `walk_rust_lang_chain`) are deleted; each language is now `<LANG>_CHAIN_CONFIG` **data** on the shared walker. The per-language deltas reduced to `ChainExtensions` bools (`expand_aliases` / `walk_inheritance` / `promote_external_qname` / `root_construction` / `extension_method_fallback` / `root_type_access`) — **one `ChainExtensions` fn-pointer total** (`root_fallback`, the npm-globals ambient probe; `normalize_type` is a separate pre-existing `ChainConfig` fn-pointer reused by C and Rust for `::`→`.`). Net **−1,294 production LOC**; **6,100** lib tests, zero regressions across all 9 slices. Commits `58ad2b24` · `329c5a51` · `c406434e` · `22c4adc7` · `73a578b3` · `b2c20fa3`. The 5 languages already on the generic walker (Dart/Kotlin/Scala/Swift/Starlark) stay on `ChainExtensions::NONE`. **14 languages now share one chain algorithm.** Migrating onto the shared ladder is widening-only for the weaker forks (deeper inheritance climb, `by_name`-prefix-unique 1.0 hop, `members_of`-final) — fires after base lookups miss, kind-gated; corpus-delta deferred to the DOC-4 recapture.
  **Honesty note:** the py/ruby/php/c batch under-counted — it migrated those 4 and reported "4 remaining" when Rust was still a fork, so QUAL-2 was briefly mis-marked complete at 8. `walk_rust_lang_chain` was then caught (a misused `bw_grep` without `regex=true` first gave a false "gone") and migrated as the 9th. The 9-count is now verified.
- **Ada is NOT a chain-walker fork.** Beyond the 9, the only remaining `walk_*_chain` is Ada's `walk_field_chain` — verified NOT a 10th chain walker: Ada's extractor emits **no `MemberChain`s** (`chain: None` on every ref; no `ada/` file among `chain: Some(...)` emitters), so `resolve_via_chain` (requires ≥2 segments) resolves nothing for Ada. It is a field-resolution helper *inside* Ada's flat-dotted-name resolver (`AdaResolver::resolve`). Forcing it on resolves nothing AND would need ~7 Ada-only flags (a gate violation). **Out of QUAL-2b scope.** Whether Ada's name resolver (and other languages' name-resolution tails) consolidate onto a shared NAME-resolution path is **QUAL-6** (the binder analogue of this task) — now in progress: every other language's `resolve_ref` is drained to data on the engine, the TS-family is staged, and Ada is one of 3 residual hooks (partially drainable, with a genuinely Ada-specific visibility/rename/instantiation core).
- **2c — Step-5 by-name fallback tail** — largely folded into the migrations; any residue is QUAL-1 H.3 reorder precision-tuning.
**Outcome:** every `LANG-*` rule (Rust traits, Go interface satisfaction, Kotlin extensions, INFER-10 alias expansion) now lands as a `ChainConfig` / `ChainExtensions` delta on the *one* walker — the gate is enforceable going forward.

### QUAL-3 — `*_synthetic_global` is import-blind external grep  ·  [generic]+[profile]  ·  ✅
**Done (`bf46353b`).** Option A executed: the whole `*_synthetic_global` ext-bare-name family is deleted (0 strategy literals remain). A bare external name now resolves only via scope/import or a curated prelude, or stays unresolved. This is the honest-floor crater behind the corpus drop (kept: `rust_prelude`, `scala_implicit_import`, `php_global_function`, `ada_modular_primitive`). True-globals like `print`/`len` will be recovered later via curated per-language **prelude strategies** (EXT-5-adjacent), not by name grep. Original audit/decision below.

**Resolved (was 🔬).** Audited the 17 `*_synthetic_global` strategies: they bind to **real** symbols parsed from real SDKs by real walkers (`dart_sdk.rs`, `flutter_sdk.rs` ~700 files, `ext:cpython-stdlib:`, `kotlin_stdlib`/`jdk_src`/`android_sdk`/maven sources jars). **Not** fabricated stubs — `ecosystem files are locators only` is honored; the "synthetic" name is a misnomer. The stub-crutch framing is dead.
**Real defect:** the binding is whole-program bare-name `by_name` with first-`ext:`-match-wins and **no import/scope check** (`<lang>/hooks.rs` `resolve_ref`; `by_name` is a flat map, `lookup_impl.rs:18`). That is Invariant #2's grep, filtered to `ext:` rows — real target, unjustified claim. `dart_synthetic_global` = 176k edges (55% of ts-immich) is mostly import-gated Flutter widgets (`ListTile`/`Widget`/`BuildContext`) bound by coincidence.
**Decision (Option A — honest floor):** **delete** the whole `*_synthetic_global` ext-bare-name family outright (the import-blind `by_name`-into-`ext:` grep). A bare external name now resolves only via a scope/import strategy, a curated prelude check, or stays unresolved — never by coincidental name match. This drops the import-gated coincidences (the ~176k Dart Flutter binds and equivalents) AND the true-globals (`print`/`len`) until per-language **prelude strategies** (curated, scope-directed — like `rust_prelude`) are rebuilt to recover the latter legitimately. The baseline will fall to the honest scope-directed floor first, then climb back on real binds. Kept: `rust_prelude`, `scala_implicit_import`, `php_global_function`, `ada_modular_primitive` (these are curated/qualified-name, not by_name grep).
**Deps:** QUAL-1b, BIND-2; QUAL-5 for the honest restatement.

### QUAL-4 — Collapse confidence to {resolved, unresolved} (D8)  ·  ⚠️
As grep goes, drop 0.x-confidence edges; a front-end answers resolved or error. Falls out of QUAL-1/QUAL-2.

### QUAL-5 — Three-state external model  ·  [generic]  ·  ✅
**Compiler feature:** a miss is not one bucket — separate "dep source absent" from "genuine unknown" so precision is measurable instead of hidden in a coverage rate.
**Done.** No schema change: the three states were already persisted in three disjoint tables — `edges` (`resolved`), `external_refs` (`external_known_unhydrated` — the EXT-1 outcome: `classify_external_ns` routed the ref to a known `ext:<module>` namespace but `locate` never hydrated the source, `loop_body.rs:608`), `unresolved_refs` (`unresolved_unknown` — `classify_external_ns → None`, `loop_body.rs:747`). The gap was the metric: `ResolutionBreakdown` (`stats.rs`) computed `internal_edges / (internal_edges + internal_unresolved)` — already precision, since `internal_unresolved` joins only `unresolved_refs` — but never surfaced the third bucket, so the dependency-availability gap was invisible and the precision contract was undocumented/untested. Added `external_known_unhydrated` (internal-origin `external_refs` count) and a `precision` field (== `internal_resolution_rate`, naming the contract) to `ResolutionBreakdown`, and surfaced both in the MCP/CLI compact header (`compact.rs`). Precision explicitly excludes the unhydrated bucket from the denominator. Failing-first locked by `resolution_breakdown_distinguishes_three_states` (resolved + dep-absent + typo → 1/1/1, precision 50%) and `external_known_unhydrated_excluded_from_precision_denominator` (all-unhydrated → precision 100%). Consumers unchanged: `cmd_resolution_gate` serializes the breakdown via serde (new fields flow through); the quality-check baseline reads only `resolution_rate`/`internal_edges` (preserved). `resolution_gate` integration suite green (rate still matches dead-code health).
**Deps:** EXT-1 ✅ (produces the `external_known_unhydrated` signal at the `locate` miss).

### FACADE-1 — `SymbolView` + `ContainingScope`: structured identity / types / containment  ·  [generic]  ·  ✅ (facade + type-half + containment source-of-truth)
**The smell.** Resolution hand-correlated three projections of the same symbol table: `by_name` → `SymbolTypeMap::get(id)` (param/return types) → containment (ancestry). All three are built **together** up front in `build_with_context`, so the fragmentation is ergonomic debt, not a storage necessity. **Roslyn's `ISymbol` is a *facade* over separately-sourced data, not one fat record** — the faithful fix is a facade, NOT a physical merge (a merge fights cache-locality: the ranking inner loop wants dense `param_types`, not containment dragged through cache).
**Type-half done.** `SymbolView<'a>` accessors (`param_types`/`return_type`/`declared_type`/`generic_params`, honest tri-state — `None`=no record, `Some(&[])`=zero) carry the seven `symbol_types.get(id).<field>` sites: `chain.rs` (`yield_type_of`, `infer_bare_call_yield`), `members.rs` (overload arg-type filter), `pattern.rs`, `dispatch.rs` (`most_specific_index`, `select_return_type`). `subtype.rs`'s variance check reads the **whole record gated on existence** — the one distinction the per-field accessors deliberately collapse — so it uses `type_data() -> Option<&SymbolTypeData>`. Borrowed accessors return **data-lifetime (`'a`)** refs (a latent facade bug: receiver-lifetime let a borrow dangle once `subtype.rs` held it past the view temporary). `bind_call_arg_generics` keeps the raw map — id-only, no `SymbolInfo`, facade N/A.
**Containment source-of-truth done.** The two flattened `enclosing_*_qname: FxHashMap<String,String>` maps on `SymbolIndex` — kind-tagged structure squeezed into two strings, the structure discarded — are **deleted**, replaced by one `FxHashMap<String, ContainingScope>` (the same string→concrete-type direction as the TypeId migration; perf/memory is not the constraint, structured code is). `ScopeFrame` carries the symbol's qname (canonical key; cumulative package path for synthesized namespace-tail frames). `ContainingScope::containing_type_qname()` / `containing_namespace_qname()` are the projections the flat maps precomputed; `enclosing_*_qname` re-express over them (existing consumers byte-identical). `SymbolLookup::containing_scope()` exposes the structure for chain-needing callers, making `SymbolView::with_scope` usable at resolve time. **ScopeArena threading is rejected** — the qname-keyed structured map IS the resolve-time source; the `ScopeArena`/`ScopeId` type (designed for id-keyed `Copy` handles) stays as unused infra, not threaded.
**Consumers migrated onto the structure:** `default_resolver::enclosing_type` (structured `containing_type_qname` first; scope_chain walk + scope_path are the synthetic-lookup fallback); `augment.rs` member-grouping (structural `parent_index`, so the incremental path matches the full-build path and files a dropped-prefix child under its real parent). The remaining qname-split sites from the prior audit are **not** clean containment migrations and stay, each for a concrete reason:
- `go/predicates::sym_package` — **dead** (no callers).
- `rust_lang/predicates::sym_module` — a *parent == module* equality filter selecting **module-level items only**; `containing_namespace_qname` would also match methods nested in types, over-resolving a bare name to a nested method. Not a containing-namespace query.
- `extract_package_name` / `extract_module_path` — **file-level** package extraction, not a per-symbol containment query.
- `discover_type_by_canonical_members` — operates on `by_name` results that include **external `.d.ts`** symbols the index's containment map need not cover; `rsplit` on the qname-in-hand is robust regardless.
- `scope_tree.rs` — **parser-level**: derives `scope_path` during extraction, *feeds* containment rather than consuming it.
- the Ada `rsplit_once` sites — **boundary parse** of ref-*target* strings, not the symbol's own qname (Bucket 3).
**Still open (separate arcs, NOT facade edits):**
- **Bucket 2 — string→`TypeId` migration arc — ~128 sites.** Bare `target_name`/`module_path`/FQN strings split because no structured type exists at that point; dissolved upstream by interning types and carrying structured refs. Rides the broader `TypeId` migration.
- **Bucket 3 — irreducible boundary parse — ~89 sites, low priority.** Extractors/walkers parsing a dotted token from source/jar/manifest. Cannot reach zero.
**Deps:** — (standalone). Bucket 2 rides the broader `TypeId` migration.

### QUAL-6 — Retire every per-language `resolve_ref` name-resolution hook (the binder analogue of QUAL-2)  ·  [generic]+[profile]  ·  ⚠️ (≈67 langs drained ✅, incl. the TS-family capstone `98b96555`, elixir, and sql; ada partially drained — a genuine special case whose `resolve_ref` stays non-zero by design)
**This is QUAL-2 for name resolution.** QUAL-2 retired the 9 chain *walkers* onto `resolve_via_chain`. QUAL-6 retires every per-language `LanguageEngineHooks::resolve_ref` *name-resolution* hook the same way: absorb each hook's behavior into a generic ladder strategy + a `LanguageProfile` **data** axis, then delete the hook (and its `*Resolver`). It answers the "separate future question" QUAL-2 left open. Same gate — a language drains by becoming data on one engine, never by a thinner fork.

**Enabling move (`c800c5ea`).** The bare-name path now calls `DefaultResolver::resolve_all_with_profile(profile)` — the full 18-strategy generic ladder, kind-gated against `profile.kind_compatible_table` — instead of the lossy 6-of-18 `resolve_bare`. `bare.rs` (`resolve_bare`, 201 LOC) + the `engine_primary` profile axis are deleted; each language's `resolve_ref` is demoted to a flagless `.or_else` residue tail (`engine.rs` `resolve_bare_pre/_post`). Net −822. Without this, no deletion below is possible.

**New generic ladder strategies** (all profile-gated, default-inert, in `core/default_resolver.rs` unless noted):
- `resolve_via_import_path` (:944) — template include/partial → the named project file (`d37e8de2`).
- `resolve_via_module_anchor` (:1017) — a ref carrying `r.module` → bind in that module's file (`1b3232e7`, extended `8a762e91`).
- `resolve_via_external_by_import` (:1171) — import-scoped bind to an external symbol (`1b3232e7`).
- `resolve_via_same_dir` (:1273) — same-directory-is-the-package bind (`8a762e91`).
- `resolve_via_head_alias` (:1306) — dotted target's head names an in-file alias decl (`734e73d7`).
- `resolve_via_alias_module_qname` — a bare target equal to an import's bound name binds to the MODULE symbol whose qname IS the import's full module path (`alias MyApp.Foo` → bare `Foo`); reusable by any namespace-qualified-import language. Gated by the `alias_module_qname` axis (drains elixir).
- `resolve_via_file_scoped_import` (:1360) + `resolve_via_alias_decoded_import` (:1423) — a file-naming import brings members into bare scope; alias-decode routes dynamic-library keywords (`734e73d7`).
- `resolve_via_package_short_name` + the `chain.rs` `qualify_current_ty` PackageShortName branch (:799) — import-names-a-package qualification (`41d6df7f`).

**New `LanguageProfile` data axes** (the data-first proof — `profile/language_profile.rs`): `builtin_skip` (:47, the BUILTIN_SKIP seam), `namespace_decline` (:57), `decline_qualified_when_prefix_imported` (:68), `module_skip` (:79), `ambient_namespace_prefixes` (:86), `import_resolution` (:95) + `import_module_path` (:101), `module_anchor` (:106, with `ModuleAnchorBind` {NameExactKind / PreferNamedElseFirst / ByNameUnderModuleDir / ByFileStem / MemberOfModuleType}) + `module_anchor_terminal` (:112) + `relative_marker` (:120), `external_by_import` (:125) + `ext_match` (:157), `name_normalization` (:134, identity-default), `package_by_directory` (:141), `wildcard_match` (:149), `head_alias` (:166), `file_scoped_imports` (:174, with `AliasDecode`), `alias_module_qname` (bool, default false — elixir opts in), the `ChainQualification::PackageShortName` variant (:38 field), and `qname_separator` (:23) newly *threaded* into the ladder. **fn-pointer accounting:** only `builtin_skip` / `namespace_decline` / `module_skip` are fn-pointers (heterogeneous predicates — lists + prefix + pattern); everything else is pure const data. Converting the list-expressible predicates to pure data is the **profile-field cleanup** follow-on.

**Drain tally — ≈60 languages to zero `resolve_ref`**, grouped by mechanism:
- JVM cohort onto `SamePackageAndImports` + Go onto `PackageShortName` — java/groovy/kotlin/scala/csharp/php/go (`b0894426`·`960aafdc`·`41d6df7f`); 8 `*Resolver`s deleted (−2,941 on the C#/PHP/Go commit alone, mostly deleted per-language `resolve_tests.rs`).
- `builtin_skip` primitive-skip + pure-delegation deletion — make + 10 builtin-skip langs (`8daee82b`); the 8-language near-delegation bucket whose `resolve_ref` only duplicated the ladder (`0b45e0a9`).
- `import_resolution` template cohort — handlebars/ejs/pug/gsp/yaml/nunjucks/markdown (`d37e8de2`), then jinja/heex/templ/dockerfile via reuse + `KindTable` data (`43e8c957`).
- `module_anchor`/separator/normalization cohort — dart/ruby/python (`1b3232e7`), swift/zig/hare (`54e40699`), cobol/vba (`6329e0cf`), ocaml/odin/fortran (`8a762e91`), prolog/puppet/erlang/scss/pascal/nim (`958c1310`), c_lang/bicep last thin hooks (`622e1253`).
- per-language new-strategy drains — hcl/robot (`734e73d7`), rust (`fcf85722`: `module_anchor`(ByNameUnderModuleDir) + `::` separator + sysroot-prelude ambient + `pub use` `is_reexport` tag), starlark (`46ff3391`: `StarlarkModuleResolver` + bazel-builtins ambient), elixir (`resolve_via_alias_module_qname` + `alias_module_qname` axis; `ElixirResolver` deleted).

**TS-family capstone — ✅ landed (`98b96555`)** (lib 6441/0, all 7 integration suites green, gate met — `resolve_ref==0` and `root_resolver()==0` across all six; the ~90-file `profile.rs` sweep is the `self_receiver_discovery` field add). The 6 (`typescript`/`vue`/`svelte`/`astro`/`mdx`/`angular`) are one coupled unit: only `typescript/hooks.rs` owned a real `resolve_ref` (delegating to `TypeScriptResolver`); the other five delegate into the TS path (a `.vue`/`.svelte`/`.astro`/`.mdx` `<script>` block *is* TS). The drain removes `resolve_ref` from all six and **retires the third resolution seam** — the `LanguageEngineHooks::root_resolver()` override, used only by Vue (`languages/vue/root_resolver.rs`) to type a bare `self`/`this` chain root via `discover_type_by_canonical_members`. It moves the axis to data: `LanguageProfile::self_receiver_discovery: SelfReceiverDiscovery { ScopePathThenDefault | CanonicalMembers{seed,siblings} }`, consumed by `ProfileRootResolver` (`core/chain.rs:310`, built at `engine.rs:230`); Vue opts in via `VUE_PROFILE`. `vue/root_resolver.rs` + tests are deleted. Gate: `resolve_ref==0` **and** `root_resolver()==0` across all six. With this landed, 6 of the 9 residual hooks dropped — only elixir/sql/ada remained; elixir and sql are now also drained (below), leaving ada.

**elixir — ✅ drained.** `resolve_via_alias_module_qname` (mirror of `resolve_via_head_alias` / `resolve_via_file_scoped_import`) binds a bare target equal to an import's bound name to the module symbol whose qname IS the import's full module path (`alias MyApp.Foo` → bare `Foo` → `MyApp.Foo`), gated by the const-data `alias_module_qname: bool` axis (elixir opts in). `ElixirResolver::resolve` + the `resolve_ref` override + the `pub use hooks::ElixirResolver` re-export are deleted; `build_file_context` / `classify_external` / `detect_flow_emissions` + the 6 flow detectors are retained. Zero elixir-specific resolution code — pure structural import-table lookup over the generic `by_qualified_name`.

**sql — ✅ drained.** All three `resolve_ref` steps became data + one generic rung. (1) `is_sql_builtin_type` skip → the existing `builtin_skip` seam (`SQL_PROFILE.builtin_skip = Some(is_sql_builtin_type)`, like prisma). (2) `sql_kind_compatible` → `SQL_KIND_TABLE` const + `kind_compatible_table` (like bicep). (3) the unconditional whole-program first-match by-name `TypeRef` bind → a new `namespaceless_global_type_lookup: bool` axis (default false) consumed by a new generic rung `resolve_via_namespaceless_global` (`core/default_resolver.rs`): for a flat-global language with no imports/namespace/scope, bind a bare target to the FIRST kind-compatible, project-internal symbol of the same name. Non-unique by design (duplicate table names across files, no structure to disambiguate) — the sibling of `resolve_via_unique_internal_name` that declines on >1; runs dead last so structural evidence always wins. `SqlHooks::resolve_ref` + `sql_kind_compatible` deleted; `classify_external`/`build_file_context`/`is_sql_builtin_type` retained (non-resolution). `resolve_ref==0` for sql; the rung serves any other namespaceless DDL/config language.

**ada — ⚠️ partial drain (genuine special case, `resolve_ref` stays non-zero by design).**
The drainable CI axis landed as DATA, reusing the existing `name_normalization` axis — **no new field**. `ADA_PROFILE.name_normalization = NameNormalization::Spec(NormSpec{ case_insensitive: true, .. })` (the same axis Pascal/SQL/Fortran/VB/COBOL/Robot use). To fold the dotted/qname rungs that pre-dated the axis and still compared byte-exact, `norm: NameNormalization` was threaded into four generic rungs in `core/default_resolver.rs` — `resolve_via_qname_exact`, `resolve_via_same_namespace`, `resolve_via_imported_namespace`, `resolve_via_namespace_import` — each gaining a case-folding fallback gated on a non-identity spec (`by_name`-leaf fold for `qname_exact`; an `in_namespace` member-scan fold for the other three). Under `NameNormalization::None` every rung borrows both sides identically, so the change is byte-for-byte inert for the other 93 languages; every case-insensitive language now gets CI dotted-qname resolution it lacked. This drained `ada_same_file_ci` from the hook (absorbed by `resolve_via_same_file` under the CI spec). The 5 modular-type primitives (`Shift_Left`/`Shift_Right`/`Shift_Right_Arithmetic`/`Rotate_Left`/`Rotate_Right`, RM 13.7) moved out of the bespoke `is_ada_modular_primitive` + `by_name("Interfaces.*")` scan into `AdaHooks::classify_external`, which classifies a bare modular-primitive call as belonging to `Interfaces` (the `Standard`-numeric pattern) — these are compiler intrinsics, not indexed project symbols, so external classification is the correct outcome.

**IRREDUCIBLE — kept as `AdaResolver` code (no generic-ladder analogue):**
```
ada     ⚠️ partial    — bare-name across imported with/use packages (ada_use_clause / ada_with_primitive),
  [hook]                own-package + ancestor-package visibility (ada_parent_pkg_visibility), three-path
                        rename-clause substitution (ada_parent_pkg_rename / ada_ancestor_pkg_rename /
                        ada_alias_substitution), generic-instantiation chasing (ada_local_instantiation +
                        chase_instantiation), variable-type field-chain dispatch (walk_field_chain /
                        probe_package_of_type), partial-qualification ancestor-prefix expansion
                        (ada_partial_qualification), last-segment import shorthand (ada_last_seg_import),
                        fully-qualified variable field chains. No other language splits members across own-spec
                        + every ancestor package, rewrites a prefix through an `instantiates X` signature, or
                        leaf-case-folds across an imported-package member scan — these are not ladder-shaped.
                        Outcome: AdaResolver shrank (CI same-file + modular-primitive gone); resolve_ref ≠ 0.
```

**Soundness notes (carried from the commits):** `module_anchor_terminal=true` terminates the ladder on an anchor miss so an unindexed external prefix isn't hijacked by a same-named local homonym (`1b3232e7`). Nim dropped its 0.70 bind-any-stdlib guess tier for engine-only bind-exact-or-decline — a rate-affecting decision, not mechanical (`958c1310`). Scala bare JVM-stdlib type names (`Throwable`/`Object`) now stay unresolved pending a future `implicit_wildcard_namespaces` profile list (known gap, `960aafdc`). A consolidated engine-resolution-test backfill across the drained languages is deferred to closeout (~1,900 lines of `*Resolver`-constructing tests were deleted with the resolvers).
**Honesty note:** `8daee82b` lists elixir among its builtin-skip set; its alias→module-qname *bind* now also moved to data (the `alias_module_qname` axis + `resolve_via_alias_module_qname` rung), so both its decline and its bind are engine+data — `resolve_ref` is gone. Rates deferred to **DOC-4** (honest-floor; no mid-refactor corpus claims).
**Deps:** QUAL-2 ✅ (the chain half) · BIND-1 ✅ (so deleting same-file greps keeps legitimate scope hits).

---

## G. Corpus-driven gaps (volume the above doesn't directly cover)

### CORPUS-1 — Component-in-template binding  ·  [profile]+[extractor]  ·  ✅
Svelte (LANG-SVELTE-1 + BIND-2a), **Vue** (incl. vuetify `VCol`/`VBtn` via `vue/global_registry.rs:111`), **Astro** (`astro/extract.rs:116`), **Angular** (`angular/extract.rs:120`), **MDX** (`mdx/extract.rs:5`) — all ✅. **Plain HTML custom-element tags — now ✅** through the existing scope-directed selector map, NOT by-name. The reverted by-name attempt (`b203ba1c` → `b9f76bc9`) failed because tag refs fell to the generic bare-name rungs against the whole index. The fix reuses the **declaration-keyed** selector map (the same `resolve_via_selector_map` binder Angular/Vue use): a new `customElements.define('tag', Class)` recognizer (`typescript/selectors.rs::extract_custom_element_defines`) — the web-platform analog of `@Component({selector})` — feeds `(tag, class_qname)` pairs into `component_selectors`; `parse_file.rs`'s selector gate widened to also harvest `define()` for `javascript`; `HTML_PROFILE.selector_resolution` flipped on (`[EdgeKind::Calls]`, `PascalToKebab`); `html/extract.rs` emits a `Calls` ref per custom-element/PascalCase tag (mirrors `astro/extract.rs::process_element`, generated-HTML early-return preserved). A `<user-card>` binds ONLY when the project did `customElements.define('user-card', …)`. **Soundness verified empirically** (`html/resolve_tests.rs::undefined_library_tag_does_not_coincidentally_bind`): a library tag like `<ion-button>` with no project define declines the selector map and — because `HTML_PROFILE` keeps every bare-name rung inert (`namespaceless_global_type_lookup=false`, `ambient_globals=Off`, no imports/scope) and `resolve_via_qname_exact` requires a dotted target — falls through to None instead of binding a same-named symbol. No new resolver, no new walker, binder untouched. **Deferred forks** (documented, not blocking): (1) registry scope kept project-wide — `customElements.define()` registers into the global `CustomElementRegistry`, so project-wide IS the correct web-component scope (architect: confirm vs. per-`<script type=module>`-import scoping, much larger); (2) inline-`<script>` `define()` in the same `.html` file routes through the embedded-region JS extractor — whether `component_selectors` survives the embedded-region merge into the host `ParsedFile` is untraced; the cross-file `define()`-in-`.js`/`.ts` case lands first.

### CORPUS-2 — DSL / markup resolvers below the 99% gate  ·  [profile]+[hook]  ·  ⚠️ (partial)
Languages whose resolvers are weak or absent and so fail the per-language 99% bar: **gsp** — ⚠️ now ON the engine (`8cf6c168`): `GspHooks::build_file_context` was missing so the engine never ran on gsp; added it + `resolve_ref` binding `<g:render template>` partials to their `.gsp` Class by path (strategy `gsp_template`, mirrors handlebars/markdown). **html** (CORPUS-1 plain custom-element tags now ✅ via the declaration-keyed selector map), **prolog** (74%). All four of bicep/vbnet/matlab/r ride the generic ladder with ZERO `resolve_ref` hooks already (vbnet adds only a `classify_external` namespace classifier, no `resolve_ref`) — none is a "weak/absent resolver"; their gaps were diagnosed individually:

- **matlab** (62%) ✅ — pure-data drain. Internal bucket: project functions called bare (`NDSort`, `CalFitness`, `UniformPoint`, …), MATLAB's one-function-per-file path resolution. `MATLAB_PROFILE.package_by_directory = true` (same-folder sibling binds first via `resolve_via_same_dir` — correct MATLAB precedence) + `namespaceless_global_type_lookup = true` (dead-last `resolve_via_namespaceless_global` first-match-binds a cross-dir unique). Toolbox intrinsics (`pdist2`, `randperm`, `chol`, …) decline (internal-only rung) and fall to `classify_external`. Two bool flips, no engine code.
- **r** (76%) ✅ — pure-data drain. R is a flat top-level function namespace (SQL-identical): project-internal exports (`group_by`, `mutate`, `aes`, `ggplot`, …) defined in sibling `.R` files bind first-match via `namespaceless_global_type_lookup = true`. `testthat`/base builtins decline (no internal symbol) and stay external; qualified `pkg::func` carries the `::` separator so the bare-name rung never sees it. One bool flip.
- **vbnet** (46% → 89.5%) ✅ — externals-parity drain (NOT a stale-cache artifact; the diagnosis was wrong). The .NET externals were already hydrating for vbnet (NuGet activation `ManifestMatch` matches `.vbproj`; dotnet-stdlib `LanguagePresent("vbnet")`; `collect_dotnet_project_files` matches `.vbproj`) — the gap was purely resolution-side MATCHING. The vbnet plugin lacked the two pieces csharp/fsharp both have: (1) **`chain_qualification` flip** `None`→`SamePackageAndImports` — so the generic `qualify_current_ty` (`type_checker/core/chain.rs`) rewrites a bare receiver type to its FQ namespace form and hard-binds (1.0) to the hydrated `ext:dotnet` symbol; (2) a **`classify_external` hook override** mirroring fsharp's, using the shared data-driven namespace classifier (`csharp::hooks::is_manifest_external_namespace` — NuGet manifest deps + System/Microsoft namespace FACTS, not an API-name table). `Imports System.X`/`Imports <NuGetPkg>` import refs and bare type refs under an external wildcard `Imports` namespace now classify external instead of falling into unresolved. Secondary mislabel fixed: `dominant_dotnet_language` returned `"vb"` for vbnet-dominant projects but the registered plugin id is `"vbnet"`. **vbnet-compactgui single-project recapture: 56.22% → 89.46%** (internal_unresolved 936→132; vbnet.imports 135→13, vbnet.instantiates 157→17, vbnet.calls 643→102). **Deferred sub-cases (same ceiling as csharp, not vbnet-specific):** `qualify_current_ty` case 3 only matches NON-wildcard imports that name the class itself, so VB's namespace-wildcard `Imports System.Windows.Controls` does NOT drive a 1.0 hard-bind of bare `Button` (no per-wildcard-prefix probe in the generic walker — csharp has the identical gap; those refs land as `external` via classify_external, the sound widening-only outcome). VB `Imports <alias> = System.X` aliased imports not threaded (`build_file_context` sets `alias: None`). `My.*` compiler namespace + late-bound `Option Strict Off` dispatch remain honestly unresolved (no on-disk artifact for `My.*` generated members).
- **bicep** (44%) — DEFERRED (needs an architecture decision). 100% of unresolved are Calls to Bicep/ARM runtime intrinsics: decorators (`@description`/`@allowed`/`@secure`/…) and ARM builtin functions (`resourceId`/`uniqueString`/`resourceGroup`/`union`/`json`/`reference`/…). These are runtime intrinsics with NO on-disk source (like Ada modular primitives). The generic ambient-globals rung needs a structural NAME source (synthetic namespace or ambient-lib file) that does not exist for bicep; the ONLY way to tell a bare builtin from a typo/undefined func is a name source, and a hand-maintained bicep-builtin table is forbidden predicate-stuffing. A legitimate source — a bicep-runtime ambient-symbol ecosystem locator surfacing the bicep CLI's intrinsic catalog (then classify via `BicepHooks::classify_external`, the Ada `Interfaces` precedent) — is a NEW ecosystem source that hits the "no new walkers without discussion" gate. Alternative: accept bicep as documented-borderline (runtime-intrinsic-bound, unindexable without a runtime catalog). Architect's call.

Deferred MATLAB precedence note: `resolve_via_namespaceless_global` first-matches arbitrarily among non-unique cross-dir names (`CalFitness` defined in ~30 folders). `same_dir` runs first and fixes the common case; a cross-dir bare call to a name with NO same-dir sibling and >1 global definition binds an arbitrary one — the same trade-off SQL accepts by design. Accept as-is, or add decline-on-ambiguity for the cross-dir case (a behavior the rung does not currently offer). Numbers are pre-recapture (DOC-4) and indicative.

---

## H. Hygiene / consolidation

### DOC-1 — Retire `RESOLUTION-EXTERNAL-ROUTING.md`  ·  ✅
Deleted 2026-05-26; its north star (D1–D9) is folded into §A/§C/§F here.

### DOC-2 — Retire `TYPE-INFERENCE-ROADMAP.md`  ·  ✅
Deleted 2026-05-26 (along with `RESOLUTION-GOAL.md`, `RESOLUTION-TASKS.md`,
`GENERIC-PARAM-UNIFICATION.md`, `baseline-gaps.md`, `baseline-by-so2025.md`).
Remaining roadmap items folded into LANG-* / INFER-* here; R1 is moot (the gate
and arrow-return path were deleted in `a3815b4e`).

### DOC-3 — Close residual long-tail  ·  ✅ (closed by correction)
The 13 `$t` are not store auto-subscriptions: each is a local parameter or `const` literally named `$t` (`$` is a legal JS identifier char), and `$t('k')` is an ordinary local call. The SFC store-desugar (`desugar_store_ref_in_place`) is correctly **not** reached — it is gated behind `host_is_svelte` on the embedded-region splice path (`embedded_regions.rs`), so plain `.ts` keeps the `$` intact; running it here would rewrite `$t` → `t` and unbind the parameter. Plain-`.ts` extraction preserves the leading `$` in both `target_name` and the chain root (regression-locked by `ts_dollar_local_param_call_keeps_dollar` in `typescript/extract_tests.rs`); the generic scope resolver binds these local calls at confidence 1.0. `$lib/...` are SvelteKit alias imports handled by `resolve_via_aliased_import` (BIND-2a); any residue is alias-table coverage, not a new mechanism.

### DOC-4 — Full-corpus recapture → the per-language 99% scoreboard  ·  ❌
`baseline-all.json` (`captured_at` 2026-05-27) predates this restructure and lacks the QUAL-5 three-state fields. One recapture **at initiative closeout** (per the one-recapture rule — *not* mid-refactor), reporting QUAL-5's three states separately and **per language against the 99% gate**. This recapture is what turns "Definition of done" from a target into a checked scoreboard; until then per-language % is honest-floor noise.

---

## Sequence — architecture-first, toward 99% per language through one engine

The ordering rule changed. The corpus gap does **not** close by writing per-language
rules; it closes by finishing the generic capabilities and **moving every language
onto them**. Per-language % is a *consequence* of that, measured once at closeout (DOC-4).

1. **QUAL-2b — consolidate the chain walkers — ✅ DONE.** All **9** `MemberChain` walkers retired onto `resolve_via_chain` (Ada is a name resolver, not a chain fork — out of scope). **14 languages** on one algorithm, **−1,294** production LOC, 1 fn-pointer, 0 regressions. The gate is enforced; everything below now lands as data/capability on the survivor. **QUAL-6 (the name-resolution analogue) — ⚠️ in progress:** ≈60 languages' `resolve_ref` hooks drained onto generic ladder strategies + profile data; the TS-family landed (`98b96555`, retiring the `root_resolver()` seam via `self_receiver_discovery` data); elixir/sql/ada residual.
2. **§B generic inference.** **INFER-5 ✅** (structural assignability, `d7626b76`). **INFER-10 ✅** (alias expansion, `a948e965`). The expression-typing track's `CallArg`-recursive prerequisite **landed** (`32104a9d`/`8b5fd790`): **INFER-4 ✅ for TS** (the other 9 languages emit `CallArg::Other → Unknown` until their `calls.rs` extraction is wired), and **INFER-8 ✅** (arg-driven generics at a terminal call) rides on it. **INFER-3 ✅ + INFER-2 ✅** (body-return inference + the interprocedural fixpoint that carries inferred returns cross-file). Remaining track work: **INFER-9** (callback-param typing, deps INFER-8 ✅ + INFER-6 ❌), the INFER-8 per-segment-call-args follow-on (bare-name site ✅ `6746b048`; per-segment deferred per the §B INFER-8 note), and INFER-3 widenings (nested/conditional returns, arrow bodies, per-language `return_query`). The `CallArg` breadth fan-out is ✅ (`c78c6d86`) — 10 languages, so INFER-4 + INFER-8 now fire across 11.
3. **CODEGEN-1 — the third symbol source.** Lombok / derive / source-gen are a large unresolved slice; one generic hook + per-language recognizers.
4. **EXT-2 finish + EXT-5 + INFER-6** — external return types, framework ambient globals, container generics (each gated on hydration).
5. **BIND-2e (generic re-export) + BIND-4 (bare-name overload)** — generic binder completeness across languages.
6. **`LANG-*` as data on the survivor walker** — Rust traits, Go interface satisfaction, Kotlin/Scala extensions, Py MRO, Swift protocols (+ Swift's missing `swift_import` strategy), CORPUS-1 HTML, CORPUS-2 DSLs. By now each is a `ChainConfig`/hook/profile delta, not a fork.
7. **DOC-4 closeout recapture** — the per-language 99% scoreboard. (DOC-3 closed by correction — the `$t` long-tail was local params, not stores.)

**State (2026-06-02):** foundations done — externals (EXT-1 / EXT-2 slice-1 / EXT-3 / EXT-4), three-state metric (QUAL-5), CFG narrowing with end-to-end `Union` dispatch across 14 languages (INFER-1), binder shadowing (BIND-1), visibility-blind over-resolve (BIND-3). **QUAL-2 ✅** — all **9** chain-walker forks retired onto `resolve_via_chain`: **14 languages on one algorithm, −1,294 production LOC, 1 fn-pointer, 6,100 lib tests green, 0 regressions** (commits `58ad2b24` · `329c5a51` · `c406434e` · `22c4adc7` · `73a578b3` · `b2c20fa3`). The "one engine" invariant is now **true** for chain resolution. **INFER-5 ✅** (`d7626b76`) + **INFER-10 ✅** (`a948e965`, alias expansion for 6 languages). **Expression-typing track (2026-06-02):** the `CallArg`-recursive prerequisite landed (`32104a9d`/`8b5fd790`), so **INFER-4 ✅ for TS** (9 langs pending extraction) and **INFER-8 ✅** (arg-driven generics, terminal call) — `unify_into` + the `chain.rs` G2 branch, adversarially reviewed (one missed-inference gap found and fixed: owner params are bindable, `is_none`-guarded). Newly tracked after the 2026-06-02 audit: **INFER-9, BIND-2e, BIND-4, CODEGEN-1**; re-scoped **INFER-7, LANG-CPP-1**; corrected **LANG-DART-1c ✅, CORPUS-1 (HTML only), CORPUS-2 (astro dropped)**. Remaining: CODEGEN-1, INFER-9 (gated on INFER-6/EXT-2), the INFER-8 per-segment-args follow-on (deferred), BIND-2e/4, `LANG-*` as data, DOC-4 closeout recapture. (INFER-4/8 `CallArg` breadth fan-out ✅ `c78c6d86` — 11 languages.)

**Update (2026-06-05) — QUAL-6 is the active work.** Every per-language `resolve_ref` name-resolution hook is being drained onto generic ladder strategies + `LanguageProfile` data (§F QUAL-6), the binder analogue of QUAL-2: ≈65 languages at zero, the 6-language TS-family landed (`98b96555`, retiring the `root_resolver()` third seam via `self_receiver_discovery` data), elixir + sql drained to zero, and ada partially drained (CI axis → `name_normalization` data + four newly-folded rungs; modular primitives → `classify_external`) — ada is a genuine special case whose `resolve_ref` stays non-zero by design. Rates deferred to DOC-4.

**Single best next move:** **finish QUAL-6.** The TS-family capstone landed (`98b96555`); drain the residual tail — elixir (`resolve_via_alias_module_qname` generic rung), sql (`namespaceless_global_type_lookup` bool + kind-table/BUILTIN_SKIP data), and Ada's drainable axes (`case_insensitive_identifiers` + builtin/ambient data, leaving the documented visibility/rename core). That closes the "one engine" invariant for *name* resolution as QUAL-2 did for chains. Then resume **CODEGEN-1 — the third symbol source.** With §B's generic-inference spine now extracted across 11 languages, the largest remaining unresolved slice is generator/macro output that never appears in parsed text — Lombok getters, Rust derive impls, C# source-gen members — which has no generic synthesis hook today. One `synthesize_symbols` method on `LanguageEngineHooks` + per-language recognizers (the C `#define` re-scan is the reference shape), no deps, high corpus leverage. INFER-9 (contextual callback-param typing) is the top remaining §B inference item but is gated on INFER-6 (container element types) ← EXT-2 (external generic signatures), so it waits on the externals block. Deferred: the INFER-8 per-segment `ChainSegment` call-args follow-on (single-expression mid-chain), revisit only if DOC-4 shows it matters. (QUAL-2b, INFER-4/5/8/10, INFER-3/INFER-2, the INFER-8 bare-name site, and the INFER-4/8 `CallArg` breadth fan-out — prior single-best-moves — are ✅ done.)

---

## Appendix — Grep-family inventory (QUAL-1b work-list)

Classification of every resolution `strategy` (every `strategy:` literal across 45 language hooks + the generic `default_resolver.rs`/`bare.rs`/`chain.rs`), bucketed by the QUAL-1b test: *does the strategy justify the bind by program structure, or match a bare name against the whole-program index?* Of ~200+ strategies the large majority are SCOPE-DIRECTED (untracked here). Three buckets need action; ~6 legitimate bare-name strategies are kept.

**Ordering bugs — ✅ FIXED.** These greps ran *before* the scope/import strategy and shadowed the correct bind; each now runs after scope/import:
- `default_same_file` / `engine_bare_same_file` gated to yield when a non-wildcard import binds the name (the generic BIND-1 fix) — true locals still win via `scope_visible` (#1).
- `elixir_synthetic_global` moved after scope/module/alias/qualified, before `elixir_by_name`.
- `erlang_otp_arity` moved after `erlang_import_arity` + `resolve_all` + same-file, before `erlang_cross_file_arity`.
- `scala_synthetic_global` early pre-scope call removed; now a final fallback after scope/import/implicit (single-segment DSL stays covered by the post-chain-failure call).
- `c_synthetic_global` moved after scope/qualified/same-file, before `c_by_name`.

### H.1 — External bare-name (the QUAL-3 synthetic family) · ~20
`by_name` filtered only to `ext:` rows → binds "any external symbol of this name." **Fix = QUAL-3 split:** keep the true-global subset, gate import-requiring symbols on an actual import.
`{c,java,kotlin,swift,python,ruby,php,perl,lua,dart,r,scala,fsharp,haskell,elixir}_synthetic_global` (each `<lang>/hooks.rs` `resolve_ref`) · `erlang_otp_arity` · `puppet_synthetic_global` · `gdscript_synthetic_global` · `heex_ext_component` · `prolog_runtime_fallback` (ext: + path heuristic) · `robot_global_name`/`robot_global_normalized` (ext: branch).

### H.2 — Internal whole-program bare-name · ~18
`by_name` over project rows with no import/scope justification ("any symbol of this name wins"). **Fix = make scope-directed or delete** (needs BIND-1 so legitimate scope hits survive).
`c_by_name` · `kotlin_by_name` · `swift_by_name` · `dart_by_name` · `elixir_by_name` · `erlang_cross_file_arity` · `rust_global_name_fallback` · `rust_global_name_scoped` · `rust_global_typeref_fallback` · `default_ranked_candidate` (dormant in `resolve_all`, still callable) · `hcl_provider_alias_cross_file` · `hcl_cross_file_bare` · `nix_attr_path_last_seg` · `puppet_internal_global` · `puppet_unqualified_fallback` · `prolog_project_by_name` · `gdscript_internal_global` · `heex_internal_component`.

### H.3 — Borderline (legitimate mechanism, weak guard) · ~20
Either a same-file/same-module fallback whose only risk is ladder ORDER, or a `by_name` scoped by a coarse path/prefix filter rather than a qualified-name lookup. **Fix = reorder after imports, or tighten the filter.**
`ts_same_file` · `default_unique_internal_name` (whole-program `by_name`, saved only by a single-candidate gate) · `default_ambient_package` · `default_ambient_namespace_path` · `ts_lib_globals` · `ts_npm_globals` · `python_ref_module_path` · `python_ref_module_via_import` · `python_module_qualified_by_name` · `python_from_import_prefix` · `ruby_external_gem` · `rust_same_file_name_fallback` · `rust_same_module_by_name` · `nim_stdlib_any` (any stdlib import admits the whole stdlib) · `odin_same_package` · `bicep_runtime_grammar` · `bash_shell_source` · `robot_dynamic_library_fallback` (first class in the imported file, name-free).

### Kept — legitimate bare-name (TRUE-GLOBAL) · ~6
No import needed; binds a real language/runtime global. **No action.**
`engine_generic_param` (declared type param) · `rust_prelude` (PRELUDE_NAMES + stdlib path) · `scala_implicit_import` (`java.lang`/`scala`/`Predef`) · `php_global_function` (`by_qualified_name` on a bare name — PHP globals live at global scope). (Ada's modular-type primitives, RM 13.7, are no longer a bind strategy — they classify via `AdaHooks::classify_external` → `Interfaces`.)

**Structural surprise (resolved for the explicit-member form):** Swift previously had **no** import strategy at all — `swift_by_name` did double duty as both "resolve via imported module" and "match any global symbol." LANG-SWIFT-1 part (a) adds the scope-directed `resolve_via_explicit_member_import` rung: an `import struct\|class\|enum\|… Module.Symbol` (dotted `module_path` ending in the imported name) now scopes a bind to the unique internal symbol of that name. The whole-module form (`import Foundation`) still scopes no bind — it needs a SwiftPM target → source-dir boundary map (deferred fork), so it is left for external classification rather than degraded to whole-program by-name.
