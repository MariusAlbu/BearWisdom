[
  {"name":"mutable array infers readonly constructor element", "source":"interface Payload { touch(): void } interface Result<T> { read(): T } interface Factory { new<T>(items: readonly T[] | null): Result<T> } declare const Build: Factory; declare const items: Payload[]; class Holder { value = new Build(items) } declare const expected: Result<Payload>;", "supported":true,"diagnosticCodes":[]},
  {"name":"readonly array infers constructor element", "source":"interface Result<T> { read(): T } interface Factory { new<T>(items: readonly T[]): Result<T> } declare const Build: Factory; declare const items: readonly string[]; class Holder { value = new Build(items) } declare const expected: Result<string>;", "supported":true,"diagnosticCodes":[]},
  {"name":"named readonly array matches readonly syntax", "source":"interface Result<T> { read(): T } interface Factory { new<T>(items: readonly T[]): Result<T> } declare const Build: Factory; declare const items: ReadonlyArray<number>; class Holder { value = new Build(items) } declare const expected: Result<number>;", "supported":true,"diagnosticCodes":[]},
  {"name":"nullable array preserves element inference", "source":"interface Result<T> { read(): T } interface Factory { new<T>(items: readonly T[] | null): Result<T> } declare const Build: Factory; declare const items: string[] | null; class Holder { value = new Build(items) } declare const expected: Result<string>;", "supported":true,"diagnosticCodes":[]},
  {"name":"array element literal does not widen", "source":"interface Result<T> { read(): T } interface Factory { new<T>(items: readonly T[]): Result<T> } declare const Build: Factory; declare const items: 'keep'[]; class Holder { value = new Build(items) } declare const expected: Result<'keep'>;", "supported":true,"diagnosticCodes":[]},
  {"name":"array syntax ignores local type namesakes", "source":"interface Array<T> { local: T } interface ReadonlyArray<T> { other: T } interface Result<T> { read(): T } interface Factory { new<T>(items: readonly T[]): Result<T> } declare const Build: Factory; declare const items: string[]; class Holder { value = new Build(items) } declare const expected: Result<string>;", "supported":true,"diagnosticCodes":[]},
  {"name":"local named array is not an intrinsic array", "source":"interface Array<T> { local: T } interface Result<T> { read(): T } interface Factory { new<T>(items: readonly T[]): Result<T> } declare const Build: Factory; declare const items: Array<string>; class Holder { value = new Build(items) }", "supported":false,"diagnosticCodes":[2345]},
  {"name":"readonly array cannot satisfy mutable constructor", "source":"interface Result<T> { read(): T } interface Factory { new<T>(items: T[]): Result<T> } declare const Build: Factory; declare const items: readonly string[]; class Holder { value = new Build(items) }", "supported":false,"diagnosticCodes":[2345]},
  {"name":"array constructor verifies fixed element type", "source":"interface Result { read(): void } interface Factory { new(items: readonly number[]): Result } declare const Build: Factory; declare const items: readonly string[]; class Holder { value = new Build(items) }", "supported":false,"diagnosticCodes":[2345]},
  {"name":"array constructor verifies generic constraint", "source":"interface Result<T> { read(): T } interface Factory { new<T extends string>(items: readonly T[]): Result<T> } declare const Build: Factory; declare const items: number[]; class Holder { value = new Build(items) }", "supported":false,"diagnosticCodes":[2345]},
  {"name":"readonly operator cannot decorate an alias", "source":"type Items = string[]; interface Factory { new(items: readonly Items): number } declare const Build: Factory; declare const items: string[]; class Holder { value = new Build(items) }", "supported":false,"diagnosticCodes":[1354]},
  {"name":"readonly tuple array inference remains explicit", "source":"interface Result<T> { read(): T } interface Factory { new<T>(items: readonly T[]): Result<T> } declare const Build: Factory; declare const items: readonly ['keep']; class Holder { value = new Build(items) } declare const expected: Result<'keep'>;", "supported":false,"diagnosticCodes":[]}
]
