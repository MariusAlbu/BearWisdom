// =============================================================================
// javascript/primitives.rs — JavaScript primitive types
// =============================================================================

// JavaScript shares the TypeScript primitive set — same runtime, same globals.
pub(crate) use crate::languages::typescript::primitives::PRIMITIVES;
