// Tests for `is_scala_builtin` were removed when the predicate itself
// was deleted (scala_stdlib + jdk_src + maven walkers cover the surface).
// Kept the file in case future predicate work lands here.
